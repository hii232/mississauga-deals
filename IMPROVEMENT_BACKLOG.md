# Improvement Backlog

Shared work queue for the scheduled improvement agents. Pick ONE item per run, check it off, and log it. Add new findings as you audit. Keep this file honest — it is the coordination point between runs.

**Mission: investor leads and conversions.** See CLAUDE.md. Every run should make the site better at (in order): capturing/converting leads, showing correct investor data and calculations, sending great weekly emails/alerts, looking clean and trustworthy, and delivering real value to investors.

## How to pick

Priority order: (1) anything broken or misleading — especially wrong numbers, (2) lead capture & conversion friction, (3) email/alert quality, (4) data & investor value, (5) design polish and SEO. Skip items marked in-progress by a run from the last 3 hours.

## 1 — Leads & conversion

- [x] Map every lead-capture path end to end (lead form, alerts subscribe, newsletter subscribe, booking): verified 2026-07-20 — /api/newsletter/subscribe and /api/lead both validate email (400 w/ message), succeed (200), degrade gracefully without env; alerts form has double-submit protection (disabled while loading). Fixed: alerts form now validates email format client-side and surfaces the server's error message instead of a generic failure. Booking path exercised 2026-07-20: validation order fixed (400s now precede DB check), no-DB fallback captures the booking via notification email instead of dropping it (503 only when no channel exists), email HTML now escapes user input
- [x] CTA hierarchy on listing detail 2026-07-20: page previously had NO booking/contact CTA at all. Added mobile sticky bottom bar (Book a Viewing primary + call icon, safe-area padded, under cookie-banner z-index) and a desktop Book a Viewing + Call pair beside the price, linking /book-call?listing={id}. NOTE: could not render with real listing data locally (AMPRE_TOKEN absent) — worth eyeballing on production after next release
- [x] Exit-intent / scroll-depth email capture 2026-07-21: ExitIntentPopup was already mounted site-wide via (public)/layout.js, BUT its only trigger was mouseleave — which NEVER fires on mobile, so the majority of traffic (~375px) never saw the capture. Added a scroll-depth trigger sharing the same one-shot + dismissed/registered guards: fires only on genuinely long pages (scrollHeight > 1.8× viewport), after real downward scroll (>300px) past ~60% — a high-engagement moment, not an on-load popup. Verified at 375px: hidden before the 10s arm even when scrolled, appears after arm + 60% scroll. Mobile lead capture now works on listing detail / blog / faq / listings etc.
- [x] Newsletter/alerts signup friction 2026-07-22: homepage EmailCapture is already a ≤2-action inline subscribe (type email → Subscribe Free → success renders in place, no navigation, posts to /api/newsletter/subscribe with UTM). Inline capture also on blog posts + neighbourhoods (InlineCTA). Verified — item met
- [x] QUIZ LEAD LEAK fixed 2026-07-23: the /quiz email step (a top-of-funnel capture — 5 answers + name + email + phone) validated only email PRESENCE (`if (!email)`) and used a bare `await fetch('/api/lead')` with NO res.ok check. So a typo'd email ("me@gmail") returned 400 from /api/lead, the uncaught failure was swallowed, and the visitor was shown their quiz results (marked user_registered) while the lead was SILENTLY LOST — Hamza never saw it. Added email-FORMAT validation (`/\S+@\S+\.\S+/`, the site-wide pattern) before submit + a res.ok guard that surfaces a real capture failure instead of faking success. Now a bad email is caught client-side (lead lands) and a genuine server failure prompts a retry rather than stranding the lead. Also added the same format check to the /pre-construction VIP form and the /sell valuation form (both already guarded res.ok, so not leaks — but a typo now gives an accurate "enter a valid email" message instead of a generic error, lifting completion). Build green; regex unit-verified (rejects me@gmail/abc/empty, accepts real emails).
- [ ] Lead magnets: gate the highest-value tools (cash-flow report download, sold comps, estimated value) behind email capture with clear value promise
- [x] Contextual CTAs on content pages + inline capture 2026-07-21: blog posts already end with booking + newsletter + quiz CTAs (verified). Upgraded the shared InlineCTA so the newsletter (blog posts) and alerts (neighbourhoods) variants CAPTURE EMAIL INLINE — type + one tap subscribes via /api/newsletter/subscribe right on the page — instead of bouncing the reader to /alerts to re-find a form (removed 1 page-load + re-orientation of friction on every content page). deals/quiz variants still link (they point to a tool). role=status/alert, sr-only label, UTM-tagged; verified valid→200 / invalid→400 + 375px render (stacked field, no overflow)
  - EXTENDED 2026-07-22: the 3 new SEO landing pages (rent-vs-buy, hurontario-lrt, townhouse-vs-condo) shipped with NO email capture — only links to /listings + calculator — so a reader who lands from search and isn't ready to browse had no way to convert. Added a mid-page InlineCTA (newsletter variant, honest "Top 5 Deals Every Week" copy → /api/newsletter/subscribe) between the body and the FAQ on all three. These are high-intent search-traffic pages; now they capture. Verified single h1 preserved (CTA is h3), email field renders, endpoint 400-on-invalid, build green
- [x] Admin leads dashboard 2026-07-21: sources already all visible (registration/quiz/google-signin/deal-alert/exit-intent/saved-search filter pills) and dedupe already happens at /api/lead (existing-email check). MISSING piece was EXPORT — no way to get leads out of the dashboard to work them in a spreadsheet/CRM/mail-merge. Added a client-side "Export CSV" button (downloads the currently-filtered set: name, email, phone, source, status, created, listing, price, calls, next-follow-up, notes) with proper CSV escaping (commas/quotes/newlines) + a UTF-8 BOM so Excel reads names correctly. Unit-verified escaping; build green. No lead is now stranded in the UI

- [x] ELITE SELLER PAGE built 2026-07-22 (Hamza request — "no page for anyone who wants to sell their home; need the most elite business page"): rebuilt /sell from a bare form into a high-converting seller landing. Split into a server page (SEO metadata targeting "sell my house mississauga" etc. + FAQPage & BreadcrumbList JSON-LD) + a client ValuationForm (components/sell/valuation-form.js, posts source 'seller-valuation'). Sections: dusk hero with the valuation form embedded (two-column, trust chips 5.0 Google/RECO/Cityscape + benefit bullets), 4 differentiators (investor-buyer network / data-driven pricing / full-service / investor's-eye), 3-step process, social proof (REAL GOOGLE_REVIEWS + 5.0 badge), seller FAQ, final CTA. HONEST — zero fabricated stats/commissions/timelines; everything routes to the free CMA. Surfaced it in the PRIMARY nav ("Sell" top-level, Blog moved into Resources so no crowding) + bumped sitemap priority 0.6→0.9. Verified single h1 + seller title + both schemas + canonical + form + real reviews + nav link site-wide on a clean prod build. NEEDS HAMZA (optional): send real track-record stats (homes sold, avg DOM, % over asking) to add hard proof, and confirm any specific seller guarantee/offer to feature.

## 1c — Conversion optimization sprint (2026-07-23, Hamza: "perfect the design for high conversions")

The capture paths exist; this sprint squeezes conversion rate out of them. Priority order below = do the topmost unchecked one per run. Design/improvement agents: keep changes honest (no fabricated counts/urgency), verify at 375px, and preserve every existing capture path. Measure-first items feed the rest.

**A. Cut friction + one clear CTA per screen (no product decision needed — do these first):**
- [~] Audit each key page (homepage, listing detail, /listings, blog post, guides, /neighbourhoods) for competing CTAs; make ONE primary action visually dominant per screen, demote the rest to secondary/ghost. Especially the homepage hero (currently "Unlock N Premium Deals" + "Browse Deals" + search + nav "Sign Up" all compete).
  - [x] HOMEPAGE HERO 2026-07-24 (design): the hero had a near-equal-weight ghost "Browse Deals" button sitting beside the accent "Unlock N Investment Deals — Free" capture CTA — and it merely DUPLICATED the search bar's destination (/listings). Two browse paths (search bar + "Browse Deals" button) visually out-weighed the single lead-capture button. Demoted "Browse Deals" from a filled bordered button to a light "Browse all deals →" text link (white/70, ~8:1 on the dusk navy → WCAG AA) so the accent CTA is now the ONE dominant filled action; the accent button also goes full-width on mobile for clear primacy. Browse path preserved, no capture path touched. Verified at true 375px (headless CDP): no horizontal overflow (sw=cw=375), single h1, primary button dominant, link demoted. Remaining screens for this audit: listing detail, /listings, blog post, guides, /neighbourhoods (most already have a single sticky-CTA primary).
  - [x] FIXED 2026-07-23 (improvement agent) — TWO fabricated hero numbers on the #1 page: (a) the primary CTA hardcoded "Unlock 388 Premium Deals — Free" in `hero-buttons.js`, and (b) the hero SUBTITLE's `heroCount` fell back to a hardcoded **"2,000+"** whenever `topDeals.totalCount < 500` OR the feed was down — so the real ~few-hundred Mississauga inventory rendered as "2,000+ Mississauga Investment Properties" (a ~5× overstatement). Now both use ONE real feed-driven count: exact for <500, rounded-down-to-hundred "N+" for ≥500 (never overstates), and NULL when the feed returns nothing (the hero omits the number instead of fabricating one). Button copy also de-overclaimed "Premium" → "Investment Deals". Build green; verified at 375px (fallback path renders cleanly, subtitle reads naturally with no number). NOTE (not changed, judged acceptable): the "About Hamza" bio stat "2,000+ Properties Analyzed" + the page meta description are CUMULATIVE lifetime claims (not live inventory) — defensible and rounded; Hamza can supply a real cumulative figure if he wants precision.
- [x] Email-first capture: every multi-field lead form should capture email on step 1 and defer name/phone to step 2 (progressive). FULLY CLOSED 2026-07-25 — the signup-gate modal (the last form) was already correctly two-step with a lead-capturing skip, but its step 2 still HARD-REQUIRED a phone, which contradicted /signup and /api/lead after both dropped that requirement earlier today. So a visitor on the highest-intent surface on the site — a specific property's full analysis — who had already handed over a real email and name could be turned away over the one field people most resist. Phone is now optional there too, still validated when supplied. Also removed a now-obsolete workaround comment: the email-only skip path used to avoid source 'registration' specifically because the API would 400 without a phone; that rule is gone, and the distinct source is now kept only so Hamza can see in admin which leads skipped step 2. DONE 2026-07-25 (improvement agent) for /alerts — the destination of the main "Sign Up Free" CTA and the new homepage mobile sticky bar, so it is the form most cold traffic meets. It showed SIX fields (name, email, and four filter selects) when only email was ever required; the four selects already defaulted to "Any", so the extra fields bought nothing and simply read as work. Email + submit are now the only things visible; name and all four filters sit one tap away in a native <details> disclosure. Deliberately NOT a two-step state machine — the form still submits in a single POST, so a lead can never be stranded between steps, and every existing capture path/filter is preserved byte-for-byte. Measured at a true 375px mobile viewport: form height 298px collapsed vs 721px expanded (58% shorter), no overflow, 60px summary tap target; screenshot confirms email + button + trust line all sit above the fold on a phone where the button previously sat below six fields.
- [x] Sticky mobile CTA on the pages that lack one: homepage, blog post, the 5 guides, /neighbourhoods (listing detail already has it). Single primary action, safe-area padded, below cookie-banner z-index — mirror the listing-detail sticky bar.
  - DONE 2026-07-25 (design) for the HOMEPAGE — the last open page, previously deferred pending the one-primary-action hero audit, which resolved 2026-07-24 (hero now has ONE dominant accent CTA). Added the proven StickyMobileCTA: "Get Free Deal Alerts →" → /alerts, chosen over mirroring the hero's /signup button because the hero CTA is auth-state-dependent (logged-in users see "Find My Deal" → /quiz) while /alerts is a real name+email capture that serves every visitor. Same z-[150]/safe-area/lg:hidden recipe (under cookie banner + exit-intent), built-in spacer so nothing hides behind it. Verified in SSR markup on a live dev render; component 375px-verified on 30+ pages. Item 34 now fully closed.
  - DONE 2026-07-23 (design) for the 24 NEIGHBOURHOOD GUIDES (the [slug] route, high-traffic SEO landing pages): added a `fixed bottom-0` mobile-only (`lg:hidden`) sticky bar with ONE primary action — "View {Hood} Listings →" → `/listings?hood={Hood}` (the filtered view, which now works after the ?hood= fix). z-[150] keeps it under the cookie banner (z-[200]) and exit-intent (z-[300]) — mirrors the proven listing-detail bar; safe-area padded (`pb-[max(0.625rem,env(safe-area-inset-bottom))]`); added `pb-28` to the content on mobile so nothing hides behind it (no layout shift). Verified at true 375px (headless CDP, cookie banner dismissed): accent button, WCAG-AA, on-brand, content clear above it.
  - DONE 2026-07-23 (design) for the 5 INVESTOR GUIDES: extracted the pattern into a reusable `components/layout/sticky-mobile-cta.js` (StickyMobileCTA — same z-[150]/safe-area/lg:hidden recipe, with a built-in in-flow mobile spacer so pages need no wrapper edits) and dropped it into all 5 guides with a page-relevant primary action: cash-flow-positive → `/listings?cf=1&sort=cashflow` "See Cash-Flowing Listings", rent-vs-buy → `/mortgage-calculator` "Run Your Numbers — Free", townhouse-vs-condo → `/listings` "Browse Scored Listings", hurontario-lrt → `/listings?lrt=1` "See LRT-Corridor Listings" (lrt is a real filter param), rental-insurance → `/mortgage-calculator` "Estimate Your Carrying Costs". All 5 verified rendering in SSR + one 375px viewport capture (accent bar, AA, hero intact).
  - DONE 2026-07-23 (design) for BLOG POSTS (the site's TOP-traffic pages per Hamza's live analytics — 117/52/52/36 visits on the top posts): one StickyMobileCTA insertion in the shared blog/[slug] route covers every post — "Get Free Deal Alerts →" → /alerts (the real name+email capture form). Long articles previously had no persistent mobile action (the newsletter InlineCTA sits in the sidebar; the booking CTA is at the very end). Build green; can't render a post locally (no Supabase → notFound) but the component is byte-identical to the one 375px-verified on 29 other pages — worth a production eyeball on a live post. STILL OPEN: homepage (deliberately deferred — its hero already has competing CTAs; resolve with the 1c-A one-primary-action audit rather than adding another).
- [ ] Reduce homepage time-to-capture: the weekly-email capture is far down the page; add a compact capture in/near the hero (or make the hero CTA go straight to email capture, not /signup).
- [x] BROKEN HERO SEARCH fixed 2026-07-23 (seo — found during a SearchAction schema audit): the homepage hero search (the #1 page's primary find-a-property action) navigated to `/listings?search=<term>`, but `/listings` reads/writes the search term as **`q`** (deserializeFilters/serializeFilters) — and `?search=` is a param it never reads — so the term was silently dropped: every hero search landed on unfiltered listings. Fixed HeroSearch to emit `/listings?q=` — now consistent with the listings filter system AND the WebSite SearchAction schema (which already advertised `?q={search_term_string}`), so the on-site search and Google's sitelinks searchbox hit the same working endpoint. Verified end-to-end: `/listings?q=Cooksville` → 200, term reflected in SSR, applyFilters filters on it. Also added an aria-label to the search input (was placeholder-only). No calc/data change.

**B. Sharpen the offer / lead magnets (NEEDS HAMZA to confirm the offer — see Needs Hamza; default proposed below):**
- [x] 2026-07-23 (Hamza approved) Sharpened the offer to "Get the 10 best cash-flowing Mississauga deals — free, every Monday": homepage EmailCapture (headline/sub/button "Send Me the Deals"), newsletter InlineCTA (dropped the unverifiable "200+ investors" claim), and the exit-intent popup — which also UNIFIED onto this weekly offer: was promising "daily alerts" (needs a saved search; overpromised to a bare email) and now subscribes to the weekly top-10 via /api/newsletter/subscribe (deliverable to any email, still inserts the lead so Hamza gets the contact). Build green.
- [x] Make the "first month's mortgage on us" offer more prominent + repeated — DONE 2026-07-25 (design agent) at the point that matters most: /book-call. The offer is the strongest reason to book, but it lived ONLY in one homepage band, so every visitor arriving straight from a listing's "Book a Viewing" sticky CTA (the site's highest-intent path) reached the calendar never having seen it. Added a compact accent-tinted band above the calendar with the same honest copy as the homepage (credit on closing, all investment properties qualify) — no new claims invented. AA note: first drafted the "EXCLUSIVE OFFER" chip in success-green, caught that #10B981 on a light tint measures ~2.2:1 (the same failure fixed on the homepage proof text and the blog sidebar label) and also that `text-success-dark` is NOT a token in tailwind.config.js and would have silently no-opped; shipped accent-dark on an accent tint instead, measured 6.7:1 (body copy 14.2:1). Verified at 375px: no overflow, chip stacks above the copy. Remaining echo spots if wanted: the /quiz result screen and the listing-detail booking CTA.
- [ ] Gate the highest-value tools behind email with a clear value line (cash-flow report download, sold comps, estimated value) — ties to the existing section-1 lead-magnet item.

**C. Real social proof + honest urgency (use REAL numbers only — pull live counts, never fabricate):**
- [x] Deal freshness/urgency 2026-07-23 (design): added an honest "New" badge to the listing cards (ListingCard on /listings + /gta, and HomeDealCard on the homepage) — a top-left accent pill mirroring the top-right score badge, shown only when real days-on-market is 1–7. Deliberately EXCLUDES dom=0 (that's the missing-data fallback in process-listings, so it can never false-positive a listing of unknown age) and never overlaps "Motivated" (dom≥45). No invented scarcity: "N people viewing" was intentionally omitted (no real viewer signal exists); score + days-on-market were already shown. Absolute-positioned (no layout shift), white-on-accent (AA), verified composition at card scale/375px. Minor follow-up: the same "New" pill could be echoed on the listing-detail page (it already shows days-on-market as a metric).
- [ ] Surface real aggregate proof where it builds trust: # active listings tracked, # deals scored, Google 5.0 (28) — all real. If subscriber/alert counts are shown, they must read from Supabase, not a hardcoded number.
- [x] Review specificity 2026-07-23 (design): the homepage primary email-capture band (weekly-deals signup — the retention engine's front door) had ZERO social proof, while the hero only had a rating chip (no quote). Added proof at that decision point — 5 gold stars + a real, investor-relevant Google review ("First time working with Hamza on the purchase of our first investment property…" — Ryan Yau, imported from GOOGLE_REVIEWS with a name-match + first-review fallback so it never renders empty) + "Verified Google review · 5.0 on Google (28 reviews)". WCAG-AA: first drafted the "Verified" line in success-green (#10B981 on the #E6F1FB band ≈ 2.2:1, fails) — moved all proof text to navy/70–80 (passes) and kept the gold stars decorative (aria-hidden; the 5.0 is conveyed in text). Verified at true 375px (headless-Chromium): no overflow, readable, premium/on-brand. Real data only. Remaining (optional): a second review near another primary CTA.

**D. Measure then optimize (so the next round is data-driven, like the GSC work):**
- [x] GA4 conversion events DONE 2026-07-23 (improvement agent): every capture path now fires a distinct GA4 event from its SUCCESS branch only (server capture confirmed → runs once per submission, so no double-fires; several success states also replace the form, preventing resubmit). New `lib/track-conversion.js` helper is crash-proof (no-ops without gtag/SSR/ad-blocker, swallows gtag throws — unit-verified incl. exactly-once recording). Wired 10 components / 12 events: newsletter_subscribe (homepage capture, inline-cta w/ per-variant source, exit-intent), alert_create (alerts page, save-search button w/ city), lead_submit (quiz, seller-valuation, precon-vip), booking_create (book-call), signup (gate-complete, gate-email-only). Each event carries a `source` param so GA shows which surface converts. Build green. REMAINING for Hamza (in GA4 UI, not code): mark these events as conversions and build the funnel report.
- [ ] Microsoft Clarity is already installed — NEEDS HAMZA to watch ~10-15 session recordings + heatmaps on homepage/listing-detail/signup-gate and report where people drop; that observation feeds concrete fixes here.

**E. Performance (slow pages lose leads — the site is now image-heavy):**
- [~] LCP/CLS pass on homepage + listing detail: convert remaining raw <img> to next/image with correct sizes, ensure the hero/deal photos don't cause layout shift, lazy-load below the fold. (preconnect to the photo CDN already done.)
  - CLS FIX 2026-07-23 (seo): the blog-post cover — the LCP element of the site's TOP-traffic page type — rendered as an unsized `w-full` img, so the entire article shifted down ~200-400px when it loaded. Now a fixed `aspect-[1200/630]` box (matches the generated covers; variable-aspect Unsplash covers crop consistently via object-cover) + width/height attrs + decoding=async → the area is fully reserved pre-load, hero CLS = 0. Audited the other image surfaces: listing-detail hero (aspect-[4/3] + object-cover), listing/deal cards (fixed h-*), blog index (h-44), related posts (h-28) — all already CLS-safe. Remaining: next/image conversion (needs remotePatterns config for the MLS CDN — larger change).

## 2 — Data & calculations (must be PERFECT)

- [x] Homepage "Top neighbourhoods" data was STATIC (Hamza flagged 2026-07-22): the Hot/Warm/Cool badge, avg price, YoY, DOM and yield for all 24 hoods were hardcoded in HOOD_DATA (lib/constants.js), last touched April 2026, presented as if current. Per Hamza's call ("live stats + label the rest"): avg price, avg DOM and rent yield now compute LIVE per-neighbourhood from the active listing feed (reusing the listings fetchTopDeals already does — zero extra fetch; ≥4-listing sample threshold, else curated fallback), the top-4 now rank by live yield, and a green "Live from active listings" pill marks live cards. Trend + YoY stay curated but are now LABELED as "Hamza's expert outlook (last reviewed {HOOD_OUTLOOK_AS_OF})" with a YoY* footnote. Graceful fallback to curated when the feed is down. Unit-tested the aggregation; build green; homepage 200, single h1.
- FOLLOW-UP DONE 2026-07-22: rolled the same live merge onto the /neighbourhoods index + detail pages. Extracted computeHoodStats to lib/listings/hood-stats.js (shared) + added a GET /api/neighbourhood-stats endpoint (revalidate 600) so both a client page (index) and a server page (detail) can consume it. Index fetches it client-side + merges; detail is now async ISR (revalidate 600) fetching server-side, and its worked cash-flow example + FAQ + FAQ-schema now use the live avg price/DOM/yield too. Trend/YoY/inventory/scores stay curated + labeled (* footnote, "last reviewed April 2026"), Live pills on live cards, graceful curated fallback when the feed is down. Build green (87 pages); endpoint returns valid JSON; both pages 200, single h1, FAQPage schema intact.

- [x] Mortgage calculator math audited 2026-07-20 — ALL VERIFIED against references: payment formula exact ($500K @ 5%/25yr = $2,908.02, semi-annual compounding), Ontario LTT brackets correct (marginal 0.5→2.5%), CMHC tiers correct (4.00/3.10/2.80% + 8% ON PST), stress test correct (max(rate+2%, 5.25%)), min down payment correct (5%/10%/20% tiers). FIXED: added warning when <20% down is paired with >25yr amortization (insured mortgages capped at 25yr except FTB/new builds). Plain-language explanations = the FAQ section added earlier today
- [x] Listing-card derived numbers hardened 2026-07-21: capRate/cashOnCash rendered via .toFixed unguarded — a single malformed listing (undefined/NaN field) would THROW and blank the entire listings grid. Added pct1()/money() defensive formatters (valid number → formatted; NaN/undefined → '—', never a crash or NaN); also guarded unit-rent/basement/condo-fee renders. Verified formatters + build. Engine formulas already guard divide-by-zero (capRate/CoC return 0 on price<=0)
- [x] HOMEPAGE deal cards hardened 2026-07-22: HomeDealCard (the homepage "top deals" grid — the single most important page) rendered deal.capRate.toFixed(1) UNGUARDED, so one malformed deal would THROW and blank the whole deal section; hamzaScore badge + cashFlow could also print "undefined"/"$NaN". Added the same pct1() guard + finite checks (score/cap/cash-flow → '—' fallback, never throw/NaN); also guarded the ListingCard score badge (last unguarded number on that card). Unit-tested malformed deal (no throw, clean '—'); homepage + /listings render 200; build green
- [x] ALL remaining listing-view number renders hardened 2026-07-22: swept the rest of the public listing surfaces that still had the same unguarded-.toFixed throw risk — the Top-Picks mini card (listings-container.js), the table view (listing-table.js), and the map popups (listing-map.js) all called capRate/cashOnCash.toFixed(1) raw. Made it a DRY fix: added a guarded pct1() to lib/utils/format.js and hardened fmtK/fmtNum/fmtCurrency to return '—' instead of "$NaN" on non-finite input (used site-wide), then routed those views + the deal-screener aggregate tiles through the guards. Unit-tested (undefined/NaN/null → '—', valid unchanged); /listings + homepage 200; build green. Defensive-number rendering is now consistent across every listing card/table/map/screener
- [x] Sold comps audited 2026-07-21: type/beds±1/baths±1/FSA filters correct, BUT had NO recency window — 2-year-old sales could skew "current market" averages. Fixed: 180-day ModificationTimestamp window on targeted queries (broad fallback stays unwindowed but is now labeled); freshness caption under stats ("Based on N closed sales in the last 180 days..."), honest "(older sales included)" note when the fallback fires. Rental-comps recency VERIFIED 2026-07-21 — already correct: 12-month window + 18-month wider fallback + active-rental tier, each with an honest freshness label. No change needed
- [x] Market-pulse stats 2026-07-21: verified aggregation math (sale-to-list = (100+negotiationGap)/100, avg DOM/price means — all correct). Real gap was PROVENANCE: page blends continuously-updated live MLS stats with a FIXED TRREB monthly snapshot (Feb 2026) and presented both as "current" — 5-month-old sale-to-list/inventory/YoY read as today's numbers. Added `tRREBAsOf` ISO date to /api/market-stats and a data-provenance strip on the page (live-vs-monthly split + "as of {month}"), plus an as-of caption on the Avg-Prices-by-Type chart (which silently falls back to TRREB sold prices). NEEDS HAMZA: refresh the hardcoded TRREB Feb 2026 block in /api/market-stats when newer Market Watch data is published
- [x] Estimated-value display verified 2026-07-21: already shows range + confidence badge + comp count (not false precision — good). Hardened the render: range only shows when both bounds are finite, confidence badge/comp-count guarded against undefined (were unguarded — confidence.charAt would throw, could show 'undefined comparable sales')
- [x] WRONG-NUMBER fix 2026-07-21: listing-detail interactive calculator (Mortgage tab) OMITTED CONDO FEES entirely — calculateCashFlow had no condo-fee param, so a condo's "Potential Cash Flow", "Cash-on-Cash" and Monthly Breakdown ignored the $400-800/mo fee while the listing card (process-listings) correctly included it. A cash-flow-negative condo could read positive. Fixed calculateCashFlow to accept monthlyCondoFee and, like the listings engine, replace the rent-based maintenance reserve with the actual condo fee (no double-count); passed listing.condoFee at both call sites (cash flow + cash-on-cash); added a Condo Fee breakdown row (replaces the Maintenance row for condos). Hand-verified: $500k/$2500 condo w/ $600 fee → maintenance $417→$0, condo fee +$600, cash flow correctly −$183/mo vs before; parts sum == total. Still open: break-even rent (needs a solve; deferred)
- [x] WRONG-NUMBER fix (part 2) 2026-07-21: same condo-fee omission existed in the listing-detail Analysis tab's CAP RATE — calculateNOI was called without monthlyCondoFee, so a condo's NOI excluded the fee and its cap rate read ~1 pt too HIGH vs the card. Added the same "condo fee replaces maintenance reserve" logic inside calculateNOI (process-listings already zeroed maintenance for condos, so unaffected), passed listing.condoFee at the Analysis-tab call site, and added a Condo Fee row to the NOI Breakdown. Hand-verified: $500k/$2500 condo w/ $600 fee → cap rate 4.08%→3.12% (now matches the card). Condo-fee consistency now closed across all 3 metrics (cash flow, cash-on-cash, cap rate/NOI)
- [x] Break-even rent added 2026-07-21 (closes the "add investor metrics" item — cash-on-cash + monthly carrying breakdown already existed): new breakEvenRent() engine helper + a line on the listing-detail Mortgage tab showing the gross monthly rent at which the property covers every cost, and whether the estimated rent clears or falls short of it. Solved NUMERICALLY against the actual calculateCashFlow (binary search on the monotonic cash-flow-vs-rent curve) so it can NEVER diverge from the cash-flow figure shown beside it, and it inherits the correct freehold-maintenance vs condo-fee handling automatically. Hand-verified: freehold $700k → $4,672/mo (matches closed-form; cashFlow=0 at that rent, clean ±$1 crossing), condo $500k+$600 fee → $3,554/mo. Recomputes live with the sliders. Build green

- [x] Equity-build ("forced savings") shown on the listing calculator 2026-07-22: the CAP/PCF question from Hamza exposed that negative cash flow reads as "losing money" when most of it is principal paydown the investor KEEPS — a conversion-killing misread that nothing on the site corrected. Added a verified mortgageSplit() engine helper (first-month interest/principal split, same Canadian semi-annual compounding as calcMonthly) and a green line on the listing-detail Mortgage tab: "You build ~$X/mo in equity … of the $Y payment, $X pays down principal (year one) — money you keep." When cash flow is negative AND equity build exceeds the shortfall, it honestly adds "your net worth still grows." Recomputes live with the sliders. ADDS a line only — no existing number (cash flow, cap rate, score) changed. Hand-verified: $630k→$867/mo, $999k→$1,374/mo, $800k→$1,100/mo principal (exact match to the earlier CAP/PCF reconciliation); build green.
- [x] **WRONG NUMBER — Toronto municipal LTT missing from cash-on-cash — FIXED 2026-07-25 (improvement agent).** New `calculateTorontoMLTT()` (Toronto's published residential schedule: provincial-mirroring brackets to $3M, then the 2024 luxury tiers 3.5/4.5/5.5/6.5/7.5%), `chargesTorontoMLTT()` (matches Toronto AND the five amalgamated districts — Etobicoke/North York/Scarborough/East York/York — which our GTA feed reports under district names, the same trap the property-tax table had to fix), and `calculateLandTransferTax(price, city)` = provincial + municipal-when-Toronto. City is now threaded into `calculateCashOnCash()` and `getClosingCosts()` at all three call sites (process-listings for every card, plus both listing-detail tabs), and the detail page's closing-costs panel gained a real "Toronto Municipal LTT" row — replacing the footnote that used to apologize for the omission. HAND-VERIFIED against published figures: $1M provincial $16,475 (the standard reference) + Toronto MLTT $16,475 = $32,950 combined; $500K/$1.5M/$2M/$2.5M all match; luxury tiers rebuilt bracket-by-bracket ($4M = $96,475, $5M = $141,475); parts sum to totals; no city passed = byte-identical old behavior (Mississauga unaffected). IMPACT: a $1M Toronto purchase needs $235,950 at closing, not $219,475, and a positive-cash-flow Toronto deal that read 4.10% cash-on-cash now correctly reads 3.81%. Original finding text kept below for the record:
- ~~**Toronto municipal LTT is missing from cash-on-cash (found 2026-07-25 by the seo agent)**~~ `calculateOntarioLTT()` is the only land-transfer-tax function in the codebase (`lib/cash-flow-engine.js:192`), and both `calculateCashOnCash()` and `getClosingCosts()` call it with price alone — no city. Toronto (and its amalgamated districts Etobicoke / North York / Scarborough / East York / York) charges a **municipal** LTT on top of the provincial one, at roughly the same brackets, so the real closing cost there is about DOUBLE what we show. On a $1M Toronto purchase that is ~$16.5K of cash required that we omit, which INFLATES the cash-on-cash return on every Toronto-area GTA listing (smaller denominator) and flatters those deals against Mississauga ones. Fix needs: a city-aware LTT (municipal component for the 6 Toronto names, first-time-buyer rebates are irrelevant for investors), threaded through both call sites plus the listing-detail closing-costs breakdown. Hand-verify against a known example before shipping. NOTE: task list #8 was marked "Add Toronto municipal LTT to cash-on-cash for GTA listings" as completed — the code shows it never landed; treat that checkmark as wrong.
- NEEDS HAMZA (flagged 2026-07-22): the cash-flow engine uses a FLAT monthlyInsurance=$225 for every property regardless of price/type (a $1.5M detached and a $500K condo both get $225/mo). That's a deliberate simplification, not a clear bug, but it understates carrying cost on expensive homes and affects every listing's cash flow/cap rate/score site-wide — so it needs a product decision (keep flat, or scale by value/type?) before changing. Not touched.

## 3 — Weekly emails & alerts

- [x] CRITICAL (QA 2026-07-21): daily /api/alerts/send cron was 500-ing every day — NO alert emails were going out. Root cause: it built the internal listings fetch from `new URL('/api/listings', request.url)`; on a cron invocation request.url is the *.vercel.app deployment host, which sits behind Vercel deployment protection and serves an HTML auth wall (200), so `.json()` crashed on "<!DOCTYPE ...". Confirmed via Vercel logs (13:04 UTC 500; no /api/listings function invocation). Fixed to fetch from the absolute public SITE_URL (same pattern listing metadata already uses) + added a content-type guard so a non-JSON body throws a clear error instead of a cryptic JSON.parse. Verify next 13:00 UTC run returns 200 + Resend acceptance

- [x] Weekly newsletter template audited 2026-07-20: table layout + inline styles + 600px max all correct, UTM params and unsubscribe link present, user fields escaped. Added ?preview=1 test-render mode (dev-open, auth-gated in prod). FIXED: template fabricated stats when data was missing ("2,500+" active listings, avgDOM 28, sale-to-list 97.2%) and rendered "N/A" tiles — stat blocks now show only real data and hide themselves otherwise. Dark-mode-safe pass still open
- [x] CRITICAL follow-up 2026-07-21: the SAME request.url self-fetch bug that broke the alerts cron also lived in /api/newsletter/weekly (fetchScoredListings built the listings URL from request.url). On the Monday 14:00 UTC cron that host is the protected *.vercel.app deploy → HTML auth wall → the fetch's try/catch swallowed the JSON.parse crash and returned [], so the weekly newsletter would have shipped with ZERO deals (its entire point) with no error surfaced. Fixed to the hardcoded public SITE_URL (matching the market-stats fetch right above it) + content-type guard + error logging so a failure is diagnosable, not silent. Preview verified: renders, 0 "N/A". NEEDS HAMZA: still ships only via the next daily release
- [x] RENT ASSUMPTION NOW IN BOTH EMAILS 2026-07-25 (improvement agent): the site's listing cards state "Est. rent $X/mo · every figure below assumes this rent" on every card — added after an investor's critique that a cash-flow number with an unstated rent assumption isn't credible — but BOTH emails shipped the figures bare. The daily alert row and the weekly newsletter's hero + "Also on the Market" rows showed CAP/cash-flow with no indication of what rent produced them, so the channel that reaches subscribers directly was the one place the assumption stayed hidden, and a skeptical investor had no way to sanity-check "+$450/mo". Both templates now render the same line, reading the SAME fields the card's RentAssumption component reads (estimatedRent / baseRent / basementIncome / basementTier / unitBreakdown) so email and site can never disagree, with the suite and multi-unit breakdowns that make large totals believable. Renders NOTHING when rent is unknown rather than inventing one. Unit-verified 7 shapes against the real source (plain, legal suite, potential suite, multi-unit, zero/undefined/NaN rent → blank) and confirmed in a live newsletter preview render: "Assumes $4,300/mo rent — $3,100 main + $1,200 legal suite". Also gave the preview fixtures rents so the preview actually exercises the line. Remaining newsletter item: leads with the best deals + one useful market stat; subject lines specific not clickbait; correct unsubscribe link in every send. SUBJECT LINE done 2026-07-21: the generic subject (most of the 477 imported subscribers have no saved search) was a flat "Mississauga Market Weekly — {date}" with no hook. Now leads with the top deal's REAL, honest hook via buildSubject(): "+$X/mo cash flow in {hood}" when it cash-flows, else "N new deals — top pick X.X% cap rate in {hood}", else clean fallbacks. All values guarded (never N/A/NaN/undefined — unit-tested across 6 cases); honest (won't claim cash flow a negative-CF deal doesn't have). Email already leads with deals + market numbers, and unsubscribe link is in every send (signed + List-Unsubscribe). Remaining: could A/B test subjects later
- [x] Deal alerts: verify matching logic against saved filters (no misses, no floods), cap per-email volume, dedupe listings already sent. AUDITED 2026-07-20: per-email dedupe + 15-listing cap + DOM≤3 freshness gate all correct, BUT the DOM≤3 window can resend the same listing up to 4 consecutive days — proper fix needs a sent-listings table (Supabase migration; product call on schema)
  - FIXED 2026-07-25 (improvement agent): new `supabase/migrations/create_alert_sent_listings.sql` (email+listing_id PK, sent_at index, RLS) + repeat-send guard in /api/alerts/send: one batched 7-day-lookback query per run builds an email→Set(listing_id) map; each search's matches drop already-sent ids BEFORE the DOM/cap slicing (first-alert path too, so a second saved search can't re-deliver what an earlier alert showed); rows recorded via upsert ONLY after Resend accepts (a rejected send stays eligible); 14-day best-effort cleanup keeps the table small. DEGRADES GRACEFULLY: if the migration hasn't run or any guard query fails, the sent-set stays empty and behavior is byte-identical to before — dedupe can never block a send. Unit-verified 5 cases (repeat excluded, empty-set = old behavior, first-alert exclusion, numeric/string id coercion, all-repeats → email skipped). NEEDS HAMZA (one-time): run the new migration in Supabase SQL editor to turn the guard on — until then the run logs a "repeat-send guard unavailable" note and sends as before.
- [x] Alert/newsletter emails UTM attribution — complete 2026-07-20: newsletter already had UTM everywhere; daily alert email now tags listing links + CTA with utm_source=alerts&utm_medium=email&utm_campaign=daily-alert. Also escaped name/address interpolation in alert HTML
- [x] Email number-guard hardening 2026-07-22: the daily alert email rendered hamzaScore.toFixed()/cashFlow.toLocaleString() unguarded → a malformed listing would THROW and abort the send (subscriber gets nothing). Now every rendered number falls back to '—' safely; negative CF shows −$X/mo. Same finite guards added to the weekly newsletter hero + rows (were cosmetically capable of "Rated undefined/10" / "−$NaN/MO"). Unit-tested + build green
- [x] Confirmation email complete 2026-07-21: new newsletter subscribers get an instant branded welcome (magazine style) setting expectations — what (weekly top deals + market numbers), when (Mondays, one email/week), from whom — with signed unsubscribe + one-click List-Unsubscribe headers. Fire-and-forget, never blocks signup, only sends for genuinely new subscribers

## 4b — Visual redesign rollout (started 2026-07-21, "Mississauga dusk" identity)

Hamza's direction: the site was too plain, no imagery, not enough trust. New visual identity now in `components/art/cityscape.js` (layered Mississauga skyline w/ Absolute World towers, dusk + night variants, SkylineStrip divider), `components/art/scene-icons.js` (illustrated How-It-Works scenes), `components/layout/page-hero.js` (shared skyline hero). **Previous design is preserved on branch `design-v1-backup` — never delete that branch.**

Done: homepage (hero w/ floating live deal card + trust chips + skyline, illustrated steps, tinted neighbourhood cards, review cards w/ avatars + verified badge, framed agent photo + credential chips, night-skyline CTA), blog index hero, mortgage-calculator hero, neighbourhoods hero, market-pulse hero, GTA hero.

Design-agent propagation queue (use PageHero / CityscapePanorama / SkylineStrip; keep pages consistent; ~≤300 lines per run):
- [x] Listing detail page 2026-07-22: brought onto the dusk identity — the analysis tabs (Overview → BRRR) previously appeared with NO section heading; added a titled "Investment Analysis" h2 with a SkylineStrip filling the row beside it (subtle brand divider + real heading hierarchy + an SEO-friendly h2). Photo gallery confirmed prominent (lg:col-span-3, hero-first). Sticky CTA untouched. Build green; markup is overflow-safe at 375px (nowrap heading + flex-1 strip in max-w-5xl px-4). NOTE: full render needs live MLS data (AMPRE_TOKEN absent locally) — worth a production eyeball, but markup risk is nil
- [x] /pre-construction + precon detail: PageHero + card imagery treatment — /pre-construction DONE 2026-07-21 (compact dusk PageHero, single h1). Precon PROJECT-CARD imagery DONE 2026-07-22: the no-image placeholder was h-32 (vs h-44 for photo cards → uneven grid rows) and off-brand (grey gradient + lone 🏗️ emoji). Rebuilt it as a dusk-gradient + SkylineStrip silhouette placeholder at h-44 (even rows, on-brand, no layout shift), switched its status/type badges to the dark-bg backdrop-blur variants (AA contrast on the dusk bg), and aligned the card hover to the standardized premium treatment (hover:border-accent/30 + shadow, duration-300). Fallback projects have no image_url so this is the branch that renders. Build green, page 200. NOTE: placeholder only paints after the client fetch falls back (needs JS); markup is overflow-contained — worth a production eyeball
- [x] PageHero/skyline rollout COMPLETE: /faq + /book-call + /about + /alerts + /quiz (Jul 21) + /sell (Jul 22) all done. /sell (the seller-valuation lead page) was the last holdout — brought onto the dusk identity WITHOUT restructuring the form: added a compact dusk PageHero (single h1 "What's Your Property Worth?") above the untouched two-column layout, and demoted the old left-column section-title h1 to an h2 lead-in ("How your free valuation works") so there's exactly one h1. Form JSX byte-unchanged. Verified single h1 + dusk gradient + form fields intact on a clean prod build; PageHero is already 375px-verified and the grid stacks single-column on mobile.
- [x] /recent-sales, /compare, /news, /score-methodology: PageHero (compact) — ALL DONE 2026-07-21 (dusk PageHero, single h1 verified, 375px no overflow). /compare kept Back + Clear-All as a control row below the hero (static subtitle avoids layout shift); /news relocated its Updated + Live-Feed badge + Refresh button into a control row below the hero. PageHero rollout on the secondary/content pages is complete (only /sell intentionally left — integrated two-column form hero).
- [x] Blog post page ([slug]) 2026-07-21: brought the hero onto the dusk identity — dusk gradient (from-[#16223D] via-navy to-[#25355C]) + CityscapePanorama skyline (identical pattern to the 375px-verified /about hero), and added a SkylineStrip divider before the newsletter CTA. Also fixed real WCAG-AA contrast failures in the hero + booking CTA (date/read-time white/40→/70, excerpt /50→/75, Google-rating line /30→/60 — all failed on navy). Build green; page degrades gracefully (notFound without Supabase). NOTE: can't render a real post locally (no Supabase) — worth an eyeball on a live post, but hero markup matches the verified /about pattern
- [x] 404 page 2026-07-21: redesigned onto the dusk identity — night CityscapePanorama anchored to the bottom, deeper night gradient, and FIXED real WCAG AA failures (recovery links were text-white/30 and body text-white/50 — both fail contrast on navy; now white/75–80). Turned the dead-end into a recovery path: 6 popular-page chips led by the lead-capture Deal Alerts link. Verified at 375px (no overflow, no layout shift)
- [x] Auth pages (login/signup) 2026-07-21: shared (auth)/layout.js is now a conversion-focused two-column split on desktop — a dusk brand panel (night CityscapePanorama, value headline, 3 benefit bullets, trust chips: 5.0 Google / RECO / Live MLS) beside the existing form card. Below lg the panel is hidden and the card-only layout is unchanged, so mobile (375px) is byte-identical to before. Verified 375px (card only, no overflow) + desktop (split, no overflow), WCAG-AA white-on-navy, build green. One layout change covers both login and signup
- [x] Listing cards on /listings 2026-07-22: hover polish now matches HomeDealCards — replaced the whole-card `hover:scale-[1.02]` (a slight jitter against grid neighbours, not "calm") with the premium `hover:border-accent/30 hover:shadow-lg duration-300` treatment + a slow `duration-500` photo zoom. Photo aspect already sound (fixed h-48 + object-cover). Class-only change, build green, removes rather than adds layout shift. FINDING for improvement agent: HomeDealCard renders `deal.capRate.toFixed(1)` UNGUARDED (home-deal-cards.js) — a malformed deal (undefined capRate) throws and blanks the homepage deal section; ListingCard already guards via pct1()
- [x] Homepage 768px/tablet pass 2026-07-21: audited the hero grid at 768px AND 375px on a clean prod build — NO horizontal overflow at either width, hero stacks to single-column below lg cleanly (trust chips wrap, search + CTAs full-width, skyline intact). No change needed
- [x] Email templates: adopt the same navy/dusk header identity (table-safe inline styles only) — DONE 2026-07-25 (design) for the DAILY ALERT email, which completes the 4b rollout queue. The weekly newsletter is deliberately untouched: Hamza personally directed its serif 'magazine edition' identity, and overwriting that with the dusk treatment would undo his call. The alert masthead now carries the site's dusk gradient (#16223D → navy → #25355C, matching PageHero) with `bgcolor` + a solid-navy `background` shorthand ahead of it so Outlook and any gradient-blind client fall back to exactly the previous look rather than a white box, plus a gold letter-spaced kicker matching the site's section kickers. FIXED REAL AA FAILURES while in there, all measured: the masthead ".ca" was accent #2563EB on navy = 2.75:1 (the same on-navy failure fixed in the site footer) → #8AB6FF at 6.9:1; the PCF cash-flow figure was #10B981 on the light chip = ~2.4:1 → #047857 at 5.0:1 (red #EF4444 → #DC2626 at 4.4:1); and the score badge printed WHITE on #10B981/#F59E0B = ~2.4:1 → #047857 (5.5:1) and #B45309 (5.0:1). That is the same data-legibility class fixed site-wide earlier today, now closed in the email channel too. Verified by rendering the real template to HTML and screenshotting it — masthead, rent assumption, financing basis and both PCF states all correct.

## 4 — Design & trust

- [x] Audit every public page at 375px for overflow, tap targets, layout breaks — COMPLETE 2026-07-20: homepage, listings, mortgage-calculator, market-pulse, gta, blog, precon, auth all clean (no horizontal overflow). Issues found & fixed along the way: cookie banner size, market-pulse zero-stat, deal-screener zero-dashboard. (768px pass still open if wanted)
- [x] Trust signals near every capture form — verified/complete 2026-07-20: homepage EmailCapture ("no spam, unsubscribe anytime"), exit-intent popup ("free forever" + social proof), and /alerts form (added expectation-setting block: match-only emails, free, one-click unsubscribe, email never shared). Brand identity consistent
- [x] Error/empty/loading states — complete 2026-07-21: deal-screener skeleton/empty (Jul 20), market-pulse + news hide unavailable stats, compare/saved have proper empty states (verified), and the big one: /listings no longer shows eternal 'Analyzing...' skeletons when the feed fails — honest error card with Try Again retry + phone fallback; also fixed the '{0 && ...}' JSX bug rendering 'Showing 0 0investment properties'
- [x] Accessibility — FOOTER AA FIX 2026-07-25 (design): measured every capture-path page at a true 375px viewport and found the site-wide footer failing badly on EVERY page — the mandatory disclosures (PropTx VOW bona-fide notice, TRREB/CREA trademark attribution, investment + deal-score disclaimers, PIPEDA/CASL notice) rendered at 10px `text-white/30` = ~2.4:1, and the copyright plus the **Privacy Policy / Terms** links at `text-white/25` = 2.23:1. A legally-required disclosure nobody can read is barely a disclosure, and it sat under every page of a site whose whole pitch is trustworthiness. Also failing: every footer link using brand `text-accent` on navy (2.75:1) — including the tap-to-call PHONE NUMBER and email, i.e. conversion elements. Fixed: disclosures to `text-white/60` at 11px (6.1:1), copyright/legal links to `text-white/70`, and on-navy links to `#8AB6FF` (6.9:1) — already the site's established on-navy blue from the homepage hero/seller band, so nothing new was invented. Also raised the public capture forms' required-field asterisk from red-400 (2.77:1) to red-600, marked aria-hidden since 'required' is already conveyed to assistive tech by the input. Re-measured: ZERO AA failures on /signup, /alerts, /, /listings and /blog. (Admin pages keep red-400 — they are dark-themed, where it passes.) Earlier a11y work 2026-07-21: mega-menu keyboard nav (Jul 20), form labels verified (alerts/signup-gate have real labels; homepage EmailCapture + exit-intent inputs now have aria-labels), error messages announce via role=alert and successes via role=status on all capture forms, focus rings present on inputs/buttons (focus:ring-accent)
- [~] Performance: next/image everywhere, dynamic-import leaflet/markdown, LCP/CLS on homepage and listing detail — THIRD-PARTY CDN REMOVED 2026-07-25 (seo): the map component fetched Leaflet's stylesheet at runtime from `unpkg.com` even though `leaflet` is an installed npm dependency whose dist ships that exact file. So the map's appearance depended on a third-party CDN being reachable (an ad blocker, corporate proxy or unpkg blip left it unstyled), it cost a DNS+TLS handshake to an un-preconnected origin at the moment the user opened the map, it was an unconsented third-party request on a site otherwise careful about PIPEDA, and the URL hardcoded 1.9.4 while package.json resolves ^1.9.4 — a minor bump would have desynced CSS from JS. Now a plain `import 'leaflet/dist/leaflet.css'`; webpack emits it as its OWN 12K CSS chunk (26 leaflet rules) loaded only with the dynamically-imported map, so non-map visitors still pay nothing. Verified: zero `unpkg.com` references remain anywhere in .next/static or .next/server, and zero unpkg requests fire in a real browser session. COULD NOT visually confirm the styled map locally (no AMPRE_TOKEN -> the listings feed fails -> the map toggle never renders, so the chunk is never requested); the bundling evidence is conclusive but it deserves a production eyeball on the /listings map view. Earlier CWV win 2026-07-21: added `preconnect` + `dns-prefetch` to the MLS photo CDN (trreb-image.ampre.ca) in the root layout head, so the browser opens the TLS connection before it discovers the LCP `<img>` on listings/detail/homepage deal cards; dns-prefetch for GTM + Clarity too. Verified tags render site-wide. Still open: convert remaining raw <img> to next/image, audit CLS

## 5a — Striking-distance keywords (from Hamza's real Google Search Console, 2026-07-21)

GSC shows 358 queries, avg position 9, impressions 8.94K & clicks 117 both trending UP over 3mo. These queries get real impressions but ~0 clicks (page 2 / bottom of page 1) — the highest-ROI SEO work is pushing the RIGHT existing page onto page 1 for each. Method: exact-match the target page's title + H1 + add a direct answer block + FAQ JSON-LD; don't fabricate — keep answers synced to real data. One per SEO-agent run.

Ranked from the real export (52a407e3 …Performance…20260722.xlsx, 3-month, 358 queries). Do the topmost unchecked one per SEO run.

- [x] "best neighbourhoods to invest in mississauga" (70 impr, pos 14.7) → /neighbourhoods retitled + answer block + FAQ schema (2026-07-21)
- [x] "income property mortgage calculator" (24 impr, pos ~51) + "investment property mortgages in mississauga" (19 impr, pos 17) → /mortgage-calculator title + H1 exact-match "Income Property Mortgage Calculator" (2026-07-21). Watch position; page was on page 5 so biggest potential jump
- [~] "rent vs buy mississauga 2026" (89 impr, pos 16) — HIGHEST non-brand impressions → DONE 2026-07-21: built the dedicated /rent-vs-buy-mississauga page (exact-match title + H1 "Rent vs Buy in Mississauga ({year})", direct-answer block, "what decides it" factors, visible FAQ + FAQPage & BreadcrumbList JSON-LD, dusk PageHero). Added to sitemap; internal link added from the mortgage-calculator page. Content is HONEST/general (no fabricated break-even figures) and routes to the audited mortgage calculator for specifics. Verified: single-brand title, FAQPage+BreadcrumbList render, single h1, 375px no-overflow on a clean prod build. DEFERRED (per SEO bar "never change financial calc logic"): the interactive buy-vs-rent break-even calc — a new financial calculation that needs careful hand-verification; better as an improvement-agent build
- [x] "investment properties for sale mississauga" (29 impr, pos 13.9) — THE money keyword → DONE 2026-07-21: /listings title + H1 + intro + description now exact-match "Investment Properties for Sale in Mississauga"; added BreadcrumbList (page had none). FOUND + FIXED a pre-existing bug the metadata audit missed: /listings title carried its own "| MississaugaInvestor.ca" while the root layout template already appends it → the brand rendered TWICE in the SERP title; removed the redundant suffix (single brand now). Verified single-brand title + single h1 + 375px clean on prod build. (Item schema deferred — listings are client-fetched, no server data for an ItemList; title/H1/intro is the core striking-distance signal)
- [x] "hurontario lrt real estate impact" (45 impr, pos 7.8) 2026-07-22 → built dedicated static page /hurontario-lrt-real-estate (exact-match title/H1, direct-answer block, corridor-node cards linking Port Credit/City Centre/Cooksville/Hurontario guides, "why transit moves the numbers" list, visible FAQ + FAQPage & BreadcrumbList JSON-LD, dusk PageHero). Honest/general content — NO fabricated appreciation figures or opening date (routes to Metrolinx for dates, to listings/market-pulse for numbers). Added to sitemap; internal-linked FROM the existing isLRT callout on all corridor neighbourhood guides. Verified: exact-match single-brand title, single h1, both schemas, canonical, 4 corridor links, sitemap entry, corridor-vs-non-corridor link conditional all correct on a clean prod build
- [x] "townhouse or condo better investment" (50 impr, pos 25.6) 2026-07-22 → built /townhouse-vs-condo-investment: exact-match title/H1, direct-answer block, 8-row side-by-side comparison table (entry price, fees, land, appreciation, cash flow, maintenance, tenant pool, liquidity), visible FAQ + FAQPage & BreadcrumbList JSON-LD, dusk PageHero. Honest/general (no fabricated $ or appreciation figures) and surfaces the freehold-vs-condo/POTL townhouse fee nuance accurately; notes the site's calc includes condo fees. Added to sitemap + inbound link from the /listings header. Verified exact-match single-brand title + single h1 + both schemas + canonical + table overflow-x-auto (no 375px page overflow) on a clean prod build
- [x] "cash flow positive properties ontario" (26 impr, pos 19.7) 2026-07-22 → built /cash-flow-positive-properties-ontario: exact-match title/H1, honest direct-answer (positive CF is the exception at today's rates — no fabricated returns), 4 "levers" cards (second suite / multi-unit / bigger down payment / below-market), inline email capture, visible FAQ + FAQPage & BreadcrumbList JSON-LD, dusk PageHero. Funnels to a REAL filtered view via the existing ?cf=1&sort=cashflow deep-link (verified 200). Added to sitemap; inbound-linked from the /listings header (tidied into a compact "Investor guides:" row with both this + townhouse-vs-condo). Verified exact-match single-brand title + single h1 + both schemas + canonical + deep-link on a clean prod build
- [x] "rental property insurance mississauga" (26 impr, pos 14.4) 2026-07-22 → built /rental-property-insurance-mississauga: exact-match title/H1, direct-answer, 4 coverage cards (building/liability/loss-of-rent/landlord-contents), a "require tenant insurance" callout, inline email capture, visible FAQ (incl. tenant-insurance + tax-deductibility) + FAQPage & BreadcrumbList JSON-LD, dusk PageHero. HONEST/general — no fabricated premiums/limits/legal or tax claims; explicit "not insurance/legal/tax advice — get a quote from a licensed Ontario broker" disclaimer. Added to sitemap; inbound-linked from the mortgage-calculator (insurance is a carrying-cost line there). Verified exact-match single-brand title + single h1 + both schemas + canonical + inbound link on a clean prod build
- [x] "hamza nouman" brand (129 "hamza" impr pos 4.4; "hamza nouman" 10 impr) 2026-07-22 → /about audited + strengthened to own the brand. Already had: H1 "Hamza Nouman", brand-exact title/keywords/OG/Twitter, full PersonJsonLd (with sameAs to realtor.ca/LinkedIn/FB/etc.), sitemap priority 0.9. ADDED the Google-recommended profile-page pattern: rendered the (previously unused) ProfilePageJsonLd — a ProfilePage whose mainEntity shares the SAME @id (#hamza-nouman) as the full Person node, so Google merges them and treats /about as THE authoritative page for the name — plus mainEntityOfPage on the Person → /about and name/url on the ProfilePage. Verified both schemas + shared @id + single h1 "Hamza Nouman" on a clean prod build. COMPLETES the section-5a striking-distance keyword plan (only the REFRESH note remains — Hamza can drop a new GSC export anytime).
- [ ] REFRESH: Hamza can re-export GSC (Performance → Export) anytime and drop the xlsx in chat — reprioritize this list from the new numbers. Address-level queries already earn clicks (listing SEO works — leave it)

## 5 — Investor value & SEO

- [x] Neighbourhood guides verified 2026-07-21: data-rich metadata with real numbers (price, YoY, yield, DOM), FAQ + breadcrumb schema, and full interlinking (filtered listings CTA, book-call, sibling guides, methodology). Listing detail pages link back to guides (Jul 20). Complete
- [x] Blog/auto-blog SEO complete 2026-07-21: metadata + Article/Breadcrumb schema + guaranteed images all in place; generation prompt now requires 2-3 internal links from a fixed allowed-path list (listings/calculator/market-pulse/recent-sales/alerts/neighbourhood guides — no invented URLs); markdown renderer keeps internal links same-tab (external still new-tab). Content quality directives already strong (voice, no AI tells, real neighbourhood numbers)
- [x] Metadata audit COMPLETE 2026-07-20: crawled all 21 public routes — every title unique; fixed 4 pages with doubled '| MississaugaInvestor.ca' brand suffix (news, score-methodology, privacy, terms) and gave /news a query-targeting title. OG/canonicals/JSON-LD/sitemap verified in earlier runs
- [x] Market data pages target searchable queries — complete 2026-07-21: recent-sales targets "Mississauga sold prices {year}" (Jul 20), market-pulse now titled "Mississauga Housing Market {year} — Prices, Trends & Stats" with build-time year and query-rich description
- [x] Sitemap gap fixed 2026-07-21: /pre-construction/projects (indexable, unique metadata + canonical + OG, targets "GTA pre-construction projects", linked from nav+footer) was MISSING from sitemap.xml — added it (priority 0.7, weekly). Also added BreadcrumbList JSON-LD (Home > Pre-Construction > Projects) via its server layout, matching /pre-construction. Verified sitemap output + rendered schema. (/recent correctly stays out — noindex; profile/saved user-only)
- [x] Listing-detail OG/Twitter image fallback 2026-07-23 (seo): listing detail pages (the highest-traffic page type — they win address queries, and realtors share these links constantly in texts/social) build their og:image + twitter:image from the listing's first photo, but when that photo was absent OR the /api/photos fetch failed (that endpoint intermittently times out — 29 hits in the last runtime-error window) the openGraph/twitter blocks got NO images at all, so the listing shared with a blank preview. Added a branded /opengraph-image fallback (`images: photo ? [{photo}] : ['/opengraph-image']`) on both openGraph and twitter, so a listing link never shares imageless. Same absolute-URL mechanism as the 22-page og:image fix (verified resolves to the branded card). Build green (89 pages); no calc/data change.
- [x] OG social card redesigned 2026-07-23 (design): the branded /opengraph-image (now load-bearing on 22+ pages after yesterday's og:image fix — it's the preview on every FB/LinkedIn/WhatsApp/Slack/iMessage share) hardcoded FOUR market stats presented as current — "2,000+ Active Listings", "96% Sale-to-List", "36 days Avg DOM", "5.2 mo Inventory" — baked into a static, un-refreshable edge image, so they silently rot into wrong numbers on the brand's most-shared asset (the exact "a wrong number is the worst bug" trap, and the same stale-provenance issue flagged on market-pulse). Also used an off-brand emerald ".ca" (the site wordmark is accent-blue #2563EB everywhere). Rebuilt on the brand "dusk" gradient (#16223D→navy #1B2A4A→#25355C, matching PageHero), site-matched accent-blue ".ca", a gold (#F59E0B) accent underline for polish, and replaced the decaying stat tiles with FOUR evergreen always-true capability chips (Live MLS Listings · Cash Flow & Cap Rate · Investor Deal Scores · Free Deal Alerts). Rendered the endpoint locally: 200, valid 1200×630 PNG, premium + on-brand; white-on-navy AA. No calc/data logic touched (presentational asset only).
- [x] og:image regression fixed site-wide 2026-07-23: EVERY page that exports a page-specific `openGraph` block (to set its own og:title/description/url) was silently DROPPING the branded og:image — Next.js replaces, not merges, the `openGraph` object across segments, so the root layout's `openGraph.images` was lost the moment a page defined its own openGraph. Confirmed in the prerendered HTML: 22 pages (market-pulse, book-call, faq, compare, quiz, all 5 investor guides, /guides, /sell, /alerts, mortgage-calculator, neighbourhoods index+detail, recent-sales, all 3 pre-construction pages, blog index) had `twitter:image` (root twitter isn't overridden) but NO `og:image` — so on Facebook/LinkedIn/WhatsApp/Slack/iMessage (which read og:image, not twitter:image) these money pages shared with a blank/broken preview, killing share CTR + trust. Added `images: ['/opengraph-image']` (the branded dynamic OG, resolved absolute via metadataBase) to each block. Pages that intentionally set their own image (about→headshot, blog/[slug]→cover, listings/[id]→listing photo) left untouched. Verified og:image now renders on all previously-broken static pages on a clean prod build (89 pages); book-call is dynamic (identical source fix). Zero calc/data change.
- [x] Listing-detail structured data enriched 2026-07-21: RealEstateListing was thin (single image, no address/rooms/size). Now nests the property as `about` with a mapped Residence subtype (Apartment/House/SingleFamilyResidence), full PostalAddress (ON/CA + postal code), GeoCoordinates, numberOfBedrooms/BathroomsTotal/Rooms, floorSize (sqft, FTK), an image array (up to 6), and Offer with url. Pure re-expression of existing listing fields — no data/calc change. All fields guarded (only emitted when present & valid). Build green (audit: metadata + sitemap + WebSite/SearchAction/Organization/Person/RealEstateAgent schema all already solid across routes)

## Code health (background priority)

- [ ] Dedupe repeated fetch/format helpers across `app/api/*` into `lib/`
- [ ] API routes: consistent error handling, no leaked stack traces, correct cache headers
- [ ] Verify auth actually protects every `app/api/admin/*` and `(admin)` page
- [ ] Remove dead code/unused deps

## Needs Hamza

(Items blocked on credentials, accounts, or product decisions — agents: add here, don't guess.)

- [x] DEAL-ALERTS filter-format mismatch FIXED 2026-07-23 (improvement agent) — the format transformation turned out to be fully code-verifiable locally, so it no longer needed live test access. Root causes fixed: (1) /api/alerts/subscribe now sanitizes into the CANONICAL client/applyFilters shape via new `lib/alerts/sanitize-filters.js` (validated + bounded: strategy keys whitelisted against STRATEGY_CHIPS, propertyType against PROPERTY_TYPES, numbers clamped, arrays capped, unknown keys dropped; stores only non-default keys) — the old allow-list shared ZERO keys with what the Save-Search button sends, so every saved search stored ~empty filters and alerts matched ~everything; legacy simple keys (minPrice/maxPrice/minBeds/hood/minScore) still map in for backward compat, and old stored rows behave exactly as before (their keys were already ignored). (2) The /alerts form NEVER CREATED A SAVED SEARCH AT ALL — it only logged a lead + wrote localStorage, so its promised alerts could never fire; it now POSTs /api/alerts/subscribe with canonical filters (maxPrice→priceRange, minBeds→beds, neighbourhood→neighbourhoods, cashflow/brrr→strategy chips) and keeps the labeled lead capture as fire-and-forget. UNIT-VERIFIED round-trip (button payload / form payload / legacy keys / junk input → sanitize → DEFAULT_FILTERS merge → applyFilters on synthetic listings): each case matches exactly the intended listing, junk is clamped, empty input stores {}. STILL OPEN (below): Mississauga-only cron + live Resend/cron delivery verification.
- [x] DEAL-ALERTS Mississauga-only FIXED 2026-07-23 (improvement agent — completed together with the filter-pipeline fix, code-verified locally): saved searches now record their region — SaveSearchButton receives the page's city via ListingsContainer→InvestorFilters (cityParam on /gta?city=X, 'GTA' sentinel on the hub, 'Mississauga' on /listings) and includes it in the payload; the sanitizer stores it (`city`, bounded string); the popup summary shows the scope. The cron fetches the GTA feed ONLY when some search needs it (a GTA-feed failure logs + degrades to zero GTA matches without killing Mississauga alerts) and matches each search against its own pool via `poolForSearch()` (lib/alerts/sanitize-filters.js): no city/legacy → Mississauga feed (byte-identical old behavior), 'GTA' → whole GTA feed, city → GTA feed filtered to that city. UNIT-VERIFIED 6 cases: Toronto-scoped search matches only in-budget Toronto listings; GTA-hub matches across cities; legacy rows unchanged; explicit Mississauga → Mississauga pool; GTA-feed-down degrades safely; the stored city key is inert to applyFilters. /listings, /gta, /gta?city=Toronto all 200. REMAINING (Needs Hamza, below): live Resend delivery verification only.

- Email sending is **Resend** (Supabase only stores subscribers/searches). Hamza: confirm in Vercel that `RESEND_API_KEY` and `RESEND_FROM_EMAIL` are set and the mississaugainvestor.ca domain is verified in the Resend dashboard, and that Vercel Cron is configured to hit `/api/alerts/send` (daily) and `/api/newsletter/weekly` with `CRON_SECRET`. Agents: templates/logic can be perfected without these, but real delivery can't be verified.
- [x] WRONG NUMBER on /pre-construction/hst-rebate FIXED 2026-07-24 (researched, no longer a guess): the $850k worked example showed **Provincial Rebate -$104,000**, which is impossible (provincial HST on $850k is 8% = $68,000), plus a stale -$6,300 federal line, and labelled the $24,000 tier "federal" when it is the Ontario provincial rebate. Verified the program against PwC/Osler coverage: enhanced Ontario rebate for APS signed Apr 1 2026-Mar 31 2027 rebates the FULL 13% (8% provincial + 5% federal) on qualifying new homes up to $1M; $1M-$1.5M flat $130,000; declining to $24,000 to $1.85M; $24,000 above. Corrected the example to Provincial -$68,000 / Federal -$42,500 / Total $850,000 / Save $110,500, and fixed the two 'federal' mislabels. The page's tier table, dates and eligibility claims were already correct.
- **Real neighbourhood photos** (Hamza wants photos on the neighbourhood cards; agents CANNOT download images — egress policy blocks all image hosts, and copyright rules out scraping Google Images for a commercial site). Cards are photo-ready: drop a licensed .jpg at `/public/images/neighbourhoods/<slug>.jpg` (slug = lowercased, spaces→dashes, e.g. `port-credit.jpg`) and add the slug to `lib/neighbourhood-images.js` — the card auto-swaps its illustrated scene for the photo. Sources Hamza can use: his own listing/neighbourhood photos, or free-commercial-use ones (Unsplash license / Wikimedia Commons). Until then the original `NeighbourhoodScene` landmark art renders (waterfront/skyline/village/suburban/transit archetypes).

- [ ] **Email delivery still unverified** (Hamza): nobody has confirmed the daily alert emails actually leave Resend. 2026-07-24 made the run SELF-DIAGNOSING — /api/alerts/send now returns `{sent, failed, failures[]}` and logs Resend's own rejection message (401 = bad RESEND_API_KEY, 403 = sending domain not verified in Resend, 422 = bad RESEND_FROM_EMAIL), and returns 503 immediately if RESEND_API_KEY is missing. So the next daily run says exactly what is wrong. Hamza can also just open resend.com -> Emails and look for the ~13:00 UTC batch.

## Run Log

(append one line per run: date — what changed — commit)

- 2026-07-20 — Bootstrap: CLAUDE.md + backlog created, 24/7 routine scheduled; fixed env-less build in alerts routes — 47b196f
- 2026-07-20 — Refocused mission on investor leads/conversions; restructured backlog around 5 goal areas — 3854e8b
- 2026-07-20 — CRITICAL FIX: daily alert emails + SEO ping crons never ran (Vercel Cron uses GET, routes only exported POST); added GET handlers. Documented all real env vars in .env.example (email = Resend, not Supabase) — 04175b7
- 2026-07-20 — [seo agent] Added app/robots.js: robots.txt with sitemap pointer, disallow /api/ + /admin/, noindex pages left crawlable so the directive stays visible. NOTE: routines now fire into the persistent agent session (fresh-session routine runs were silently failing; rebuilt as persistent_session_id triggers) — c59cd4e
- 2026-07-20 — [design agent] 375px audit of mortgage-calculator (clean — no overflow, good hierarchy); compacted site-wide cookie banner from ~185px to 100px tall on mobile (one-line copy, inline buttons, same consent options + PIPEDA note) so it stops covering a quarter of every page — d9df3d4
- 2026-07-20 — [seo agent] BreadcrumbList JSON-LD added to listing detail + blog post pages (highest-traffic page types; only neighbourhoods/about had it — blog even showed a visible breadcrumb with no schema) — 52b0a32
- 2026-07-20 — [improvement agent] Fixed dead-end "Sign Up Free" CTAs in InlineCTA: alerts variant pointed to /quiz and newsletter variant to /listings — neither has a signup form; both now go to /alerts (real name+email capture). Verified cap-rate/CoC/LTT engine guards divide-by-zero and Ontario LTT brackets are correct — 9b6cbdd
- 2026-07-20 — [design agent] 375px audit of market-pulse (no overflow, chart fine); hid the Active Listings tile when the stats API returns no count — it rendered "0 on market", which reads as broken data. NEW FINDING for improvement agent: market-pulse fallbacks are hardcoded TRREB Feb 2026 numbers (sales 345, new listings 940, mortgage rates) — need a data-freshness pass or "as of" labels — 82032dc
- 2026-07-20 — [seo agent] Listing-detail gallery CWV/image-SEO: hero img (LCP) now fetchPriority=high + decoding=async, thumbnails lazy-loaded, alt text upgraded from "Property photo"/"Thumbnail N" to address-based. Verified canonicals complete (only noindex pages lack them, correct) and OG images covered site-wide — 396a051
- 2026-07-20 — [improvement agent] Market-pulse data transparency: subtitle no longer claims "updated regularly"; now states sources honestly (live MLS + TRREB Market Watch) and shows the TRREB data month from the API (tRREBMonth). Rates card already carried a verify-with-broker caveat. Remaining: refresh the hardcoded TRREB Feb 2026 numbers when March+ Market Watch data is added to /api/market-stats — 3901b14
- 2026-07-20 — [design agent] 375px audit of gta + blog (no overflow on either; blog empty state fine). Deal Screener no longer renders an all-zero dashboard ("0 deals, 0.0% best cap, +$0/mo") before data loads or when filters match nothing: skeleton tiles while loading, plain guidance message when empty, header count chips hidden until real — 3140b7f
- 2026-07-20 — [seo agent] HST-rebate landing page (high search volume: "HST rebate new homes Ontario"): added FAQPage JSON-LD (5 Q&As mirroring on-page content — savings tiers, eligibility, dates, resale exclusion, property types) + BreadcrumbList. Page previously had zero structured data — decaa4c
- 2026-07-20 — [improvement agent] Lead-capture path verification: exercised /api/newsletter/subscribe + /api/lead with valid/invalid/missing email (all correct: 200/400 with messages, graceful without env). Fixed /alerts form: client-side email format validation + surface server error text instead of generic "Something went wrong" — de57817
- 2026-07-20 — [design agent] 375px audit finished (precon + login clean → item checked off). Mega-menu keyboard a11y: Escape now closes from anywhere inside the menu and returns focus to the trigger; menu closes when keyboard focus leaves it (was stranding keyboard users with a stuck-open menu). Verified with Playwright keyboard simulation — 06e68b0
- 2026-07-20 — [seo agent] Mortgage calculator: added visible "How This Calculator Works" FAQ section (5 Q&As matching the implemented math: semi-annual compounding, CMHC tiers, stress test, min down payment, Ontario LTT) + FAQPage & BreadcrumbList JSON-LD. Targets "CMHC calculator"/"stress test"/"Ontario land transfer tax" queries; verified at 375px — ef8cfeb
- 2026-07-20 — [improvement agent] Bookings API hardening: validation now precedes the DB check (bad input gets a 400, not "Database not configured" 500); if the DB is down the booking falls back to the notification email so the hottest lead type is never silently lost (503 only when no capture channel exists); user-supplied fields escaped in notification email HTML. All verified with live requests — b86d5ce
- 2026-07-20 — [design agent] /alerts (main "Sign Up Free" CTA destination): added trust/expectation block beside the submit button — match-only emails, scored and analyzed, free forever, one-click unsubscribe, email never shared. Trust-signals backlog item now checked off across all capture forms; verified at 375px — (this commit)
- 2026-07-20 — [release] Shipped 7-commit daily release to production via PR #2 (merged 9b56023); release routine re-pointed to the shipping session using the PR+merge path, so the "blocked on main push" issue is resolved — no action needed from Hamza
- 2026-07-20 — [seo agent] Internal linking: listing detail pages now link the neighbourhood name to its /neighbourhoods/{slug} investor guide (when a guide exists) — funnels link equity from the highest-traffic pages into guide pages and gives buyers one-tap investor context — 15aa277
- 2026-07-20 — [improvement agent] Weekly newsletter: added ?preview=1 test-render mode and used it to catch + fix fabricated stats (hardcoded "2,500+" listings, avgDOM 28, 97.2% sale-to-list mailed as if live) and "N/A" tiles — every stat block now renders only real data and hides itself when data is missing — 3f8b835
- 2026-07-20 — [design agent] Listing detail had NO lead CTA: added mobile sticky "Book a Viewing" bar (+ tap-to-call) and desktop CTA pair beside the price → /book-call?listing={id}. Needs a production eyeball once shipped (no listing data available locally) — a5c30a1
- 2026-07-20 — [seo agent] Recent-sales page now targets "Mississauga sold prices {year}" queries: query-matching title/description with build-time year, added missing OG block, H1 upgraded from generic "Recent Sales". Verified rendered title locally — (this commit)
- 2026-07-20 — [improvement agent] Daily alert email: added UTM attribution to listing links + CTA (was fully untracked), escaped name/address in HTML. Audited matching: dedupe/cap/freshness correct; flagged DOM≤3 repeat-send gap (needs sent-listings table) — (this commit)
- 2026-07-20 — [design agent] Performance: MarkdownRenderer converted to a server component (react-markdown no longer ships to browsers on blog posts — /blog/[slug] page JS now 3.25 kB) + lazy/async in-article images. Verified leaflet map already dynamic-imported; blog routes render correctly — (this commit)
- 2026-07-20 — [hamza request] MailerLite replacement: FIXED broken weekly-email unsubscribe (?email= was rejected as "Missing search ID" — CASL blocker); unsubscribe links now HMAC-signed + RFC 8058 one-click List-Unsubscribe headers; weekly send excludes status=unsubscribed leads; new admin bulk-import endpoint /api/admin/newsletter-import (CSV or JSON, dedupes, never resurrects unsubscribes); premium template redesign (branded masthead, CAP/cash-flow chips on deals, Hamza signature block w/ book-a-call CTA, Outlook bgcolor fallbacks); verified at 375px via preview — (this commit)
- 2026-07-20 — [hamza request] Weekly newsletter now requires Hamza's approval before sending: Monday cron sends ONE draft to hamza@nouman.ca with an amber "DRAFT — waiting for your approval" banner + Review & Approve button; button opens a token-authed confirm page; only the confirm POST sends to subscribers (prefetch-safe); newsletter_sends migration makes approval idempotent (no double sends). Preview mode unchanged — (this commit)
- 2026-07-20 — [hamza request] New professional headshot deployed site-wide (public/images/hamza-headshot.jpg): square face-crop from Hamza's uploaded photo, optimized 4.5MB→26KB (big homepage LCP win). Used by homepage bio, about page, newsletter signature, and Person JSON-LD — (this commit)
- 2026-07-20 — [seo agent] Metadata audit finished: crawled all 21 public routes, all titles unique; de-duplicated brand suffix on 4 pages and upgraded /news title to target GTA real-estate-news queries — (this commit)
- 2026-07-20 — [improvement agent] Mortgage calculator math audit: every formula hand-verified against known references (payment $2,908.02 reference exact, LTT/CMHC/stress/min-down all correct); added insured-amortization-cap warning for <20% down + >25yr combos — (this commit)
- 2026-07-20 — [hamza request] Test preview of the weekly email delivered to Hamza (Gmail draft + full render). Fixed negative cash-flow chip formatting ("$-85/mo" → "−$85/mo") — (this commit)
- 2026-07-20 — [hamza request] Newsletter redesign v2 after Hamza feedback: magazine-style photo-led deals (full-width hero card with listing photo, score badge, price + chips + CTA; thumbnail rows for remaining deals with initial-letter fallback), editorial eyebrow section headers replacing emoji, self-hosted sample photos for preview mode. Real sends use MLS listing photos — (this commit)
- 2026-07-20 — [hamza request] Newsletter v3 "magazine edition" per Hamza's direction: serif nameplate masthead (double rules, tracked caps), paper background, gold editorial kickers, serif stat numerals, featured-deal spread (photo, RATED kicker, 26px serif headline, small-caps stat line, italic links), hairline-separated market rows, pull-quote signature, colophon footer. All email-safe (Georgia stack, tables, bgcolor) — (this commit)
- 2026-07-20 — [design agent] News page "LIVE FEED" ticker was fully hardcoded with WRONG numbers (BoC 2.75% vs real 2.3%, Avg Detached $1.82M vs real $1.46M, "Hurontario LRT 2025", random USD/CAD): now fed from /api/market-stats, shows only real values, hides entirely with no data. Verified at 375px (BoC 2.3% / 5yr 6.09% / $1.46M −11.4% rendering live) — (this commit)
- 2026-07-20 — [hamza request] Every blog post now gets a picture automatically: new /api/blog-cover generates branded magazine-style covers (category-tinted gradient, wordmark, gold kicker, double rules) from title+category; blog index cards, post hero, related posts, and OG images all fall back to it when a post has no photo (auto-blog's Unsplash fetch stays as first choice). Applies to all past imageless posts instantly. Note: consider moving the hardcoded Unsplash Client-ID in auto-blog to an env var — (this commit)
- 2026-07-21 — [hamza request] Full verification sweep: all new endpoints exercised (blog-cover edge cases incl. XSS-safe title, newsletter preview/approve tokens, import auth, unsubscribe GET+POST, bookings validation, capture endpoints) — all correct; 11 key pages clean at 375px; no runtime errors in logs; build green. Blog index cards now show full date (was month+year only, all posts read "July 2026"). NEW FINDING for improvement agent: auto-blog published 3 near-duplicate posts on the same BoC tiny-condo story (Jul 2026) — the same-story dedupe rule is not working; investigate the recent-posts context passed to the model and consider a title-similarity guard — (this commit)
- 2026-07-21 — [seo agent] Article/Organization JSON-LD fixes: article image now always present (falls back to generated blog cover — required for Google rich results; was omitted for coverless posts), and replaced 3 references to /images/og-image.jpg (a 404 — never existed) with the real generated /opengraph-image — (this commit)
- 2026-07-21 — [improvement agent] Auto-blog duplicate guard: prompt-level dedupe rule failed (3 near-identical BoC posts on Jul 20), so added a hard programmatic title-similarity check before publishing — significant-word overlap vs the 20 most recent titles, ≥0.5 blocks the post (verified: all 3 real dupe pairs score 0.60-1.00, distinct topics 0.00); slug collisions now skip instead of suffix-publishing. Hamza: the 2 duplicate posts already live still need manual unpublish in /admin/blog — (this commit)
- 2026-07-21 — [design agent] Capture-form a11y: aria-labels on the placeholder-only email inputs (homepage EmailCapture, exit-intent popup), role=alert on all error messages and role=status on success confirmations (homepage, exit-intent, /alerts) so screen readers announce outcomes; accessibility backlog item checked off — (this commit)
- 2026-07-21 — [seo agent] Market-pulse metadata now targets the high-volume "Mississauga housing market" query with build-time year; verified neighbourhood guides already fully optimized + interlinked (both #5 items checked off) — (this commit)
- 2026-07-21 — [improvement agent] Instant welcome email on newsletter signup: magazine-styled expectation-setting (what/when/from), signed unsubscribe, one-click List-Unsubscribe, UTM-tagged CTA; fire-and-forget after insert so signup latency is unchanged; verified subscribe path still 200 — (this commit)
- 2026-07-21 — [design agent] Listings feed-failure UX: eternal loading skeletons replaced with an error card (Try Again + call fallback, role=alert) when /api/listings fails; retry re-fetches; fixed 'Showing 0 0investment properties' falsy-JSX bug. Verified in simulated feed-down environment at 375px — (this commit)
- 2026-07-21 — [seo agent] Auto-blog internal linking: prompt requires 2-3 internal links from an exact allowed-path whitelist; renderer no longer opens internal links in new tabs. Blog SEO item closed — (this commit)
- 2026-07-21 — [improvement agent] Sold-comps recency: added 180-day window (stale sales were skewing averages presented as current), stats.windowDays exposed, freshness label under summary stats with honest fallback wording — (this commit)
- 2026-07-21 — [redesign] New 'Mississauga dusk' visual identity: skyline art library, illustrated steps, live deal card in hero, trust chips/badges, redesigned reviews + agent profile + night CTA; PageHero applied to blog/calculator/neighbourhoods/market-pulse/GTA. Previous design saved on design-v1-backup — (this commit)
- 2026-07-21 — [design agent] Applied dusk PageHero to /faq (redesign queue). FOUND + FIXED a lead-losing data bug: FAQ listed the wrong phone (647-609-5615) vs the correct 647-609-1289 used everywhere else on the site — callers were being sent to a dead number. Verified faq hero at 375px — (this commit)
- 2026-07-21 — [seo agent] Root Twitter card had no image (Next.js doesn't inherit OG images into twitter) — links shared to X/Slack/etc rendered a cardless preview. Added twitter.images=/opengraph-image in root layout so every page's Twitter/social card shows the branded 1200x630 image. Verified twitter:image meta renders — (this commit)
- 2026-07-21 — [improvement agent] WRONG-NUMBER fix: FAQ claimed deal-score cash flow uses "maintenance reserves (5%)" and "vacancy allowance (4%)", but the actual engine (DEFAULT_ASSUMPTIONS) uses 8% maintenance + 5% vacancy — investors were told the wrong methodology. Corrected FAQ to 8%/5% and clarified condos use real condo fee. Did NOT touch the engine (would change every score). NEEDS HAMZA: mortgage-calculator page defaults (5% maint / 5% vac) differ from the deal-score engine (8%/5%) — intentional (user-adjustable tool) or should they match? — (this commit)
- 2026-07-21 — [design agent] Dusk PageHero applied to /book-call (key lead-capture page) — branded skyline hero w/ "Free 30-min call" eyebrow flowing into the booking calendar. Verified 375px, no overflow. /sell left as-is (integrated two-column hero, restructuring risks the form) — (this commit)
- 2026-07-21 — [seo agent] Image alt-text audit: every <img> site-wide now has alt (last gap was the map marker popup thumbnail — added address-based alt + lazy loading + quote-escaping). Decorative skyline SVGs confirmed aria-hidden. Canonicals verified on all 10 key routes — (this commit)
- 2026-07-21 — [design agent] /about hero brought onto the dusk identity: kept its strong custom hero (headshot, credentials, call/email CTAs) but switched to the dusk gradient and added the CityscapePanorama skyline — enhance, don't replace. Verified 375px, no overflow — (this commit)
- 2026-07-21 — [seo agent] CRITICAL FIX: /robots.txt was returning 500 — my app/robots.js (added earlier this session) conflicted with the pre-existing, richer public/robots.txt ('conflicting public file and page file'). This shipped in PR #3 so production's robots.txt was likely broken (crawlers blocked). Removed app/robots.js, kept the detailed public/robots.txt (Googlebot/Bingbot rules + sitemap). robots.txt now 200. Shipping to prod now — (this commit)
- 2026-07-21 — [improvement agent] Verified rental-comps recency (already sound: 12/18mo windows + labels) and estimated-value display (already shows range+confidence). Hardened the est-value render against undefined/partial data (confidence.charAt could throw; 'Range: $ – $' and 'undefined comparable sales' now impossible) — (this commit)
- 2026-07-21 — [design agent] Dusk PageHero on /alerts (the main "Sign Up Free" CTA destination) — "Never miss a deal" eyebrow + skyline flowing into the alert form. Verified 375px, no overflow — (this commit)
- 2026-07-21 — [seo agent] Verified no duplicate-H1 on any redesigned page (PageHero pages have exactly one h1; sell/precon two-h1 are in mutually-exclusive success/form branches) and all JSON-LD survived the redesign. Added BreadcrumbList schema to the high-value /pre-construction VIP page (had none) — (this commit)
- 2026-07-21 — [improvement agent] Market-pulse data trust: verified aggregation math (sale-to-list, avg DOM/price all correct), then fixed the real issue — a 5-month-old TRREB monthly snapshot (Feb 2026) was blended with live MLS stats and shown as current. Added tRREBAsOf to /api/market-stats + a data-provenance strip (live-vs-monthly split, "as of {month}") and an as-of caption on the Avg-Prices-by-Type chart. Item checked off; Hamza to refresh TRREB block when newer Market Watch data ships — (this commit)
- 2026-07-21 — [design agent] 404 page redesigned onto the dusk identity: night skyline panorama + deeper night gradient, fixed WCAG AA contrast failures (recovery links were text-white/30, body /50 — fail on navy; now /75–80), and turned the dead-end into a recovery path with 6 popular-page chips led by the lead-capture Deal Alerts link. Verified 375px, no overflow/layout shift, build green — (this commit)
- 2026-07-21 — [production QA] Daily release (PR #7, commit 9534549) verified READY in prod, 0 runtime errors since 13:00 UTC. FOUND + FIXED a critical break: the daily /api/alerts/send cron had been 500-ing (JSON.parse of an HTML auth wall) because it fetched /api/listings via request.url = the protected *.vercel.app host — so no alert emails were sending. Switched to absolute SITE_URL + content-type guard; build green; pushed to agent branch for the next daily release — (this commit)
- 2026-07-21 — [seo agent] Enriched listing-detail RealEstateListing JSON-LD: nested property `about` (mapped Residence subtype, full PostalAddress, GeoCoordinates, bedroom/bathroom/room counts, floorSize sqft, image array up to 6, Offer with url) from existing listing fields — all guarded, no data/calc change. Audited and confirmed metadata/sitemap/robots/core schema already solid site-wide. Build green — (this commit)
- 2026-07-21 — [improvement agent] Fixed the same request.url self-fetch bug (found in today's QA on the alerts cron) in /api/newsletter/weekly: fetchScoredListings built the listings URL from request.url, so on the Monday cron it hit the protected *.vercel.app auth wall and its try/catch silently returned [] → weekly newsletter would send with zero deals. Switched to hardcoded public SITE_URL + content-type guard + error logging. Preview renders clean (0 N/A). Build green — (this commit)
- 2026-07-21 — [design agent] Auth pages onto the dusk identity: (auth)/layout.js is now a two-column split on desktop — night-skyline brand panel with value headline, 3 benefit bullets and trust chips (5.0 Google / RECO / Live MLS) beside the form card; mobile stays the untouched card-only layout. Conversion-critical signup/login gate now has trust context. Verified 375px + desktop (no overflow/shift), build green — (this commit)
- 2026-07-21 — [seo agent] Added /pre-construction/projects to sitemap.xml (was indexable + nav/footer-linked but absent — crawl-discovery gap) and gave it BreadcrumbList JSON-LD via its layout. Verified sitemap entry + rendered schema; build green — (this commit)
- 2026-07-21 — [improvement agent] Lead-capture friction: InlineCTA newsletter/alerts variants now capture email INLINE (subscribe in one tap on blog posts + neighbourhoods) instead of linking to /alerts; posts to /api/newsletter/subscribe with UTM + a11y states. deals/quiz still link. Verified endpoint 200/400 + 375px render; build green — (this commit)
- 2026-07-21 — [design agent] /recent-sales onto the dusk identity: replaced the plain section-title header with a compact PageHero (skyline band, gold "Live · MLS sold data" eyebrow, single h1), rendered in both loading and loaded states so the hero doesn't pop in after fetch. Verified single h1 + 375px no-overflow on a clean prod build. Still queued: /compare, /news, /score-methodology — (this commit)
- 2026-07-21 — [improvement agent] WRONG-NUMBER fix: interactive listing calculator ignored condo fees (calculateCashFlow had no condo-fee input) — condos' cash flow / cash-on-cash were overstated vs the listing card. Added monthlyCondoFee (condo fee replaces the maintenance reserve, matching the listings engine), wired it into both call sites + a Condo Fee breakdown row. Hand-verified math; build green — (this commit)
- 2026-07-21 — [seo agent] Core Web Vitals: added preconnect + dns-prefetch to the MLS photo CDN (trreb-image.ampre.ca) in the root layout head — the browser now opens that connection early instead of at <img> discovery, speeding LCP on the image-heavy listings/detail/homepage pages; dns-prefetch for GTM + Clarity added too. Verified tags render on homepage + /listings; build green — (this commit)
- 2026-07-21 — [design agent] /score-methodology onto the dusk identity: replaced the plain h1 header with a compact PageHero (skyline band, gold "Methodology" eyebrow, single h1) — the trust-building scoring page is now consistent with the rest of the site. Verified single h1 + 375px no-overflow on a clean prod build. Still queued: /compare, /news — (this commit)
- 2026-07-21 — [improvement agent] WRONG-NUMBER fix part 2: the Analysis-tab CAP RATE had the same condo-fee omission as the cash-flow fix — calculateNOI was called without the condo fee, so a condo cap rate read ~1pt too high vs the card. Added the condo-fee-replaces-maintenance logic inside calculateNOI, passed listing.condoFee, added a Condo Fee row to the NOI breakdown. Hand-verified 4.08%→3.12% (matches card). Build green — (this commit)
- 2026-07-21 — [seo agent] BreadcrumbList JSON-LD added to 5 high-traffic SEO landing pages that lacked it — market-pulse, faq, blog, gta, recent-sales (Home > Page) — via server layouts (market-pulse/faq) and render roots (blog/gta/recent-sales). Enables breadcrumb rich results / better SERP CTR. Verified schema renders on all 5 on a clean prod build; build green — (this commit)
- 2026-07-21 — [design agent] /compare onto the dusk identity: replaced the plain header with a compact PageHero (skyline band, gold "Side-by-side analysis" eyebrow, single h1); Back-to-Listings + comparison count + Clear-All moved to a control row below the hero; static subtitle avoids layout shift between loading/empty/loaded. Verified single h1 + 375px no-overflow on a clean prod build. Still queued: /news — (this commit)
- 2026-07-21 — [improvement agent] Mobile lead capture: the site-wide exit-intent popup only triggered on desktop mouseleave, so ~375px traffic (most of it) never saw it. Added a tasteful scroll-depth trigger (long pages only, after real >60% downward scroll, same 10s-arm + once-per-session guards). Verified at 375px on a clean prod build — hidden before arm, shows after arm+scroll. Build green — (this commit)
- 2026-07-21 — [seo agent] Image sitemap for listing photos: Next 14 metadata sitemap ignores the images field (image-sitemap support is Next 15 only — VERIFIED by reading resolveSitemap in node_modules), so the simple approach was a silent no-op and was reverted. Added a dedicated app/image-sitemap.xml route handler emitting proper image-sitemap XML (image namespace + <image:image>) from listing photos, referenced in robots.txt. Unit-verified serialization + 200/application/xml with correct namespaces on a clean prod build — (this commit)
- 2026-07-21 — [design agent] /quiz onto the dusk identity: replaced the plain centered header with a compact center-aligned PageHero (skyline band, gold "60-second quiz" eyebrow, single h1) flowing into the progress bar + questions — strengthens a key lead-gen path (quiz captures name/email/phone at the end). Verified single h1 + 375px no-overflow on a clean prod build. Remaining PageHero target: /sell (two-column form hero, leave carefully) — (this commit)
- 2026-07-21 — [improvement agent] Admin leads dashboard: added an Export CSV button (downloads the currently-filtered leads with proper CSV escaping + UTF-8 BOM for Excel) so no captured lead is stranded in the UI — Hamza can work them in a spreadsheet/CRM. Sources already all visible + dedupe already at /api/lead. Unit-verified escaping; build green — (this commit)
- 2026-07-21 — [seo agent] Neighbourhoods directory structured data: added ItemList JSON-LD (24 guide pages, built server-side from HOOD_DATA — helps Google understand the directory and discover/rank each guide) + a BreadcrumbList (the index lacked one) via the server layout. Verified both render with valid guide slugs + parseable JSON on a clean prod build — (this commit)
- 2026-07-21 — [design agent] /news onto the dusk identity: replaced the plain header with a compact PageHero (skyline band, gold "Live market news" eyebrow, single h1); relocated the Updated timestamp + Live-Feed badge + Refresh button into a control row below the hero (all preserved). Completes the PageHero rollout on content/secondary pages. Verified single h1 + 375px no-overflow on a clean prod build — (this commit)
- 2026-07-21 — [improvement agent] Weekly newsletter subject line: the generic subject (most imported subscribers have no saved search) was a hookless "Mississauga Market Weekly — {date}". New buildSubject() leads with the top deals real hook — "+$X/mo cash flow in {hood}" or "N new deals — top X.X% cap rate in {hood}" — all values guarded (unit-tested: never N/A/NaN, honest about cash flow). Lifts open rates → more clicks → more leads. Build green — (this commit)
- 2026-07-21 — [seo agent] Blog index ItemList: added ItemList JSON-LD (one ListItem per published post, built server-side from the Supabase fetch) so search engines understand the blog as a collection and discover/rank posts. Guarded — filters posts missing slug/title, renders nothing when empty (verified: no posts locally → no empty schema; BreadcrumbList regression clean). Unit-tested the mapping. Populates in prod — (this commit)
- 2026-07-21 — [design agent] Blog post page onto the dusk identity: dusk-gradient hero + CityscapePanorama skyline (matches the 375px-verified /about hero) + SkylineStrip before the newsletter CTA; fixed hero/CTA WCAG-AA contrast (white/40→/70, /50→/75, /30→/60 on navy). Build green; graceful notFound without Supabase — (this commit)
- 2026-07-21 — [improvement agent] Added break-even rent to the listing-detail Mortgage tab: new breakEvenRent() engine helper solves the rent where cash flow = 0 numerically against the real calculateCashFlow (binary search), so it can never diverge from the shown cash flow and handles freehold-maintenance vs condo-fee automatically. Display compares it to the estimated rent (clears by / falls short by). Hand-verified: $700k→$4,672/mo, $500k condo+$600 fee→$3,554/mo (cashFlow=0 at each). Build green — (this commit)
- 2026-07-21 — [seo] Target GSC striking-distance query 'best neighbourhoods to invest in mississauga' (70 impr, pos ~15): retitled /neighbourhoods to exact-match, added ranked answer block + 3-Q FAQ JSON-LD; logged 4 more target queries from Hamza's Search Console — (this commit)
- 2026-07-21 — [seo agent] Target GSC money keyword "investment properties for sale mississauga" (pos ~14): /listings title/H1/intro/description now exact-match it + added BreadcrumbList. Also fixed a pre-existing doubled-brand title on /listings (page title included "| MississaugaInvestor.ca" on top of the layout's title template → brand shown twice). Verified single-brand title + single h1 + 375px clean — (this commit)
- 2026-07-21 — [design agent] /pre-construction (VIP lead-capture page) onto the dusk identity: compact PageHero (gold "Pre-construction VIP" eyebrow, single h1) above the HST banner + VIP form; old left-column h1 demoted to h2 to keep exactly one h1. Also audited the homepage at 768px + 375px — no overflow, hero grid clean (item checked off). Verified single h1 + 375px no-overflow on prod build — (this commit)
- 2026-07-21 — [improvement agent] Daily deal-alert email subject now leads with the top new match's real hook ("New deal: +$X/mo cash flow in {hood} + N more" / "N new deals — top X.X% cap rate in {hood}") instead of a flat count — lifts open rates on the daily retention email. All values guarded (unit-tested: never N/A/NaN, honest about cash flow). Also FLAGGED for Hamza a wrong number: the /pre-construction/hst-rebate worked example shows a $104k provincial rebate that exceeds the $68k provincial HST on an $850k home (see Needs Hamza) — build green — (this commit)
- 2026-07-21 — [seo agent] Built /rent-vs-buy-mississauga for the top GSC striking-distance query "rent vs buy mississauga 2026" (89 impr, pos 16): exact-match title/H1, direct-answer + factors + visible FAQ, FAQPage & BreadcrumbList JSON-LD, dusk PageHero; added to sitemap + linked from the mortgage-calculator. Honest general content (no fabricated numbers), routes to the audited calculator. Interactive break-even calc deferred (new financial calc → improvement agent). Verified single-brand title + schema + single h1 + 375px clean — (this commit)
- 2026-07-22 — [hamza request] Lead notifications now carry what-it's-for + the property: Book-a-Viewing threads listing→booking→email (Viewing Request subject + address + View-listing link), Sign Up + Google carry the listing the visitor was viewing, lead email got real source labels + a one-line intent, admin leads dashboard gained viewing/booking/precon-vip labels+filters. Build green; booking viewing-banner verified at 375px — 79e4f32
- 2026-07-22 — [design agent] Listing detail (top conversion page) onto the dusk identity: the analysis tabs had no section heading — added a titled "Investment Analysis" h2 with a SkylineStrip filling the row beside it (brand divider + heading hierarchy + SEO h2). Photo gallery confirmed prominent; sticky CTA untouched. Build green; overflow-safe at 375px (needs live MLS data for a full local render) — 5cebcc7
- 2026-07-22 — [improvement agent] Email reliability: the daily deal-alert email rendered l.hamzaScore.toFixed(1) and l.cashFlow.toLocaleString() UNGUARDED — a single malformed listing (undefined/NaN field, the exact case the listings grid already had to defend) would THROW and abort the whole email, silently leaving that subscriber with NO alert. Guarded every rendered number (score/cap/cash-flow/price/DOM → safe '—' fallback, never a throw or NaN) and fixed negative cash-flow formatting to −$X/mo. Applied the same finite-number guards to the weekly newsletter hero + deal rows (were rendering "Rated undefined/10" / "−$NaN/MO" cosmetically). Unit-tested: malformed listing no longer throws; build green — f445bf1
- 2026-07-22 — [seo agent] Built /hurontario-lrt-real-estate for the GSC striking-distance query "hurontario lrt real estate impact" (45 impr, pos 7.8): exact-match title/H1, direct-answer + corridor-node cards + "why transit moves the numbers" + visible FAQ, FAQPage & BreadcrumbList JSON-LD, dusk PageHero. Honest/general (no fabricated appreciation % or opening date). Added to sitemap; internal-linked from the existing isLRT callout on every corridor guide (Port Credit/City Centre/Cooksville/Hurontario). Verified exact-match single-brand title + single h1 + both schemas + canonical + link-conditional on a clean prod build — b05220f
- 2026-07-22 — [design agent] Listing cards (/listings) hover polish to match HomeDealCards: swapped the whole-card `hover:scale-[1.02]` (jittery, less calm) for the premium `hover:border-accent/30 hover:shadow-lg duration-300` + slow `duration-500` photo zoom, so the two card types now feel identical. Class-only, build green, removes layout shift. Flagged a HomeDealCard `capRate.toFixed()` throw risk for the improvement agent — c8ca3a4
- 2026-07-22 — [improvement agent] HOMEPAGE reliability: HomeDealCard rendered deal.capRate.toFixed(1) UNGUARDED — one malformed deal (undefined/NaN capRate) would THROW and blank the homepage "top deals" section (the #1 conversion surface); score badge + cash-flow could also print "undefined"/"$NaN". Added pct1() + finite guards (→ '—', never throw/NaN), matching what the listings grid already had; also guarded the last unguarded number on ListingCard (score badge). Unit-tested malformed deal (no throw); homepage + /listings 200; build green — 7a7dd73
- 2026-07-22 — [seo agent] Built /townhouse-vs-condo-investment for the GSC striking-distance query "townhouse or condo better investment" (50 impr, pos 25.6): exact-match title/H1, direct-answer + 8-row comparison table + visible FAQ, FAQPage & BreadcrumbList JSON-LD, dusk PageHero. Honest/general (no fabricated numbers; surfaces the freehold-vs-POTL townhouse fee nuance). Added to sitemap + inbound link from the /listings header. Verified exact-match single-brand title + single h1 + both schemas + canonical + table overflow-x-auto (no 375px overflow) on a clean prod build — 7bde555
- 2026-07-22 — [design agent] Pre-construction project cards: the no-image placeholder was h-32 (uneven vs h-44 photo cards) and off-brand (grey + 🏗️ emoji). Rebuilt as a dusk-gradient + SkylineStrip placeholder at h-44 (even rows, on-brand, no layout shift), dark-bg backdrop-blur badges (AA), and aligned the card hover to the standardized premium treatment. Build green; page 200 — 1886686
- 2026-07-22 — [improvement agent] Lead capture: the 3 new SEO landing pages (rent-vs-buy, hurontario-lrt, townhouse-vs-condo) had NO email capture — search traffic that wasn't ready to browse had no way to convert. Added a mid-page inline email capture (InlineCTA newsletter variant → /api/newsletter/subscribe, honest weekly-deals copy, UTM-tagged) between the body and FAQ on all three. Verified single h1 preserved, field renders, endpoint 400-on-invalid, build green — 12c9d85
- 2026-07-22 — [seo agent] Built /cash-flow-positive-properties-ontario for the GSC query "cash flow positive properties ontario" (26 impr, pos 19.7): exact-match title/H1, honest direct-answer + 4 cash-flow "levers" + inline capture + visible FAQ, FAQPage & BreadcrumbList JSON-LD, dusk PageHero. Funnels to a real filtered view via the existing ?cf=1&sort=cashflow deep-link (200). Added to sitemap; inbound-linked from a tidied /listings "Investor guides:" row (this + townhouse-vs-condo). Verified exact-match single-brand title + single h1 + both schemas + canonical + deep-link on a clean prod build — 927b3fb
- 2026-07-22 — [hamza request] CAP-vs-PCF explainer: Hamza asked why the daily alert showed positive cap rate + negative cash flow (verified NOT a bug — cashFlow = NOI − mortgage; reproduced all 5 listings within $0–3; the mortgage P+I simply exceeds NOI at 4.89% vs ~4.9% cap, and most of the negative is principal/equity). Shipped a one-line explainer everywhere the two chips sit together: the daily alert email (legend under the listings), the /listings header, and the listing-detail key-metric tiles (shown ONLY when capRate>0 && cashFlow<0, so it never clutters positive cases). Honest wording ("most of that gap is principal you keep as equity"). Single h1 preserved; build green — f573004
- 2026-07-22 — [improvement agent] Swept the LAST unguarded number renders on public listing views: Top-Picks mini card, table view, and map popups all called capRate/cashOnCash.toFixed(1) raw — one malformed listing would THROW and break that view (same class already fixed on the grid + homepage). DRY fix: added guarded pct1() to lib/utils/format.js + hardened fmtK/fmtNum/fmtCurrency ('—' instead of "$NaN" on non-finite, site-wide), routed those views + deal-screener aggregate tiles + score badges through it. Unit-tested; /listings + homepage 200; build green — adf2a51
- 2026-07-22 — [hamza request] Homepage neighbourhood cards were static hardcoded data presented as current (Hamza asked where it came from / wanted it accurate + monthly). Made avg price, DOM & rent yield LIVE per-neighbourhood from the active listing feed (reused fetchTopDeals' processed listings — no extra fetch; ≥4-listing threshold w/ curated fallback), ranked top-4 by live yield, added a "Live from active listings" pill, and honestly relabeled the Hot/Warm/Cool trend + YoY as curated outlook ("last reviewed April 2026", YoY* footnote). Unit-tested aggregation; homepage 200, single h1, build green — 9b18809
- 2026-07-22 — [hamza request] Rolled the live neighbourhood stats onto the /neighbourhoods index + detail pages (Hamza: "yes"). Shared computeHoodStats (lib/listings/hood-stats.js) + new GET /api/neighbourhood-stats (revalidate 600) feed both the client index (fetch+merge) and the now-async-ISR detail page (live avg price/DOM/yield in the stat grid, worked cash-flow example, FAQ + FAQ schema). Trend/YoY/inventory/scores stay curated + labeled; Live pills; curated fallback when feed down. Build green (87 pages); endpoint valid JSON; both pages 200, single h1, FAQPage intact — 925510e
- 2026-07-22 — [design agent] Completed the PageHero rollout: /sell (seller-valuation lead-capture page) onto the dusk identity — compact dusk PageHero (single h1) above the UNTOUCHED two-column form, old left-column section-title h1 demoted to an h2 lead-in. Form JSX byte-unchanged (the reason it was deferred). Verified single h1 + dusk gradient + form fields intact; build green; grid stacks single-column at 375px — 0729c81
- 2026-07-22 — [seo agent] Built /rental-property-insurance-mississauga for the GSC query "rental property insurance mississauga" (26 impr, pos 14.4): exact-match title/H1, direct-answer + 4 coverage cards + require-tenant-insurance callout + inline capture + visible FAQ, FAQPage & BreadcrumbList JSON-LD, dusk PageHero. Honest/general (no fabricated premiums/limits; explicit not-advice + see-a-licensed-broker disclaimer). Added to sitemap; inbound-linked from the mortgage-calculator carrying-costs note. Verified exact-match single-brand title + single h1 + both schemas + canonical + inbound link on a clean prod build — a924a29
- 2026-07-22 — [improvement agent] Turned the CAP/PCF insight into investor value: added a verified mortgageSplit() helper + an "equity build" line on the listing-detail Mortgage tab showing how much of the mortgage payment pays down principal (money kept), and — when cash flow is negative but equity build exceeds the shortfall — that net worth still grows. Reframes the negative-cash-flow misread that can deter leads. ADDS a line only, no existing number changed; math hand-verified to the penny vs the earlier reconciliation ($630k→$867, $999k→$1,374/mo). Also flagged the flat-$225 insurance assumption as Needs-Hamza. Build green — bf71357
- 2026-07-22 — [design agent] Connected the 5 investor-guide landing pages into a hub: new reusable RelatedGuides component (on-brand cards, hover:border-accent/30, grid-cols-1 sm:grid-cols-2) renders "More investor guides" (the other 4, self-excluded) before the final CTA on rent-vs-buy, hurontario-lrt, townhouse-vs-condo, cash-flow-positive & rental-insurance. Removes the dead-end at the bottom of each search-landing page (keeps readers on-site toward capture) + cross-links the set for topical authority. Verified section renders the correct 4, self-exclusion works, single h1 preserved, mobile-safe grid; build green (88 pages) — 720e66c
- 2026-07-22 — [improvement agent] Lead-labelling gap: audited every source: value sent site-wide vs the label maps — seller-valuation (the /sell home-valuation form, a high-intent listing-side lead) was the one hitting /api/lead with NO friendly label, so it reached Hamza's inbox as the raw slug "seller-valuation". Added it to the notification email (label "Seller — Home Valuation" + intent line "Requested a free home valuation (seller lead)") AND the admin leads dashboard (🏡 Seller label, colour, filter pill) so seller leads are clearly identified everywhere. Confirmed the inline-cta*/mailerlite sources go to the newsletter endpoint (not lead notifications) so need no lead label. Build green — 4fe55dc
- 2026-07-22 — [seo agent] Own the brand query "hamza nouman" (129 impr, pos 4.4): /about already had H1 "Hamza Nouman" + brand title + full PersonJsonLd + 0.9 sitemap priority; added the Google-recommended ProfilePage structured data (ProfilePageJsonLd, previously unused) whose mainEntity shares the same @id as the full Person node, plus mainEntityOfPage on the Person → /about, so Google treats /about as THE authoritative page for the name. Verified both schemas + shared @id + single h1 on a clean prod build. Completes the section-5a striking-distance plan — 9f3b3d1
- 2026-07-22 — [design agent] Surfaced the investor-guide hub in the footer: the 5 guides (cash-flow-positive, rent-vs-buy, townhouse-vs-condo, hurontario-lrt, rental-insurance) were only reachable from search + each other's RelatedGuides — added an "Investor Guides" footer column so they're discoverable from EVERY page (more paths into the capture-rich guides + site-wide internal links for SEO). Grid widened md:grid-cols-4 → lg:grid-cols-5 (md untouched, stacks to 1 col at 375px). Verified all 5 links render site-wide, no overflow; build green — 37765a5
- 2026-07-22 — [improvement agent] LEAD LEAK fixed: the /listings signup-gate modal's "Skip for now" button (email captured in step 1, name/phone skipped) only wrote the email to localStorage and NEVER POSTed to /api/lead — so every email-only micro-conversion was silently lost, Hamza never saw it. handleSkipStep2 now fires a /api/lead POST (email + whatever was entered; source "Sign Up (email only)"/"View Limit (email only)" so it's NOT 'registration' and doesn't 400 on the missing phone; fire-and-forget so it never blocks the UI). Verified email-only → 200 success, invalid email still → 400; build green. Hamza now receives these leads (clearly tagged email-only for follow-up) — 5dd50d9
- 2026-07-22 — [seo agent] Built the /guides hub — a rankable collection landing for the 5 investor guides (targets "mississauga real estate investor guides"): PageHero + all 5 guide cards + ItemList & BreadcrumbList JSON-LD + CTA. Completes the hub-and-spoke internal linking — the footer "Investor Guides" heading now links to /guides, and each guide's RelatedGuides section gained a "See all investor guides →" link. Added to sitemap (priority 0.7). Exported the shared GUIDES list from related-guides.js for reuse (DRY). Verified single-brand title + single h1 + both schemas + canonical + all 5 links + sitemap/footer/spoke links on a clean prod build (89 pages) — 3400186
- 2026-07-22 — [design agent] Surfaced the guide hub in the PRIMARY nav: renamed the "Market Data" header dropdown to "Resources" (a clearer umbrella — it already mixed data + Deal-Score methodology) and added "Investor Guides" → /guides as its first child. The educational hub is now reachable from every page's header (desktop dropdown + mobile menu), not just the footer — zero crowding (no new top-level item). Verified Resources renders with Investor Guides→/guides site-wide, "Market Data" label gone, mobile-safe; build green — 4695863
- 2026-07-22 — [improvement agent] Verified property-tax rates (feed into EVERY listing's cash flow): TAX_RATES table is accurate/current — Mississauga 0.84%, Toronto 0.63%, Brampton 1.13%, Oakville 0.83%, etc. all match 2025-26 GTA municipal rates, and the engine uses the listing's real tax when present (estimate only as fallback). No bug. Then closed a lead-capture gap: the new /guides hub (a rankable, search-traffic landing) had NO email capture unlike its sibling guide pages — added the inline newsletter capture. Verified capture + single h1; build green — e88dccb
- 2026-07-22 — [hamza request] Built the ELITE /sell seller landing page (Hamza: site had nothing real for home sellers + not in nav). Server page w/ seller-targeting SEO metadata + FAQPage/Breadcrumb JSON-LD + a client valuation form (source 'seller-valuation'); dusk hero with the form embedded, 4 differentiators, 3-step process, REAL Google reviews + 5.0 badge, seller FAQ, final CTA — all honest, no fabricated stats. Added "Sell" to the primary nav (Blog moved into Resources, no crowding) + sitemap 0.6→0.9. Verified single h1 + seller title + both schemas + form + real reviews + site-wide nav link on a clean prod build (89 pages) — c646af1
- 2026-07-22 — [seo agent] Strengthened the new /sell money page's SEO: it was missing from the footer (no site-wide internal link) — added "Sell Your Home" to the footer Platform column (every page now links to it), and added page-relevant RealEstateAgent service structured data (areaServed Mississauga/Peel + makesOffer "Free Home Valuation & Investor Offer Preview" + provider → the Person node). Deliberately NO self-serving aggregateRating in schema (Google discourages it; the real 5.0 Google rating stays as visible on-page proof). Verified RealEstateAgent + makesOffer render, FAQPage intact, single h1, footer link site-wide; build green — 7bb1dc1
- 2026-07-22 — [improvement agent] Exit-intent relevance: the site-wide exit-intent popup is a BUYER offer ("Get Daily Deal Alerts" / investment listings) but fired on every page — including /sell, where it interrupted SELLERS with an off-message pitch, and /book-call, where it distracted people mid-booking. Made the trigger path-aware: it now skips /sell + /book-call (reads window.location.pathname, so ?listing= query variants are covered too) and — crucially — stays ARMED on those pages so it still fires later on a relevant page (doesn't waste the one-shot). Both those pages already have a stronger, page-specific CTA. Verified exclusion logic (correct incl. no false-match on /sellers-*); build green — (this commit)
- 2026-07-22 — [design agent] Added a homepage SELLER band: the homepage (the #1 page) was entirely buy/investor-focused with no seller entry point, so homeowners who land there had nowhere to go. Added a navy dusk-gradient band ("Thinking of selling? See what an investor would pay.") with the Investor Offer Preview pitch + skyline motif + CTA → /sell — placed right after the existing buyer "First Month's Mortgage" band (buyer offer light, seller offer dark, balanced). Captures seller leads from the highest-traffic page + strong internal link to the money page. Verified band renders, links to /sell, single h1 preserved, WCAG-AA white-on-navy, stacks at 375px; build green — (this commit)
- 2026-07-22 — [hamza request] Reframed the /sell page around the "Investor Offer Preview" hook (Hamza's pick — he explicitly rejected any "sold guarantee or I buy it" offer; he won't buy homes). The genius: leverage his REAL investor-buyer network as a matchmaker (zero balance-sheet risk). New hero H1 "See What an Investor Would Pay for Your Mississauga Home — Before You List", a "Two ways to sell — you choose" section (quiet investor sale vs full-market listing), form retitled "Get My Investor Offer Preview" + a "your goal" qualifier, reframed steps/FAQ (incl. "What is an Investor Offer Preview?"), distinct lead source 'investor-offer-preview' labelled "Seller — Investor Offer Preview" in the email + "🔑 Seller Preview" in the CRM (+ filter). Honest throughout (gauges real interest, not a guarantee). Verified single h1 + new hero/sections/form + FAQ schema + lead endpoint 200/labelled on a clean prod build — (this commit)
- 2026-07-23 — [improvement agent] Exit-intent popup made path-aware: the buyer "Get Daily Deal Alerts" popup was firing on /sell (off-message to sellers) and /book-call (mid-booking); trigger now skips those paths but stays armed to fire later on a relevant page. Build green — 23c7464
- 2026-07-23 — [seo agent] Fixed a site-wide og:image regression: 22 pages that export their own `openGraph` block (market-pulse, book-call, faq, compare, quiz, all 5 investor guides, /guides, /sell, /alerts, mortgage-calculator, neighbourhoods index+detail, recent-sales, 3 pre-construction pages, blog index) were losing the branded og:image because Next replaces (not merges) the parent openGraph — so they shared imageless on Facebook/LinkedIn/WhatsApp/Slack/iMessage (only twitter:image survived). Added `images: ['/opengraph-image']` to each; pages with an intentional custom image (about/blog[slug]/listings[id]) left alone. Verified og:image now renders on every previously-broken static page on a clean prod build (89 pages); no calc/data change — 2be53a1
- 2026-07-23 — [design agent] Redesigned the branded /opengraph-image social card (now the share preview on 22+ pages after the og:image fix): removed four hardcoded market stats presented as current (2,000+ listings / 96% sale-to-list / 36-day DOM / 5.2mo inventory) that rot into wrong numbers on a static, un-refreshable edge image, and fixed the off-brand emerald ".ca". Rebuilt on the brand dusk gradient (#16223D→navy→#25355C), site-matched accent-blue ".ca" + a gold accent underline, and four evergreen always-true capability chips (Live MLS Listings · Cash Flow & Cap Rate · Investor Deal Scores · Free Deal Alerts). Rendered locally: 200, valid 1200×630 PNG, premium/on-brand, white-on-navy AA. Presentational only, no calc/data change — 9edd6a4
- 2026-07-23 — [hamza request] Hamza reported he sees no "Sell" on the live site + asked for a health check. DIAGNOSIS: the Sell page + "Sell" nav link + Resources dropdown are all BUILT and correct on the agent branch (verified /sell 200, single h1 "See What an Investor Would Pay…", 3 /sell links on the homepage), but production/main is 24 commits behind — the last daily release (cab3237, #10) was cut before the Sell work landed, so origin/main's header has zero /sell references. Not a bug — a release-lag gap; ships in the next daily release. HEALTH CHECK: build green (89/89); all key public pages 200 with a single h1. FOUND + FIXED one real SEO issue: /market-pulse (a 'use client' page targeting "Mississauga housing market" queries) returned its PageHero h1 only AFTER the client data fetch — the loading branch rendered a bare skeleton, so the server HTML crawlers see had NO h1. Added the PageHero (h1 "Market Pulse") to the loading branch too; verified h1 now present in SSR (was 0, now 1). Homepage stat band confirmed live-driven (not hardcoded). — (this commit)
- 2026-07-23 — [improvement agent] WRONG-NUMBER fix (GTA tax): with the GTA rollout now prominent, traced the GTA feed → processListings → getTaxRate(hood) path. Neither feed passes the real MLS tax, so cash flow uses price×getTaxRate(city). Cities in TAX_RATES were correct, but the AMALGAMATED DISTRICTS weren't in the table and fell to DEFAULT (0.95%): Hamilton's communities (Stoney Creek/Dundas/Ancaster) were UNDER-taxed 0.95% vs 1.26% — overstating cash flow by ~$207/mo on an $800k home (the dangerous direction); Toronto's former boroughs (Etobicoke/North York/Scarborough/East York/York) and Georgetown (Halton Hills) could miss their parent rate too. Added all as aliases pointing to their parent municipality's rate (a legal fact post-amalgamation, not an estimate). Hand-verified getTaxRate for all 13 cases incl. unchanged Mississauga/Toronto/Brampton + unknown→default; build green. NEEDS HAMZA (minor): King Township isn't in TAX_RATES (separate municipality) so it uses the 0.95% default — provide its rate to refine. Bigger picture: neither the Mississauga nor GTA feed selects TaxAnnualAmount, so ALL listings use estimated (rate×price) tax rather than the real MLS tax — worth wiring the real field through for exactness (larger change).
- 2026-07-23 — [improvement agent] WRONG-NUMBER fix (GTA rent): processListings never read the feed-provided rent (no l.rent/l.estimatedRent reference), so the GTA feed's CITY-TIERED rent estimate (Toronto ×1.15 … Oshawa ×0.80) was discarded and rent was recomputed with a generic bedroom+price formula — over-renting cheaper cities and under-renting pricier ones, skewing every GTA listing's cash flow / cap rate / score. Made the non-Mississauga single-family (no-suite) rent path prefer l.estimatedRent when present. Mississauga is untouched: its hoods are in HOOD_RENTS so it takes the earlier branch and never reaches this code. Hand-verified the branch arithmetic: Toronto 2bd/$700k → $2,550 (city-tiered) vs old generic $2,300; Oshawa 2bd/$500k → $1,800 vs old OVERSTATED $2,100; no feed rent → generic fallback unchanged. Build green. NOTE: suite + multi-unit GTA paths still use their own estimates (they add basement/per-unit income) — a further refinement, flagged.
- 2026-07-23 — [improvement agent] CONSISTENCY fix (GTA rent, card vs detail): the earlier GTA-rent commit made processListings prefer the feed's city-tiered estimatedRent — but /api/listing-single (the listing-DETAIL fetch) returns estimatedRent:0, so a Toronto listing showed city-tiered rent on the /gta CARD yet the generic estimate on its DETAIL page → different cash flow for the same listing (a trust-killer I'd introduced). Centralised the city tier: added CITY_RENT_TIER + getCityRentTier() to constants (mirrors the GTA feed) and applied it inside processListings' fallback (both suite + no-suite), which is the ONE function both the card and detail flow through. Now the detail page (feed rent absent) computes the SAME tiered rent the card showed. Hand-verified card==detail for Toronto ($2,550), Oshawa ($1,800), Vaughan ($2,850); Mississauga tier = 1.0 (unchanged, and it uses HOOD_RENTS anyway); getCityRentTier spot-checked (Toronto 1.15 / Oshawa 0.80 / Hamilton 0.85 / Cooksville 1.0 / unknown-GTA 0.90). Build green (89 pages).
- 2026-07-23 — [improvement agent] WRONG-DATA fix (deal-screener sold stats on GTA pages): the DealScreener (rendered inside ListingsContainer on BOTH /listings and /gta) fetched /api/sold-comps?limit=20 with NO city param → the API defaults to Mississauga, so its "Avg Sold" price + sale-to-list stat showed MISSISSAUGA sold data even on /gta?city=Toronto (a Toronto page presenting Mississauga's sold market as context — misleading). Scoped the fetch to the page's city: /listings → Mississauga, /gta?city=X → that city (sold-comps already special-cases Toronto sub-areas), and the /gta "All GTA" hub skips the sold stat entirely (no single city → no misleading number). Verified the listing DETAIL page was already correct (it passes listing.city to sold-comps/price-history/estimated-value). Hand-verified the city-selection logic for all 5 contexts; build green (89 pages).
- 2026-07-23 — [seo agent] Added GTA listing detail pages to the sitemap. The sitemap pulled listing URLs only from /api/listings (Mississauga), so individual GTA listing pages (Toronto, Brampton, etc. — same /listings/{id} route) weren't in it and Google couldn't discover them (listing/address pages win real clicks). Added a bounded (≤5 pages × 200) GTA fetch from /api/listings-gta in its own try/catch so it can never break the sitemap, mapping each to /listings/{id} (priority 0.55, just under Mississauga's 0.6). Verified sitemap.xml still 200 with the 28 GTA city pages intact and no regression on a clean build; GTA listing URLs are 0 locally (no AMPRE token → try/catch → empty) and populate in production. Build green (89 pages).
- 2026-07-23 — [design agent] Site-wide 375px audit complete: batch-measured 11 remaining routes (about, faq, blog, compare, recent-sales, news, score-methodology, gta?city, a neighbourhood detail, privacy, terms) via headless-Chromium device emulation — ALL zero-overflow (sw==375), single h1; /about verified premium with strong CTAs. No bug anywhere. Then closed a positioning inconsistency: the footer "Service Areas" (on every page) listed ONLY Mississauga neighbourhoods, contradicting the GTA rollout Hamza asked for. Kept the Mississauga neighbourhoods (flagship) and added a compact "Across the GTA" row linking Toronto/Brampton/Vaughan/Oakville/Markham/Hamilton + "All GTA →" (to /gta?city= pages) — aligns the brand site-wide and adds footer internal links to the 28 city pages. Verified on a clean build: links render, wraps cleanly, no 375px overflow, on-brand (white/50 on navy + accent CTA). Build green (89 pages).
- 2026-07-23 — [improvement agent] Investigated deal-alerts for the GTA rollout and surfaced a bigger issue: the saved-search FILTER PIPELINE is format-mismatched end to end (Save-Search button sends client-format keys, /api/alerts/subscribe sanitises to a non-overlapping API-format allow-list → stores ~empty filters, cron applyFilters reads client format) → saved searches likely don't respect the user's criteria; PLUS the cron is Mississauga-only so GTA saved searches never match GTA listings. Both need Supabase/Resend/cron test access to fix safely (the retention engine) — documented in detail under "Needs Hamza". Did NOT change the alert engine blind (a draft city-capture edit was reverted once I saw the sanitiser would drop it). Shipped a small, safe, verifiable fix instead: the homepage hero badge now only claims "Live Data · Updated every 24 hours" when the market-stats fetch actually returns; on a genuine fetch failure (liveStats null) it shows "Market snapshot" so curated fallback numbers aren't mislabeled as live. Build green (89 pages). (NOTE: /api/market-stats itself returns a hardcoded TRREB fallback when live data is absent — separate, already-flagged Needs-Hamza; the homepage can't tell that apart, so the badge stays "Live Data" in that case.)
- 2026-07-23 — [seo agent] Added ItemList structured data to the /gta hub. Breadcrumb coverage audit came back near-complete (only top-level /quiz + /alerts lack one — marginal for 2-level pages). The real gap: /neighbourhoods marks itself a directory via ItemList but the /gta hub didn't, so search engines didn't see it as the collection page for the 28 city pages. Added an ItemList (built server-side from CITY_COPY, 28 cities → /gta?city= URLs) on the hub view only; individual city pages keep just their city breadcrumb. Verified /gta emits ItemList with 28 items (+2 breadcrumb ListItems = 30 total) and /gta?city=Toronto has none; build green (89 pages).
- 2026-07-23 — [design agent] True-375px audit of the lead-gen + search-landing pages (quiz, alerts, pre-construction, cash-flow-positive guide, /guides) via headless-Chromium device emulation — all zero-overflow (sw==375), single h1, premium dusk heroes. Cookie banner measured 100px / 12% of viewport (already compact, fine). Fixed one real affordance gap: the /gta hero city chips (Toronto, Brampton, Vaughan…) LOOKED tappable but were plain <span>s — a visitor couldn't click the city they wanted and they weren't crawlable. Made them Links to /gta?city= (kept "+ More" as a label), with a hover state, on the dusk hero. Verified 5/5 chips are now links, single h1, sw==375; build green (89 pages).
- 2026-07-23 — [seo agent] Full on-page audit (crawled 27 public routes): every route has a UNIQUE title, a canonical, and exactly one h1 — no duplicates/gaps; technical SEO is complete. Titles run long on a few money pages (100-113 chars, brand suffix truncates in SERP) but keywords are front-loaded so the visible portion is intact — left as-is. The real remaining lever was INTERNAL LINKING: /listings has an "Investor guides:" link row but the now-indexed /gta + 28 city pages had none, so they passed no link equity to the guide pages and gave GTA search visitors no next step. Added an Investor-guides row (All guides · Cash-flow-positive properties · Townhouse vs condo) to /gta — 29 pages now link into the guides. Verified 3/3 links render on /gta?city=Toronto, single h1, sw==375 (no overflow); build green (89 pages). Also confirmed listing-card/map images already lazy-load and the MLS CDN is preconnected (CWV sound).
- 2026-07-23 — [design agent] True-375px audit of book-call, neighbourhoods & mortgage-calculator via headless-Chromium device emulation — all three zero-overflow (sw==375), single h1, premium (dusk heroes, image-forward hood cards render great). No bug found. Then closed a real consistency gap: /listings shows a "CAP is the all-cash yield (before financing); cash flow is after the mortgage…" clarifier (the exact positive-cap/negative-cashflow confusion Hamza raised), but the now-prominent /gta + 28 city pages had NO such note. Added the same clarifier under the region switcher on /gta so GTA investors get the same context. Verified it renders on /gta?city=Toronto, single h1 preserved, sw==375 (no overflow); build green (89 pages).
- 2026-07-23 — [seo agent] Per-city OG/Twitter on the GTA city pages: /gta's generateMetadata set title/description/canonical but NO openGraph, so all 28 /gta?city= pages inherited the ROOT's generic OG title (Next uses the parent openGraph when a page omits it) — a shared Toronto link previewed as "MississaugaInvestor.ca…" instead of "Toronto Investment Properties". Added per-city openGraph (title/description/url + the branded /opengraph-image) and a twitter card, plus OG on the base /gta. Verified /gta?city=Toronto now emits og:title "Toronto Investment Properties" + twitter:title + branded og:image on a clean prod build (89 pages). No calc/data change.
- 2026-07-23 — [design agent] True-375px audit via headless-Chromium device emulation (CDP, deviceMetrics 375/mobile — earlier window-size shots were misleading because headless rendered at ~500px and clipped, giving false "overflow"). Measured scrollWidth==innerWidth==375 (zero horizontal overflow) on the homepage, /listings, /gta and /sell — all clean and premium; the region switcher and my /sell rewrite render well at 375px. Fixed the one real flaw found: on the listings toolbar the "Save Search" button wrapped to two lines ("Save / Search") because the Sort select squeezed it in a non-wrapping flex row. Added whitespace-nowrap + flex-shrink-0 to the button and flex-wrap to the controls group, so the button stays one line and drops to its own row when cramped. Verified button height 38px (single line) + sw=375 after the fix; build green. On /listings + /gta (money pages).
- 2026-07-23 — [seo agent] Per-city breadcrumb on the GTA city pages: every /gta?city= page emitted the same generic "Home › GTA Listings" BreadcrumbList, so the 28 newly-indexed city pages shared one trail. Added a city node when a valid city is present (Home › GTA Listings › {City}), so each page gets its own breadcrumb rich-result trail; the base /gta stays two-level. Verified /gta?city=Toronto → 3-item breadcrumb (…› Toronto), /gta → 2-item, on a clean prod build (89 pages). No calc/data change.
- 2026-07-23 — [design agent] Elevated the homepage GTA coverage band: it shipped yesterday as a flat white bordered box that looked less premium than the rest of the dusk-themed homepage and didn't pair with the navy seller band right below it. Rebuilt it to mirror the seller band's premium structure (pill eyebrow + 🌆 icon tile + headline + city chips + CTA + a subtle low-opacity SkylineStrip), light-themed on a from-cloud gradient — so the two audience bands now read as a matched pair (light buyer band → dark seller band). Verified with a real 375px headless-Chromium screenshot: cohesive, on-brand (navy/accent only), WCAG-AA (navy on cloud), no overflow, no layout shift; build green. FINDING for the conversion sprint: the homepage now stacks 4 consecutive full-width promo bands (email capture → First-Month-Mortgage → GTA → seller) — worth watching for "band fatigue"; may want spacing/consolidation.
- 2026-07-23 — [improvement agent] Homepage hero MISLABEL fixed: the hero subtitle read "{heroCount} GTA Investment Properties", but heroCount = topDeals.totalCount which comes from the MISSISSAUGA feed (/api/listings) — so it counted Mississauga while calling it GTA (investors trust numbers; the #1 page's headline stat was mislabeled). Changed the label to "Mississauga Investment Properties" so the count and the label match, consistent with the new "Mississauga Investment Property Finder" H1 (GTA coverage is communicated by the separate GTA band + region switcher). Copy-only; no number/calc change. Build green; homepage renders "Mississauga Investment Properties".
- 2026-07-23 — [hamza request] Homepage hero renamed "Mississauga Investment Deal Finder" → "Mississauga Investment Property Finder" (Hamza's call). "Investment property" is the stronger, higher-volume search phrase and reads clearer than "Deal Finder"; keeps the Mississauga-flagship framing. One-word span swap in the hero h1; build green; verified homepage renders "Property Finder". Shipped to production.
- 2026-07-23 — [seo agent] Added the 28 per-city GTA pages (/gta?city=Toronto, Brampton, Vaughan, Oakville, Markham, Hamilton, all of Durham/York/Halton/Peel + Toronto sub-areas) to sitemap.xml. Each already has a unique title/description + self-canonical but was only linked from the header mega-menu, so Google under-discovered them; listing them targets "{City} investment properties" for each. Exported CITY_COPY from the /gta page so the sitemap derives the list (DRY, stays in sync). Verified sitemap.xml 200 with 28 /gta?city= URLs on a clean prod build (89 pages). No calc/data change.
- 2026-07-23 — [hamza request] GTA-wide rollout (Hamza: "I wanna handle listings all over the GTA not just Mississauga"; chose "Mississauga flagship + full GTA"). The site already had a full GTA feed (/api/listings-gta — Toronto + ~30 cities) + per-city /gta?city= pages + a header mega-menu, but the core experience (homepage, /listings) was Mississauga-only. Added a shared RegionSwitcher (components/listings/region-switcher.js — native <select>, mobile-friendly, grouped by region) on BOTH /listings (default Mississauga) and /gta (All GTA / any city), so a visitor can jump to any GTA city in one tap; it just navigates to the existing pages (no feed change, each page keeps its own SEO, Mississauga stays the flagship default so its #1 rankings are untouched). Added a homepage "Now across the whole GTA" band with city chips → /gta?city=. Verified: switcher renders + reflects the active city on /listings, /gta, /gta?city=Toronto (Toronto selected, H1 "Toronto Investment Properties") + Brampton; homepage band + chips render; build green (89 pages). Shipped to production.
- 2026-07-23 — [hamza request + COMPLIANCE] Reframed /sell away from the "quiet/private investor sale" angle. Hamza: "sell quietly to an investor [is a] terrible idea, I just wanna sell their home at a great price netting them the most. private listings are banned by RECO." The live page marketed a "Path 1: Sell quietly to an investor — fast, private, no showings, no sign on the lawn" off-market path — which is both off-message AND a RECO compliance risk (marketing off-MLS private/exclusive sales). Rewrote /sell entirely around selling on the OPEN MARKET for the most: new H1 "Sell Your Mississauga Home for Top Dollar", removed the "Two ways to sell / quiet investor sale" section wholesale, reframed the hero/differentiators/steps/FAQ/CTA around data-driven pricing + full MLS/marketing exposure + hard negotiation ("Marketed to every serious buyer … on the open market"). Updated the valuation form (goal options no longer offer a "quiet sale"; button "Get My Free Home Valuation"; source reverted to 'seller-valuation' → "Seller — Home Valuation" label; removed "private/confidential" microcopy) and the homepage seller band ("Find out what your home is worth"). Purged every "investor offer preview / off-market / quiet / private / what an investor would pay" reference from all public pages (verified by grep). Build green (89 pages); single h1; shipped to production (compliance).
- 2026-07-23 — [seo agent] Listing-detail social image fallback: listing pages built og:image/twitter:image from the listing's first photo, but a missing photo or a failed /api/photos fetch (that endpoint intermittently times out) left the openGraph/twitter blocks with no image → the listing shared with a blank preview (and realtors share these links constantly). Added a branded /opengraph-image fallback on both og + twitter so a listing link never shares imageless. Build green (89 pages); no calc/data change — (this commit)
- 2026-07-23 — [improvement agent] QUIZ lead leak fixed: the /quiz email gate (5 answers + name + email + phone — a high-intent capture) validated only email PRESENCE and used a bare `await fetch('/api/lead')` with no res.ok check, so a typo'd email 400'd, the failure was swallowed, and the visitor saw their results while the lead was silently LOST. Added email-format validation (site-wide `/\S+@\S+\.\S+/`) + a res.ok guard (a real failure now prompts retry instead of faking success). Added the same format check to the /pre-construction VIP + /sell valuation forms (not leaks — already res.ok-guarded — but a typo now gives an accurate message, lifting completion). Build green; regex unit-verified — (this commit)
- 2026-07-21 — [design] Neighbourhood cards redesigned image-forward (Hamza request): premium NeighbourhoodCard with landmark scene art per area (waterfront lighthouse / skyline / village / suburban / transit archetypes), name+yield overlaid on the image, hover zoom, applied to homepage preview + /neighbourhoods index. Photo-ready: real .jpg drops into /public/images/neighbourhoods/ via lib/neighbourhood-images.js manifest — (this commit)
- 2026-07-21 — [design] Neighbourhood scene art now varies per neighbourhood (deterministic sky palette + sun position + mirror from the name hash) so same-archetype areas (all waterfront hoods, etc.) look distinct instead of identical — Clarkson≠Port Credit≠Lakeview. Still photo-ready — (this commit)
- 2026-07-23 — [planning] Added the conversion-optimization sprint (section 1c) from Hamza's 'perfect the design for high conversions' ask: prioritized A-friction/CTA, B-offer/lead-magnets, C-social-proof/urgency, D-measurement, E-performance. Agents work topmost-first; offer copy + Clarity review flagged for Hamza — (this commit)
- 2026-07-23 — [conversion] Offer sharpened + unified (Hamza-approved): homepage capture, newsletter InlineCTA, and exit-intent popup all now lead with "the 10 best cash-flowing Mississauga deals, free every Monday" (was vague "Sign Up Free"/"daily alerts"). Exit-intent switched to the weekly newsletter endpoint so its promise is deliverable; removed an unverifiable "200+ investors" claim. Sprint item 1c-B done — (this commit)
- 2026-07-23 — [design agent] Social proof at the decision point (sprint 1c-C): the homepage's primary email-capture band (weekly-deals signup) had ZERO proof. Added 5 gold stars + a real investor Google review (Ryan Yau — "…the purchase of our first investment property…", imported from GOOGLE_REVIEWS with a name-match + first-review fallback so it never renders empty) + "Verified Google review · 5.0 on Google (28 reviews)". Caught + fixed a WCAG-AA contrast fail (success-green #10B981 on the #E6F1FB band ≈ 2.2:1 → moved proof text to navy/70–80, stars stay decorative aria-hidden). Verified true-375px via headless-Chromium: no overflow, premium, AA. Also FLAGGED for the improvement agent a hardcoded "388" in the hero CTA (hero-buttons.js) — a stale number on the #1 button; the real live count already exists server-side. Build green (89 pages) — (this commit)
- 2026-07-23 — [improvement agent] WRONG-NUMBER fix (homepage hero): killed TWO fabricated figures on the #1 page. The primary CTA hardcoded "Unlock 388 Premium Deals — Free" (hero-buttons.js), and — worse — the hero subtitle's heroCount fell back to a hardcoded "2,000+" whenever the real feed count was <500 or the feed was down, so the true ~few-hundred Mississauga inventory displayed as "2,000+ Mississauga Investment Properties" (~5× overstatement; "a wrong number is the worst bug"). Both now use one real feed-driven count (exact <500, rounded-down "N+" for ≥500 so it never overstates, and omitted entirely when the feed returns nothing — no fabricated number). Button de-overclaimed "Premium"→"Investment Deals" and takes the count as a server prop. Left the cumulative "2,000+ Properties Analyzed" bio stat + meta description as-is (lifetime claim, defensible). Build green (89 pages); verified 375px fallback path renders clean — (this commit)
- 2026-07-23 — [seo agent] Fixed the BROKEN homepage hero search while auditing the WebSite SearchAction schema: the SearchAction correctly advertises `/listings?q={search_term_string}` and the listings filter system reads/writes the term as `q`, but HeroSearch was navigating to `/listings?search=<term>` — a param /listings never reads — so the #1 page's primary search silently dropped every term and showed unfiltered results. Changed HeroSearch to emit `?q=`, aligning the on-site search with both the listings filters and the SearchAction schema (sitelinks searchbox + hero search now hit the same working endpoint). Added an aria-label to the placeholder-only search input. Verified end-to-end (/listings?q=Cooksville → 200, term in SSR, applyFilters filters on it); build green (89 pages); no calc/data change — (this commit)
- 2026-07-23 — [design agent] Honest freshness cue on listing cards: added a top-left accent "New" pill (mirrors the top-right score badge) on ListingCard (/listings + /gta) and HomeDealCard (homepage), shown only when real days-on-market is 1–7. Excludes dom=0 (the process-listings missing-data fallback) so it never false-positives, and can't collide with "Motivated" (dom≥45). Fresh inventory is the highest-converting, and this is an honest urgency signal from data already on the listing — no invented scarcity (the "N people viewing" idea was omitted; no real viewer signal). Absolute-positioned (no layout shift), white-on-accent (AA); verified the badge composition at card scale/375px. Build green (89 pages); no calc/data change — (this commit)
- 2026-07-23 — [seo agent] WRONG-NUMBER in SERP metadata (money-keyword page): /listings' `<title>` and meta description both hardcoded "1,800+" active listings ("…— 1,800+ Scored for Cash Flow" / "browse 1,800+ active listings…") — a fabricated inventory count shown directly in Google results, and inconsistent with the site's other guessed counts (hero button 388, hero subtitle fallback 2,000+). Rewrote both to stable, honest, keyword-front-loaded copy with NO number: title "Investment Properties for Sale in Mississauga — Scored for Cash Flow & Cap Rate", description "…every active listing scored for cash flow, cap rate, and ROI…". The server-rendered H1/intro were already number-free (unchanged). Verified the rendered title/description on a clean prod build (/listings 200, no "1,800+" anywhere). Left the homepage's "2,000+ properties analyzed" as-is (a cumulative lifetime claim, not active inventory — defensible). No calc/data logic touched — (this commit)
- 2026-07-23 — [improvement agent] BROKEN NEIGHBOURHOOD FILTER fixed (lead-capture friction): the /listings filter pipeline only read `?hoods=` (plural, from the filter UI), but 6 links across 4 files pointed to `?hood=` (singular) — the neighbourhood guides' primary "View Live Listings in {Hood}" CTA (all 24 guides), the homepage "Popular:" hood chips + hood cards, market-pulse, and the /neighbourhoods index. `?hood=` was silently ignored, so every one of those CTAs dumped visitors onto the UNFILTERED listings page instead of the neighbourhood they clicked (same class as the hero `?search=` bug). Robust one-place fix in deserializeFilters: accept BOTH `hoods` and `hood`, merged + de-duped into a fresh array (never mutates the shared DEFAULT_FILTERS), so all existing links AND any bookmarked/indexed `?hood=` URLs now filter correctly. Verified /listings?hood=Cooksville → 200 with the Cooksville filter active in SSR; build green (89 pages). No calc/data change — (this commit)
- 2026-07-23 — [design agent] Sticky mobile CTA on the 24 neighbourhood guides (sprint 1c-A): search visitors land on a guide's [slug] page and may never scroll to the mid-page CTA, so added a fixed-bottom, mobile-only (lg:hidden) bar with ONE primary action — "View {Hood} Listings →" → /listings?hood={Hood} (the filtered view, now working after the ?hood= fix). z-[150] keeps it under the cookie banner (z-[200]) + exit-intent (z-[300]) so it never covers a capture surface (mirrors the proven listing-detail bar); safe-area padded; content gets pb-28 on mobile so nothing hides (no layout shift). One [slug] edit covers all 24 guides. Verified at true 375px via headless-Chromium (cookie banner dismissed): accent button, WCAG-AA, on-brand, content clear above it; build green (89 pages). No calc/data change — (this commit)
- 2026-07-23 — [seo agent] BreadcrumbList JSON-LD on /quiz + /alerts — the two remaining lead-capture pages that lacked it (both 'use client', so added the schema via their existing server layout.js alongside the metadata, mirroring the /faq pattern). Enables breadcrumb rich-results on two conversion pages. Verified BreadcrumbList renders in SSR on both; build green (89 pages). Also SWEPT all /listings?<param> deep-links against what deserializeFilters actually reads — after the earlier ?search=→?q= and ?hood= fixes, every remaining link (cf, sort, q, hood) now uses a real param, so no more silently-dropped filters. RE-CONFIRMED + escalated the site-wide HST "$130,000 / -$104,000" wrong-number flag (Needs Hamza — a rebate can't exceed the $68k provincial HST on an $850k home; figures are policy/eligibility-dependent so agents must not guess). No calc/data change — (this commit)
- 2026-07-23 — [design agent] Sticky mobile CTA on the 5 investor guides (sprint 1c-A, continuing last design run's neighbourhood-guide rollout): extracted the proven bar into a reusable StickyMobileCTA component (z-[150] under cookie/exit-intent, safe-area padded, lg:hidden, built-in in-flow spacer so no wrapper edits) and applied it to all 5 guide pages with page-relevant primary actions (cash-flow → filtered cf=1 listings, rent-vs-buy → calculator, townhouse-vs-condo → listings, LRT → lrt=1 filtered listings, insurance → calculator). Verified all 5 CTAs render in SSR + a true-375px viewport capture (accent bar, AA, no overflow, hero intact); build green (89 pages). Remaining sticky-CTA targets: homepage, blog post — (this commit)
- 2026-07-23 — [seo agent] robots.txt group-override bug fixed: per the robots spec a crawler obeys ONLY its most specific matching group, so the file's Googlebot and Bingbot groups (each just "Allow: / + Disallow: /api/,/admin/") were silently CANCELLING the * group's disallows (/_next/, /admin.html, /profile/, /saved/, /recent/, /login, /signup) for exactly the two bots that matter. Rewrote to a single * group with infrastructure disallows only (/api/, /admin/, /admin.html, /_next/); the five user/auth routes are excluded via their `robots: noindex` metadata instead of a crawl block (Google can only see noindex on pages it may crawl — the repo's own stated approach, and all five verified emitting "noindex, nofollow" in SSR: login/signup via the (auth) layout, profile/saved/recent via their layouts). robots.txt serves 200 with one consistent group for every bot; build green (89 pages). No calc/data change — (this commit)
- 2026-07-23 — [improvement agent] RETENTION ENGINE FIXED (deal-alerts filter pipeline): /api/alerts/subscribe sanitized against a legacy allow-list sharing ZERO keys with the client-format filters the Save-Search button sends → every saved search stored ~empty filters → the daily cron matched ~ALL listings instead of the user's criteria; AND the /alerts form never created a saved search at all (lead + localStorage only — its promised alerts could never fire). Fixed with one canonical shape: new lib/alerts/sanitize-filters.js (client/applyFilters format, validated+bounded, legacy keys mapped in, stores only non-default keys) now used by subscribe, and the /alerts form now POSTs subscribe with mapped canonical filters (lead capture kept as fire-and-forget). Unit-verified the full round-trip (button/form/legacy/junk payloads → sanitize → DEFAULT_FILTERS merge → applyFilters on synthetic listings — each matches exactly the intended listing; junk clamped). Old stored rows unaffected (keys already ignored). Build green (89 pages). Still open: cron is Mississauga-only for GTA searches + live Resend delivery check (Needs Hamza) — (this commit)
- 2026-07-23 — [design agent] Sticky mobile CTA on BLOG POSTS (sprint 1c-A, completing the content-page rollout): blog posts are the site's top-traffic pages (117/52/52 visits on top posts per Hamza's analytics) yet long articles had no persistent mobile action — the newsletter capture sits in the desktop sidebar and the booking CTA is at the article's very end. One StickyMobileCTA insertion in blog/[slug] covers every post: "Get Free Deal Alerts →" → /alerts (whose form now actually creates saved searches after the pipeline fix). Proven component (z-[150] under cookie/exit-intent, safe-area, in-flow spacer, 375px-verified on 29 pages). Build green (89 pages); production eyeball recommended on a live post (no local Supabase). Homepage sticky deliberately deferred to the 1c-A competing-CTA audit — (this commit)
- 2026-07-23 — [improvement agent] GTA SAVED SEARCHES now get GTA alerts (completes the retention-engine repair): searches record their region (SaveSearchButton ← page city via ListingsContainer/InvestorFilters; 'GTA' sentinel on the hub; sanitizer stores bounded `city`; popup summary shows the scope) and the daily cron matches each search against its own pool via poolForSearch() — Mississauga feed for legacy/no-city rows (old behavior byte-identical), GTA feed (fetched only when needed, failure-isolated) for GTA scopes, city-filtered for a specific city. Unit-verified 6 region cases (Toronto-only matching, GTA-hub cross-city, legacy unchanged, feed-down degrades safely, city key inert to applyFilters); /listings + /gta + /gta?city=Toronto 200; build green (89 pages). Deal-alerts backlog is now code-complete — only live Resend delivery verification remains (Needs Hamza) — (this commit)
- 2026-07-23 — [seo agent] CWV/CLS fix on the blog-post cover (LCP element of the site's top-traffic page type): the cover rendered as an unsized w-full img → the whole article shifted down when it loaded. Now a fixed aspect-[1200/630] box + object-cover (matches the 1200×630 generated covers; variable-aspect Unsplash covers crop consistently) + width/height attrs + decoding=async — image area fully reserved pre-load, hero CLS = 0. Audited all other image surfaces (listing-detail aspect-box hero, fixed-height cards/index/related thumbnails) — already CLS-safe; /faq FAQPage schema confirmed present (no gap). Build green (89 pages); no calc/data change — (this commit)
- 2026-07-23 — [improvement agent] GA4 conversion events on EVERY capture path (sprint 1c-D — conversion measurement, mission #1): new crash-proof lib/track-conversion.js (no-op without gtag, swallows throws; unit-verified exactly-once + SSR-safe) fired from the success branch of all 10 capture components — newsletter_subscribe (homepage/inline-cta/exit-intent), alert_create (alerts page/save-search w/ city), lead_submit (quiz/seller-valuation/precon-vip), booking_create (book-call), signup (gate-complete/gate-email-only) — each with a `source` param so GA shows which surface converts. Success-branch-only placement = no double-fires. Build green (89 pages). Hamza: mark these as conversions in the GA4 UI + build the funnel — (this commit)
- 2026-07-24 — [design agent] Homepage hero one-primary-action pass (sprint 1c-A, mission #1): the #1 page's hero had a near-equal-weight ghost "Browse Deals" button that only duplicated the search bar's destination (/listings), so two browse paths out-weighed the single accent lead-capture CTA. Demoted it to a light "Browse all deals →" text link (WCAG-AA on dusk navy) and made the accent "Unlock … Free" button the ONE dominant action (full-width on mobile). Browse path preserved; no capture path touched. Verified at true 375px (headless CDP): no overflow (sw=cw=375), single h1 — (this commit)
- 2026-07-24 — [seo agent] Removed the self-serving aggregateRating (5.0/28) from the global OrganizationJsonLd — it rendered on EVERY page via the root layout (listings, blog, tools, legal) where those reviews aren't shown, which is exactly the self-serving-review pattern Google's rich-results policy prohibits (risks the whole LocalBusiness markup being discarded or a manual action). A rating belongs only on a page that displays the underlying reviews. Also dropped the needless 'use client' from components/seo/json-ld.js (pure <script> emitters) so the JSON-LD renders server-side and stops shipping to every client bundle. Build green (89 pages). NEW FINDINGS for the improvement agent: (1) filter-utils.js:46 'HIGH CAP' chip tooltip says "above 4%" but the filter is capRate>=5 — a data-trust mismatch on /listings + /gta; (2) /sell still claims "1,800+ Mississauga listings" (overstated vs real active inventory + inconsistent with the 2,000+-analyzed figure); (3) daily alert email is missing a CASL physical mailing address + one-click List-Unsubscribe headers + preheader, and formats $1M+ as "$1050K" — (this commit)
- 2026-07-24 — [improvement agent] Daily deal-alert email quality & CASL compliance (mission #3, highest-frequency email): (1) CASL — added the physical mailing address (885 Plymouth Dr, Unit 2, Mississauga, ON L5V 0B5) to the footer; it was the ONLY template missing it (legal requirement in every commercial email, and this one sends daily to the whole saved-search base). (2) Deliverability — added RFC 8058 one-click List-Unsubscribe + List-Unsubscribe-Post headers (via unsubscribeUrl(email)) that Gmail/Yahoo bulk rules require; the visible per-search footer link still works. (3) Open rate — added a hidden preheader so the inbox preview isn't masthead junk. (4) Numbers — prices now format via fmtPrice ($1.05M not "$1050K", empty not "$0K" for missing price) and the CTA drops the fabricated "1,800+" → "Browse All Listings". Build green (89 pages); fmtPrice unit-checked. Live Resend send verification still Needs Hamza — (this commit)
- 2026-07-24 — [design agent] Trust/contrast pass on the signup-gate modal (the #1 listings conversion surface): replaced the fabricated "388 premium deals available right now" urgency line — no live count backs it, and fake scarcity undercuts the "premium and calm" bar — with honest evergreen copy ("New investment deals added daily"). Also raised the AA-failing helper + social-proof labels from text-slate-400 (~2.85:1 on white) to text-slate-500 (~4.6:1) so "Free forever…", "Listings Scored", "Google Rating", "No Catch" pass WCAG AA. Build green (89 pages); class/copy-only, no logic. NEW FINDING for the improvement agent: this modal's handleStep2 still marks the visitor registered WITHOUT checking res.ok (a 429/400 fakes success and loses the lead), and the step-2 phone placeholder is a 555 number the server rejects — both real lead-capture bugs on this branch (already fixed on the feature branch off main) — (this commit)
- 2026-07-24 — [seo agent] CWV: the homepage hero "top deal" card image (a strong desktop LCP candidate, above the fold) rendered with default priority and no dimensions. Added fetchPriority="high" + decoding="async" + intrinsic width/height (300×160, matching its fixed h-40 container) so the browser discovers and reserves it early — faster LCP on the #1 page with zero layout shift. Photo CDN is already preconnected. Build green (89 pages); no data/calc change — (this commit)
- 2026-07-24 — [improvement agent] Closed the signup lead-drop on THIS branch (mission #1 — highest-value lead type). Both /signup and the listings signup-gate modal called res.json() then marked the visitor registered + redirected/closed WITHOUT checking res.ok — so a 400 (server rejects fake/555 phones on registration) or 429 (rate limit) showed success while the name+email+phone+CASL lead was never saved. Added res.ok guards to both (surface the error, let them retry). Also fixed the phone placeholders/example that were literally 555 numbers the server rejects → (647) 361-1234. Build green (89 pages). (These were fixed on the feature branch off main earlier; this brings the 24/7 branch in line.) — (this commit)
- 2026-07-24 — [design agent] Conversion + WCAG-AA pass on /market-pulse (high-intent research page): (1) Conversion — the page only offered a link CTA at the very bottom (→/quiz); added an inline email-capture (InlineCTA newsletter variant → /api/newsletter/subscribe) mid-page after the price-by-type chart so engaged readers convert in one tap without leaving. (2) Trust/AA — all 14 data labels (metric tiles, sold-comps stats, recent-sales table headers, hot/warm/cool headers) rendered in text-slate-400 (~2.85:1 on white/cloud, fails WCAG AA); raised to text-slate-500 (~4.6:1). Verified at true 375px (headless CDP): page renders, inline capture + email field present, no horizontal overflow, single h1; build green (89 pages). No data/calc change — (this commit)
- 2026-07-24 — [seo agent] /news CWV + metadata: the page server-renders an aggregate of external RSS feeds but had NO revalidate, so it was fully dynamic — re-fetching every source feed on every request (slow TTFB/LCP + needless load). Added `export const revalidate = 1800` (30-min ISR) to cache the render. Also completed its metadata to match the rest of the site — added openGraph + twitter card tags (it only had title/description/canonical). Build green (89 pages). NEEDS HAMZA: /news is a pure third-party-headline aggregator (thin/duplicate-content + outbound links away from the funnel) — decide whether to `noindex` it (protects domain topical focus) or add original framing; not changed unilaterally since noindex removes it from search — (this commit)
- 2026-07-24 — [improvement agent] Weekly newsletter open-rate: added a hidden preheader to buildEmailHTML (app/api/newsletter/weekly). Previously the email went straight from <body> into the masthead table, so the inbox preview snippet was junk (masthead/date text) — wasted prime real estate next to the subject line. Now teases the content ("This week's top cash-flowing Mississauga deals, scored — plus live market stats inside"). Completes the preheader coverage across all four templates (announcement/alert already had one; weekly was the last gap). Build green (89 pages) — (this commit)
- 2026-07-24 — [design agent] Legibility + WCAG-AA on the /neighbourhoods index (top SEO landing page — "best neighbourhoods to invest in Mississauga", 24 ranked cards): the yield-badge "Rent Yield" caption rendered at text-[8px] (below the legibility floor) → text-[10px]; and every card data label (Avg Price, YoY Change*, Avg DOM, Inventory*), the "Hamza's Take" label, and the recent-sales table headers rendered in text-slate-400 (~2.85:1 on white/cloud, fails AA) → text-slate-500 (~4.6:1). Verified at true 375px (headless CDP): no horizontal overflow (sw=375), slate-500 labels present, single h1; build green (89 pages). No data/calc change — (this commit)
- 2026-07-24 — [improvement agent] Added lead capture to two high-intent pages that had NONE (mission #1): /recent-sales (investors checking what actually sold) and /compare (comparing listings side-by-side = high buying intent). Both previously offered zero email-capture path. Added an inline InlineCTA — newsletter variant on /recent-sales (foot of page), alerts variant on /compare (below the comparison) — each subscribes in place via /api/newsletter/subscribe or the alerts capture. Verified: /recent-sales SSR contains the CTA headline + email input; /compare renders the capture post-hydration with no 375px overflow, single h1. Build green (89 pages) — (this commit)
- 2026-07-24 — [seo agent] Middleware matcher: added `image-sitemap.xml` to the negative-lookahead exclusion (it already excluded sitemap.xml/robots.txt but not the image sitemap), so the Supabase session middleware no longer runs on bot fetches of /image-sitemap.xml — a small crawl-efficiency/latency win on a bot-only route. Build green (89 pages). NOTE: the high-value SEO backlog is now thin — most public routes have complete metadata/OG/canonical/JSON-LD, robots/sitemap are correct, and CWV is addressed. The remaining big-ticket SEO items are larger projects: (1) the 28 /gta city pages fetch listings client-side (server content = h1+sub+breadcrumb only; deeper SSR listing content would need a real render, not skeleton), and (2) next/image migration for the photo-heavy pages (needs remotePatterns for the MLS CDN) — both flagged for a dedicated effort — (this commit)
- 2026-07-24 — [design agent] Legibility + WCAG-AA on the shared NeighbourhoodCard (the homepage "Top neighbourhoods" preview cards — #1 page): the "Rent Yield" badge caption rendered at text-[8px] (below the legibility floor) → text-[10px]; the Avg Price / YoY / Avg DOM stat labels at text-[9px] → text-[10px]; and the positive YoY value in text-success (#10B981, ~2.5:1 on the cloud tile — fails AA for 12px bold) → text-emerald-700, negatives text-red-500 → text-red-600 (both pass). Build green (89 pages); class/size only, no data change — (this commit)
- 2026-07-24 — [improvement agent] Welcome email (newsletter/subscribe) open-rate: added a hidden preheader so the inbox preview teases "your first Mississauga Market Weekly lands Monday…" instead of the letter-spaced masthead. This was the LAST email template without a preheader — coverage is now complete across all four (announcement, weekly, daily alert, welcome). The welcome already had the CASL address + one-click List-Unsubscribe. Build green (89 pages) — (this commit)
- 2026-07-24 — [seo agent] Completed Twitter Card metadata on /recent-sales and /compare — both had title/description/canonical/openGraph but no `twitter` card, so an X/Twitter share fell back to a non-large card. Added `summary_large_image` cards (matching blog/sell/gta/neighbourhoods). NOTE: audited this run — sitemap covers all money pages (/sell /compare /recent-sales /guides /quiz /market-pulse /mortgage-calculator + neighbourhoods), listing detail has PropertyJsonLd, /neighbourhoods has metadata via its layout, robots/canonicals complete. The design/SEO backlog is now genuinely thin — remaining big-ticket items are the 28 /gta client-rendered listing pages (deeper SSR) and next/image migration (both larger dedicated efforts, flagged for Hamza). Build green (89 pages) — (this commit)
- 2026-07-24 — [design agent] WCAG-AA on the /listings filter drawer (investor-filters.js — the high-traffic listings filter UI): the visible section headings ("Search", "Neighbourhoods", "Property Type", "Price Range", "Strategy", "Advanced Filters") and the "Popular:"/"Filters:" labels rendered in text-slate-400 (~2.85:1 on white, fails AA) → text-slate-500 (~4.6:1). Left icons/placeholders/the $ adornment untouched. Class-only, no logic; build green (89 pages). NOTE: verified the site-wide header trust-bar contrast is fine (white/60 ≈6:1, white/50 ≈4.7:1 both pass AA) — no change needed there — (this commit)
- 2026-07-24 — [improvement agent] Added lead capture to /mortgage-calculator (mission #1) — a high buying-intent tool (running mortgage/cash-flow numbers on a purchase) that had ZERO email-capture path. Added an inline InlineCTA (newsletter variant → /api/newsletter/subscribe) at the foot, after the FAQ. Build green (89 pages). Capture coverage is now on all the high-intent tool/research pages (market-pulse, recent-sales, compare, mortgage-calculator) — (this commit)
- 2026-07-24 — [seo agent] Completed the Twitter Card on /mortgage-calculator layout (had title/description/canonical/openGraph but no twitter:card) → summary_large_image, matching the rest of the site. Build green — (this commit)
- 2026-07-24 — [design agent] WCAG-AA + legibility on the listings data labels (where the numbers investors trust actually render): the primary listing-card metric strip (DOM/CAP/CoC/Cash Flow-mo) and the Top-Picks mini-cards used text-slate-400 (~2.85:1, fails AA) → text-slate-500; the 9px "Cash Flow/mo"/"CF/mo" labels → 10px (legibility floor); positive cash-flow value text-emerald-500 (~2.5:1) → emerald-600 (passes), and a negative cash-flow value that was rendered in muted text-slate-400 (near-invisible AND misleading for a negative number) → text-red-600 so a losing deal reads as one. Class-only, gated/unlocked structure untouched; build green (89 pages) — (this commit)
- 2026-07-24 — [seo agent] Completed BreadcrumbList JSON-LD coverage on the last two content pages that lacked it: /faq (had FAQPage schema but no breadcrumb) and /score-methodology (had neither). Every other content/landing page already carried BreadcrumbList; these two now match, so Google can render the Home › FAQ / Home › How the Deal Score Works trail and the site graph is consistent. NOTE audited this run: metadata coverage is complete (every client page has a layout.js with metadata), the sitemap already includes all static pages + neighbourhood guides + GTA city pages + Mississauga & GTA listings + blog posts, and JSON-LD is broad. Class/markup only, no logic; build green (89 pages) — (this commit)
- 2026-07-24 — [improvement agent] Removed a fabricated count from a lead-capture surface: the exit-intent popup's social-proof footer claimed "1,800+ active listings tracked daily across Mississauga", but the homepage's own hero logic documents the real *active Mississauga* inventory as ~few-hundred (1,800+ is a GTA-wide figure misattributed to Mississauga alone; prior runs already scrubbed "1,800+"/"388" from /listings metadata, /sell, and the signup gate). Replaced with a truthful, still-punchy line: "Every new Mississauga & GTA MLS listing — scored for cash flow daily." Build green (89 pages). NEEDS HAMZA (do not guess): verify/refresh the remaining hardcoded social-proof stats — homepage "2,000+ Properties Analyzed" + "5.0 ★ Google Rating", signup-gate "4,000+ Listings Scored" + "5.0 ★ Google Rating", about-page "1,800+", auth-layout "1,800+ live MLS listings across the GTA". Cumulative/rating claims may be defensible but are unverifiable from code — confirm the real figures — (this commit)
- 2026-07-24 — [design agent] WCAG-AA on the /listings Deal Screener stats dashboard (the summary panel above the results — the numbers that frame the whole page): headline opportunity-stat values text-emerald-500/amber-500 (~2.5:1 / ~1.9:1 on white — fail even the large-text 3:1 bar) → emerald-600/amber-600 (left accent border kept as decoration); all stat labels + subtitles + the disclaimer text-slate-400 (~2.85:1) → text-slate-500; and the Portfolio Analytics negative averages (Avg Cash Flow / Avg CoC) were rendered in muted text-slate-400 — near-invisible AND misleading for a negative number → text-red-600 so a negative average reads as a loss (matches the listing-card fix). Class-only, no logic/number changes; build green (89 pages). The listings-view AA/legibility pass is now complete across listing-card, Top-Picks, filter drawer, and the Deal Screener — (this commit)
- 2026-07-24 — [seo agent] Descriptive alt text on content images that had alt="" (empty = "decorative", wrong for content — hurts image SEO + a11y): the /news feature-card image and list thumbnail → alt={article.title}, and the blog related-post cover → alt={r.title}. Their siblings already used title-based alt (blog hero alt={post.title}, blog index cards, neighbourhood cards), so this closes the last content-image alt gaps. Markup only, no logic; build green (89 pages). NOTE: audited image alt site-wide — the only remaining alt="" cases are genuinely decorative (photo-lightbox uses positional "Photo N"/"Thumbnail N" which is acceptable for a gallery viewer) — (this commit)
- 2026-07-24 — [improvement agent] Added lead capture to /faq (mission #1) — an organic-search landing page (ranks via FAQPage rich results) that had NO email-capture path, only a WhatsApp/Quiz CTA that demands a higher-commitment reach-out. Added an inline InlineCTA (newsletter variant → /api/newsletter/subscribe, inline email field) between the answers and the "Still have questions?" block, so a researcher not ready to message directly can still convert with one field. Rebalanced the spacing (gradient CTA mt-16→mt-8) so there's no giant gap. Build green (89 pages). Remaining content pages without capture: /about (has agent contact CTAs — lower priority) and /score-methodology (low-intent informational) — (this commit)
- 2026-07-24 — [design agent] Turned the /listings no-results empty state into a lead-capture moment (conversion-first design, mission #1 + #3). Both the grid AND table views previously dead-ended a filtered search with "No properties match your filters / Try adjusting" — but an investor whose *specific* criteria return nothing right now is the highest-intent alert signup there is. Redesigned to: navy headline "No properties match your filters right now", a line noting new deals are added daily, a primary accent "Get free deal alerts" button → /alerts, and a muted "or adjust your filters" hint. Fixed the AA-failing text-slate-400 subtext → slate-500, and swapped the fixed h-64 (which could clip the taller content at 375px) for min-h-[16rem] + padding so it never crops on mobile. Palette from tailwind (navy/accent), no layout shift; build green (89 pages) — (this commit)
- 2026-07-24 — [design] Homepage length cut 18% on mobile (15.5 → 12.8 screens / 12,569 → 10,358px): merged the two full-width secondary bands (GTA coverage + seller valuation) into one compact two-up; reviews show 3 on mobile / all 6 from md; neighbourhood preview shows 2 on mobile / all 4 from sm. Also raised every sub-10px label (8-9px 'Rent Yield'/'Avg Price'/'Avg DOM') to 10px — homepage now has ZERO sub-10px text (was 34 elements). Verified at 375px + 1440px with CSS confirmed loaded; desktop content unchanged (4 cards, 6 reviews); no overflow — (this commit)
- 2026-07-24 — [seo agent] Metadata-correctness pass. (1) Fixed the site-wide <html lang="en"> → "en-CA": every schema already declares inLanguage 'en-CA' and the whole business is Canada-targeted (Canadian address, areaServed, CAD), so the bare "en" was an inconsistent localization signal on every page. (2) Enriched the blog post OpenGraph with article freshness/authorship — publishedTime (created_at), modifiedTime (updated_at), authors: ['Hamza Nouman'] — fetching the two date columns in generateMetadata; type was already 'article'. This mirrors the authoritative dates/author already in the Article JSON-LD, so social unfurls and article-understanding now see the same freshness signal. Verified the core JSON-LD is otherwise complete this run (Person/RealEstateAgent with sameAs/areaServed/contactPoint/hasOfferCatalog, WebSite SearchAction → ?q=, RealEstateListing with geo/offers/floorSize/images, Article with dates+author). Build green (89 pages) — (this commit)
- 2026-07-24 — [improvement agent] Fixed a mislabeled filter number (mission #2 — clearly presented data). The /listings "HIGH CAP" strategy chip's tooltip promised "Cap rate above 4%" but the actual filter is capRate >= 5 (capRate is a percentage), so an investor selecting HIGH CAP expecting 4%+ silently loses every 4–5% property. Corrected the tooltip to "Cap rate 5% or above" to match the real behavior — left the filter threshold untouched (that's data/product logic, not mine to change). Verified this text isn't duplicated elsewhere. Also verified /alerts (where many CTAs now route) is a solid low-friction capture: email-only required, real validation, does NOT fake success on server rejection, creates the actual saved search the cron matches, and the cashflow/brrr → cf/brrr strategy mapping matches STRATEGY_CHIPS keys. Build green (89 pages). NEEDS HAMZA: decide the intended HIGH CAP threshold — is it 5% (current filter) or 4% (old tooltip + the mortgage calculator's "good cap rate" marker uses >= 4)? Align chip filter + calculator so "strong cap rate" means one number site-wide — (this commit)
- 2026-07-24 — [design] Round 2 of the measured design audit: /sell 11.4→10.2 screens (FAQ expanded→native <details> accordion, answers still in HTML for crawlers + FAQPage schema untouched) and its valuation form moved 1.1→0.82 screens down on mobile (hero reordered headline→FORM→bullets via flex order; desktop two-column preserved via explicit grid placement). /neighbourhoods 18.2→9.6 screens collapsed (first 9 shown + 'Show all 24' reveal; extras CSS-hidden NOT unmounted so all 24 stay in the DOM for SEO/ItemList). SITE-WIDE legibility: static sweep found 11 sub-10px elements in home-deal-cards.js that rendering missed (deal cards need live MLS data) — the homepage money section had 8px CAP/Cash-Flow/DOM labels on phones; all public sub-10px text now 10px floor. Admin leads+analytics: labels 9px→10px and text-white/20→/55, /30→/60 (was ~1.5:1 contrast, far below WCAG AA) so Hamza can actually read his lead dashboard. Verified 375px+1440px with CSS confirmed loaded; no overflow, single h1, zero sub-10px site-wide — (this commit)
- 2026-07-24 — [correctness] FIXED the flagged HST-rebate wrong number after researching the actual program (PwC/Osler): $850k example now Provincial -$68,000 (8%) / Federal -$42,500 (5%) / Total $850,000 / Save $110,500 — the old -$104,000 provincial line exceeded the $68,000 of provincial HST actually paid. Also relabelled the $24,000 tier from "federal" to provincial in both the tier card and the FAQ. Tier table, dates and eligibility were already accurate — (this commit)
- 2026-07-24 — [reliability] Alert-email sends are no longer silently swallowed: a Resend rejection was skipped with no log, so a misconfigured sender looked identical to "nobody matched today". Now captures Resend's status+message per failure, logs a diagnosis hint (401/403/422), returns {sent, failed, failures[]}, and 503s up front if RESEND_API_KEY is missing — (this commit)
- 2026-07-24 — [design] AUDITED the listing detail page (previously the one blind spot — it needs the live MLS feed). Used Playwright route-interception to mock /api/listing-single + photos/comps/history, so the real UI renders with realistic data and ZERO code changes; script kept at scratchpad/listing-audit.js for reuse. Result: CLEAN — mobile 5.0 screens / desktop 2.5, no overflow, zero sub-10px text, single h1, sticky CTA present, and zero NaN/undefined in the rendered output (the number-guarding work holds). No fixes needed — (this commit)
- 2026-07-24 — [seo agent] Added the missing ItemList + BreadcrumbList JSON-LD to the /neighbourhoods index. This is a primary SEO page (targets "best neighbourhoods to invest in Mississauga") yet it had NO structured data at all — and its own code comment even claimed "the page's ItemList schema still matches what's rendered" while no such schema existed. Built the ItemList statically from HOOD_DATA (the same 24 that render; extras are CSS-hidden, never unmounted, so markup always matches), each entry linking to its neighbourhood guide, plus a Home › Neighbourhoods breadcrumb — matching the /guides and /blog index pattern. Markup only, no logic; build green (89 pages). NOTE: /guides and /blog already had ItemList+Breadcrumb, so collection-schema coverage on index pages is now complete — (this commit)
- 2026-07-24 — [design agent] WCAG-AA on the conversion modals every visitor sees. The signup gate (the #1 conversion surface — shown to every non-registered user at the view limit) rendered its phone-number reassurance ("We'll text you top deals…") and the "Skip for now" link in text-slate-400 (~2.85:1 on white, fails AA); the contact-CTA card's brokerage-licensing trust line ("Cityscape Real Estate Ltd. · Licensed by RECO") did too — all → text-slate-500 (hover states deepened one step to match). Left the google-signin disabled-button slate-400 (disabled controls are AA-exempt) and placeholders untouched. Class-only, no logic; build green (89 pages). Conversion-surface + listings-view AA is now thorough — remaining slate-400 is on genuinely decorative/disabled elements. NEEDS HAMZA still open: verify the signup-gate "4,000+ Listings Scored" / "5.0 ★" social-proof numbers — (this commit)
- 2026-07-24 — [improvement agent] Weekly newsletter honesty (mission #2 + #3): the "Average Prices · By Property Type" section embeds TRREB monthly Market-Watch SOLD prices (detached/semi/townhouse/condo), but the email is dated the send week and its preheader calls the contents "live market stats" — so a months-old board figure read as this week's number. Added a source/as-of caption under that section from the API's tRREBMonth ("Sold prices · TRREB Market Watch, {month} (latest monthly board data)"), rendered only when the field is present (graceful otherwise). The live active-listing stats (avg price/DOM/sale-to-list) are unchanged. No fabricated numbers. Build green (89 pages). NEEDS HAMZA (still open, data-entry): the hardcoded TRREB fallback in /api/market-stats is February 2026 (tRREBAsOf 2026-02-28) — refresh with the latest Market Watch when available; the as-of labels now make the staleness visible rather than hidden — (this commit)
- 2026-07-24 — [seo agent] Marked the /news aggregated third-party article links (BNN, Financial Post, Better Dwelling, CBC, etc. — both the featured card and the compact list) rel="noopener noreferrer nofollow". Per Google's link guidelines these outbound links to sources the site doesn't editorially vouch for should carry nofollow/ugc; the /news page carries dozens of them, so this is the correct crawl-hygiene signal for an aggregation page (internal links are untouched). Markup only; build green (89 pages). NOTE — audited SEO broadly this run and it is in strong shape: every indexable page has a canonical (noindex utility pages correctly omit it), single-h1 across all public pages, /listings + /gta canonicalize their filter params, listing-detail metadata is per-listing with the real photo as OG image + branded fallback, and profile/saved/recent/auth are noindex. Largest remaining SEO item stays the 28 /gta client-rendered listing pages (deeper SSR) — a dedicated effort, flagged for Hamza — (this commit)
- 2026-07-24 — [design agent] Added the persistent mobile StickyMobileCTA to /market-pulse (→ /alerts, "Get Free Deal Alerts"). It's a 391-line data-heavy page with a warm audience, but its only conversion path was the end-of-page /quiz CTA a mobile scroller may never reach; the proven sticky component (mobile-only, safe-area padded, own in-flow spacer so nothing hides behind it, z-[150] below the cookie/exit-intent capture surfaces) now keeps one low-friction email-capture action reachable while scrolling — complementing, not replacing, the /quiz strategy CTA. No layout shift (spacer built in), palette from tailwind (btn-primary accent). Build green (89 pages). NOTE: /recent-sales, /compare, /guides and /news are the remaining long content pages without a sticky mobile CTA — candidates for the same treatment next design run — (this commit)
- 2026-07-24 — [improvement agent] Email-quality consistency (mission #3 + #2): the daily deal-alert email rendered each listing's cap rate as raw `cap + '%'`, so a capRate of 5.23 printed "CAP 5.23%" — inconsistent with the rest of the product, which shows cap to ONE decimal everywhere (the /compare table, the listings cards via pct1, and even this same email's OWN subject line, which already uses capRate.toFixed(1)). Changed the deal-card badge to cap.toFixed(1) + '%' so the alert reads "CAP 5.2%". Guards unchanged (null → "—"); PCF and DOM badges were already clean. No calculation touched — presentation only. Build green (89 pages). Audited the rest of the hot lead paths this run and they're solid: BookingCalendar (the highest-intent form) requires only name+email, validates, and does NOT fake success on a server rejection; /alerts likewise — (this commit)
- 2026-07-25 — [blog quality — Hamza request "I want high quality blogs"] Three fixes to the blog pipeline. (1) WRONG NUMBER, worst-bug class: the auto-blog prompt hardcoded "5-year fixed mortgages are around 4.5–5%" while the site's own /api/market-stats publishes 6.09% — every generated post shipped a materially wrong rate. Replaced the hardcoded anchors with a live fetch of /api/market-stats (avg/median price, DOM, sale-to-list, months of inventory + market type, full rate card incl. stress test, BoC/prime/inflation, asking rents by bed), plus explicit accuracy rules: never contradict the site's figures, always name the report month when citing monthly board data, never invent a precise statistic. Degrades to SILENCE if the fetch fails (asserting nothing beats asserting a stale rate) — verified against the real values and the null/empty/partial cases. (2) BROKEN RENDERING: the blog used react-markdown with NO remark-gfm, so every Markdown table rendered as raw "| Area | Price |" pipe text in a paragraph — verified before/after with renderToStaticMarkup (before: no <table>; after: <table> + <th> + 3 rows). Added remark-gfm and styled table/thead/tr/th/td (+ del), with the table in its own overflow-x-auto wrapper so a wide comparison never side-scrolls the article at 375px. (3) DEPTH: raised the target from 900–1300 to 1300–1800 words, now requires three neighbourhoods with real numbers and one worked example (purchase price → rent → monthly result), invites ONE phone-readable comparison table (3–5 rows) now that tables actually render, and turns the closing section into three or four specific actionable takeaways instead of a summary. Build green (89 pages) — (this commit)
- 2026-07-25 — [data — Hamza sent MW2606] REFRESHED the TRREB figures from February 2026 to June 2026 (MW2606, released 3 Jul 2026), transcribed from the PDF Hamza supplied. Mississauga: 567 sales, avg $1,014,120 (was $963,747), median $880,000, 1,632 new listings, 2,589 active, SNLR 35.1%, 4.9 months inventory, 97% SP/LP, 29 LDOM, 47 PDOM. GTA: 6,770 sales (+9.4%), avg $1,058,658 (-3.9%), 17,282 new (-12.9%), 27,329 active (-13.5%), 29 LDOM, 42 PDOM. Peel: 1,167 sales, avg $966,024. Economic refreshed (GDP -0.1% Q1, employment +0.7%, unemployment 7.6%, inflation 3.2%, BoC 2.3%, prime 4.5%) and 1-yr fixed 5.84→5.49. Per-type Mississauga sold data now uses the report's real categories with a `sales` key instead of a `feb2026` key — a month baked into a key name is part of why the staleness went unnoticed. Per-type YoY is explicitly commented as GTA-wide, because Market Watch publishes no per-municipality YoY.
  ROOT CAUSE + SYSTEMIC FIX: TRREB has no API/feed — Market Watch is a monthly PDF, so these constants only change when a human updates them, and nothing made the age visible. Added `tRREBFreshness()` which computes months-behind from tRREBAsOf and exposes `tRREBMonthsBehind`, `tRREBIsStale` (>= 2 months, since a one-month lag is normal) and a `tRREBRefreshNote` reading "Need new market data…". The admin dashboard now shows an amber banner with a link to TRREB Market Watch the moment it's overdue. Unit-verified: June data in July = 1 behind/not stale; the old Feb data would have flagged 5 months behind. Trend series jump Feb→Jun 2026 with a comment saying Mar/Apr/May are genuinely missing rather than estimated. Build green; verified live against a dev server — (this commit)
- 2026-07-25 — [data — Hamza sent MW2603/04/05/06] Filled the market-history gap with four verified reports. New `mississaugaMonthly` table in /api/market-stats: Feb→Jun 2026, one row per published Market Watch (sales 345→452→516→568→567, avg $963,747→$966,615→$980,653→$971,047→$1,014,120, DOM 36→29, inventory 5.2→4.9, SNLR 32.4→35.1 — a market visibly tightening through spring, matching TRREB's "year of two halves" call). June re-extracted from the second copy and cross-checked exactly against the first — extraction validated. priceTrend/salesTrend are now DERIVED from this table; the old invented round-number 2025 points are deleted, not kept as decoration. The blog generator now receives the month-by-month table and is told to describe direction of travel, cite specific months, and never extrapolate past the last row. Also propagated June everywhere stale figures hid: lib/analysis-prompt.js (the per-listing AI analysis context — was Feb prices AND a "5yr fixed ~4%" rate matching nothing; now June + momentum note + GTA-labelled YoY) and FIXED getPromptHash() to hash the whole prompt — it hashed only the first 100 chars, so market-context updates never invalidated /api/analyze's cache and stale analyses would have served forever; market-pulse fallbacks (345/940/32.4/5.2/96%/5.84% → June values, comment pinning them to MW2606); weekly-newsletter preview sample (now mirrors June and exercises the TRREB caption); live-listings avgPrice fallbacks now follow the latest transcribed TRREB average instead of frozen 970000. Build green; API verified on a clean dev server (the earlier "failure" was a zombie dev server squatting port 3000) — (this commit)
- 2026-07-25 — [seo agent] /market-pulse structured data + share cards, following the market-data refresh: (1) added the missing Twitter Card (this page was skipped in the earlier summary_large_image sweep that covered recent-sales/compare/mortgage-calculator); (2) added Dataset JSON-LD — now that the page presents a real monthly TRREB statistics series (Feb–Jun 2026), Dataset is the fitting schema and makes the page eligible for Google Dataset Search. Marked TRREB as creator/isBasedOn and the site as publisher (honest attribution), variableMeasured lists the nine stats actually shown, and every field is deliberately month-agnostic (open-ended temporalCoverage '2026-02/..', no figures baked into markup) so the schema can never silently rot the way the hardcoded stats did. Build green (89 pages) — (this commit)
- 2026-07-25 — [correctness] RESOLVED the flagged "4.89% vs 6.09% rate inconsistency" — and the earlier framing of it was WRONG, so correcting the record: the two numbers measure different things. TRREB reprints the Bank of Canada CONVENTIONAL MORTGAGE series, which are POSTED rates; the engine's 4.89% is a discounted CONTRACT rate. With prime at 4.5%, nobody signs a 6.09% five-year fixed, so 4.89% is defensible and the deals are NOT "all overstated" as previously claimed. Do not raise the engine rate to the posted rate.
  The REAL bug underneath: `stressTest` was hardcoded 8.09 (posted 6.09 + 2). The federal test is the greater of CONTRACT rate + 2% or 5.25%, so it keys off the contract rate — correct value is max(4.89+2, 5.25) = 6.89%. It's now derived from DEFAULT_ASSUMPTIONS.annualInterestRate instead of hardcoded, so it can never drift from the engine again. That wrong 8.09% was being fed verbatim into every AI listing analysis and every generated blog post, telling investors they had to qualify ~1.2 points higher than reality — i.e. it made deals look UNREACHABLE, the opposite of the earlier claim. (The /mortgage-calculator was always right: it computes max(rate+2, 5.25) from the user's own rate.)
  Also fixed the presentation, which was the actual user-facing inconsistency: /market-pulse listed posted fixed rates bare next to a variable CONTRACT rate, so a 5-year fixed appeared to cost 1.6 points more than variable, under a header claiming "approximate rates from major Canadian lenders" (they're BoC benchmarks, not lender quotes). Now: each posted rate carries a "posted" tag, the intro names the real source, and the footnote states plainly that posted is not the rate you'll be offered and discloses the ~4.89% contract assumption behind the cash-flow figures. The analysis prompt and blog data block now separate POSTED from CONTRACT rates and forbid quoting a posted rate as the investor's borrowing cost. No calculation changed — build green (89 pages), stressTest verified 6.89 live — (this commit)
- 2026-07-25 — [correctness — Hamza: "pull it from Google"] The "5.0 on Google · 28 reviews" claim was HARDCODED in 11 places (homepage trust chips + agent profile + reviews header, header trust bar, /about, /sell x2, /auth layout, blog post CTA + author box, signup-gate modal, email-capture, announcement email). Nothing verified it and nothing updated it — the same silent-drift failure as the TRREB data, except this one is a public claim about our own reputation, which is worse. Replaced all of them with a single live source: lib/google-rating.js calls the Places API (New) place-details endpoint with a rating,userRatingCount field mask (cheapest SKU), 6h revalidate; /api/google-rating exposes it to client components so the key never reaches the browser. Server components (homepage, /about, /sell, /auth, blog post) call the lib directly; client components (header, signup gate) use the route. DELIBERATE DESIGN: there is NO hardcoded fallback rating anywhere. Missing env vars, an API error, a malformed response, or a profile with zero reviews all yield null, and every consumer HIDES the claim rather than showing an unverified number. Verified: build passes with no env vars, and fetchGoogleRating() returns null when unconfigured or partially configured; label formatting handles singular ("1 review") and rounds to one decimal.
  NEEDS HAMZA — this is NOT deployed yet, on purpose: shipping before the credentials exist would make the Google rating vanish from the hero and cost real social proof. Add to Vercel env: GOOGLE_PLACES_API_KEY (Google Cloud → enable "Places API (New)" → create key → restrict to Places API) and GOOGLE_PLACE_ID (the Business Profile place ID). Both documented in .env.example. The moment they're set, the rating goes live and self-updates every 6 hours — (this commit)
- 2026-07-25 — [correctness — Hamza: "any blog any email that gets sent is updated with every stat and based on data"] Audited every generated-content path and removed the last places market numbers were frozen in code. (1) lib/analysis-prompt.js — which drives EVERY per-listing AI analysis — had the whole market context hardcoded. It now BUILDS ITSELF from live /api/market-stats at request time: avg/median price, SNLR, inventory, sale-to-list, DOM, market type, the Feb→Jun direction-of-travel line from mississaugaMonthly, posted-vs-contract rates, stress test, sold benchmarks by type with GTA-labelled YoY, and asking rents. No figure is hardcoded; if the fetch fails the market block is REPLACED with an explicit "unavailable — do not quote market-wide numbers" instruction rather than reciting stale figures. (2) getPromptHash() now hashes the rendered prompt, so a data refresh automatically invalidates every cached analysis — verified: June and July payloads produce different hashes, identical payloads produce identical ones. Combined with the earlier fix (it used to hash only the first 100 chars of a static string) this closes the path where cached analyses could quote months-old prices forever. (3) The weekly-newsletter ?preview=1 route was rendering from a hardcoded sample stat block — a preview built on frozen numbers can't verify the one thing that matters, so it now renders from the real live stats. Verified: build green, prompt output inspected for June/July/no-data cases. Blog generator was already live-data grounded; alert email carries no market figures.
- 2026-07-25 — [improvement agent] BROKEN NUMBERS, worst-bug class: none of the homepage's "Live MLS data" stats were actually live. market-stats' fetchLiveListingStats() read RAW MLS field names (ListPrice, DaysOnMarket, PropertyType/PropertySubType) but /api/listings returns MAPPED listings (price, dom, type, subType) — so every filter matched zero rows and each stat silently fell through to its fallback. Production proved it: avgDOM served the hardcoded 28, avgPrice served the TRREB monthly figure, and every avgPrices.*.count came back 0 — all of it labelled "Live MLS data · PropTx" on the homepage trust chip. Now reads mapped names with the raw names kept as a secondary fallback, so it works whichever shape it's handed. SECOND bug found while testing the fix: the per-type buckets used independent substring filters, and "Semi-Detached" contains "Detached" while "Condo Townhouse" contains "Townhouse" — semis were being counted into the detached average and condo-towns into freehold towns, inflating both. Replaced with a single-assignment classifier that checks most-specific first (semi → condo → town → detached); verified each listing now lands in exactly one bucket. Hand-verified against a synthetic 5-listing set: avgPrice 837,500 and avgDOM 25 computed correctly from mapped rows, raw-shape rows still work, no double counting. Build green — (this commit)
- 2026-07-25 — [design agent] Removed the last frozen numbers from the homepage stats band (the exact tiles Hamza screenshotted). StatsBar fell back to a hardcoded {200+ listings, 28 days, $970K avg, 97.2% sale-to-list, $964K sold, 5.2 mo} object — a February snapshot — and fetchLiveStats() applied the same defaults one layer up (28 / 970000 / '97.2%'), plus an inline `s.avgSoldPrice || '$964K'`. So ANY hiccup in /api/market-stats silently redisplayed months-old figures beneath a "Live MLS data · PropTx" trust chip, visually indistinguishable from working. Now every value stays null when unsourced, each tile is omitted individually if it has no real number, and the whole band returns null rather than render empty — matching the "never fabricate, simply omit" rule already used in the weekly email. Design held: the lg column count now tracks the number of real tiles (6/5/4/3) so a partial set stays balanced instead of leaving a ragged gap in a fixed 6-col grid; mobile stays 2-col, no layout shift, navy/accent palette untouched. Build green. Combined with the live-stats field-name fix in the previous commit, the homepage band is now genuinely live or genuinely absent — never stale-but-plausible — (this commit)
- 2026-07-25 — [correctness] Google rating: added a hand-entry path so the numbers can be real without a Places API key (Hamza declined the Google Cloud/billing setup). lib/google-rating.js now resolves in priority order: live Places API if GOOGLE_PLACES_API_KEY + GOOGLE_PLACE_ID are set → MANUAL_RATING (rating, reviewCount, verifiedOn) → null/hidden. Crucially the manual block is NOT an anonymous hardcode like the eleven "5.0 · 28 reviews" strings it replaces: it carries the date the profile was actually checked, and googleRatingFreshness() flags it after 90 days with a refresh note now surfaced as an amber admin banner linking to the Business Profile — same treatment as the TRREB data. An API error also falls back to the dated manual figures rather than blanking the trust chip. MANUAL_RATING is currently all-null, so every rating claim stays hidden site-wide until Hamza supplies the real figures — verified: fetchGoogleRating() returns null and freshness reports "No Google rating is set". Build green. NEEDS HAMZA: the current rating, review count, and (optionally) new review text to refresh GOOGLE_REVIEWS in lib/constants.js — (this commit)
- 2026-07-25 — [seo agent] Made sitemap `lastmod` honest. The sitemap regenerates every 6 hours and stamped `lastModified: now` on ALL ~80 static URLs (28 static pages + 24 neighbourhood guides), telling Google that every guide, legal page and landing page changed twice a day. Google treats a consistently unreliable lastmod as grounds to ignore lastmod for the entire site — which would have discarded the accurate per-listing and per-post dates too, the ones that actually drive efficient recrawl. Now: `now` only on the 7 genuinely feed-driven URLs (/, /listings, /gta, /recent-sales, /news, /blog, /pre-construction/projects) where it's true; neighbourhood guides carry HOOD_OUTLOOK_AS_OF (their real as-of date); static editorial/legal pages carry NO lastmod (explicitly fine per Google — omitting beats lying); blog posts use updated_at||created_at with no fake fallback. ALSO found the listing pages never had a real date at all: ModificationTimestamp is selected from the AMPRE feed but was dropped during mapping in /api/listings, so the sitemap always fell back to `now` for every listing — now exposed as `modificationTimestamp` and consumed, giving ~2,500 listing URLs genuine per-listing lastmod. Caught a latent trap while testing the month parser: `new Date('not-a-month 1 UTC')` does NOT return Invalid Date in V8, it coerces to the year 2001 — so a typo in HOOD_OUTLOOK_AS_OF would have silently published a 25-year-old lastmod on all 24 guides; replaced with explicit month-name + 4-digit-year validation, verified against 12 edge cases. Build green — (this commit)
- 2026-07-25 — [investor feedback] Acted on an experienced investor's critique of the homepage. (1) CREDIBILITY — their read was that a 6.88% cap on a $769K 7-bed Malton detached meant per-bedroom rooming-house rent. Traced the model: it does NOT do that. That listing resolves to ~$3,900 main unit (HOOD_RENTS Malton 4-bed + detached premium) + ~$2,000 legal basement (getBasementIncome, 3-bed legal) = ~$5,900 — a main+suite structure that's near-default in Malton. Defensible but invisible, which is the actual bug. Every listing card now shows "Est. rent $X/mo" with its split ("$3,900 main + $2,000 legal suite", or "N units") and "Every figure below assumes this rent", ungated by design — an assumption you can't audit before signup isn't credible. (2) GATING — the hero deal card showed cap/cash-flow openly while the SAME property was locked in the Top Deals grid one scroll down, asking for signup to see numbers just read. The #1 deal is now the deliberate free sample, rest gated. (3) NAV — claim that "GTA Deals"/"Resources" are dead text is FALSE: desktop is a keyboard-accessible mega-menu button, mobile is an accordion (mobileExpanded). No change needed. (4) TRAFFIC — the real gap. Wrote 5 pillar posts (~4,250 words total) in lib/blog/seed-posts.js, version-controlled rather than pasted into the admin UI so they're reviewable and correctable when data moves: 7-bedroom rooming-house math (turns the credibility objection into authority content), Malton guide, Cooksville/Hurontario LRT corridor, best cash-flow neighbourhoods 2026, HST rebate for investors. Every figure is one the site publishes (June 2026 TRREB, HOOD_RENTS, the rent model, the 2026 HST tiers); each carries an as-of line, honest downside sections, and 2-4 real internal links; three use Markdown tables (now that remark-gfm renders them). Publish via POST /api/admin/blog/seed with x-admin-key — idempotent, skips existing slugs, ?dryRun=1 to preview. Validated all 5: unique slugs, 780-1050 words, valid internal paths. NEEDS HAMZA: run the seed call (or say the word and I'll do it if ADMIN_SECRET is available). NOTED, not actioned: /sell needs its own traffic — it's 80% down the homepage and should be a standalone SEO/Ads target for "what is my Mississauga home worth" — (this commit)
- 2026-07-25 — [autonomy — Hamza: "do it once do it right, don't ask me"] Closed both items that were waiting on Hamza, without him. (1) The 5 pillar posts now publish THEMSELVES: the auto-blog cron (daily 08:23 UTC, runs with real credentials) inserts any missing seed slugs before generating — idempotent, no-ops forever after the first run; on the run where seeds land it pings search engines and skips that day's AI post (five pillars landing at once IS the day's publishing, and it avoids generating a near-duplicate of a just-seeded topic). No admin curl needed; posts go live at the next cron (~08:23 UTC today). (2) Google rating chip restored TODAY without fabricating verification: attempted independent verification via web search — results only echoed our own site's old claim (circular, rejected as proof). Instead MANUAL_RATING now carries Hamza's own previously-published figures (5.0 · 28, matching the transcribed reviews in constants) with verifiedOn deliberately NULL, so the chip renders again but googleRatingFreshness() flags it immediately and the admin dashboard shows "re-check the Business Profile" until someone confirms the live numbers and stamps a date. Honest-by-construction: owner-asserted, marked unverified. VERIFIED live post-merge: avgDOM 40 (real, not the hardcoded 28), per-type counts 943/214/63/1316 — the live-MLS field-name fix is confirmed working in production. NOTE for Hamza (flag only): a search result titles the Facebook page "Hamza Nouman Royal LePage Signature Realty" while the site says Cityscape everywhere — if the brokerage changed, that's a compliance-relevant fix; if the FB title is stale, ignore — (this commit)
- 2026-07-25 — [design agent] Extended the rent-assumption disclosure to the HOMEPAGE deal surfaces — the exact cards the investor critique was aimed at, which the first pass missed (it only covered /listings cards). The hero deal card (the first metric anyone sees on the site) now shows "Assumes $X/mo rent incl. basement suite / across N units" under its CAP/CF chips, and the Top Deals grid shows the same short form on ungated cards (two-up at 375px, so the full main+suite breakdown stays on the listing page; gated cards keep the signup line instead). Also swept this file's AA misses from the earlier contrast passes: metric labels text-slate-400 → slate-500, cash-flow values emerald-500/red-400 → emerald-600/red-600. Build green. Left on the branch for the daily release per the no-extra-builds instruction — (this commit)
- 2026-07-25 — [improvement agent — Hamza: RECO issue, "fix today, never use the old brokerage name again"] Older stored blog posts (pre-dating this repo's generator prompt) name Hamza's PREVIOUS brokerage in their in-content author bios — advertising under a brokerage a registrant doesn't belong to is a RECO violation, on pages Google indexes. Confirmed live before fixing: the LRT post also carries 6 literal U+FFFD chars where em-dashes belong ("a transit project � it is..."), 4 more on the blog index; repo code itself has ZERO old-brokerage references (verified) — the defects live only in the 116 stored DB posts. Fix, two layers: (1) lib/blog/sanitize-content.js corrects the old brokerage name → "Cityscape Real Estate Ltd., Brokerage" and restores space-flanked U+FFFD → em-dash (every live occurrence is space-flanked; verified against the actual strings, idempotent, clean text passes through byte-identical), applied at render on the blog post page (content+excerpt+title+metadata+JSON-LD), blog index, related-post titles, and the weekly newsletter's latest-post block — live for all 116 posts the moment this deploys, no DB access needed. (2) The auto-blog cron (08:23 UTC today) now rewrites any stored row the sanitizer would change, so the DATA is permanently clean; idempotent no-op afterwards. Also added a hard prompt rule: never name any brokerage other than Cityscape (without repeating the old name in the prompt). Merged to production immediately — compliance can't ride the daily release. NOT MINE TO DO: Hamza's WhatsApp broadcast of the Best Neighbourhoods post — his channel, his send — (this commit)
- 2026-07-25 — [seo agent] Retargeted /sell at the query homeowners actually type. The page is the site's ONE seller-intent asset and (per the logged finding) can't ride the investor homepage — but its title targeted "Sell Your Mississauga Property", while the dominant seller query is valuation-shaped ("what is my house worth in Mississauga"). Title → "What Is My Mississauga Home Worth? Free Home Valuation", description leads with the same question and differentiates from online estimates ("real comparable sales, not an online estimate"), H1 → "What's Your Mississauga Home Worth?" (the top-dollar promise stays in the line below), OG updated, and added the Twitter Card this layout was missing from the earlier sweep. FAQ schema on the page already covers valuation questions ("Do I have to commit to selling to get a valuation?"), so the page now reads coherently for the query end-to-end. Metadata + one heading; no calculations, build green — (this commit)
- 2026-07-25 — [design agent] Paginated the /blog index. With 120+ published posts it rendered EVERY card into one ~660KB HTML document, and the Supabase query dragged the full CONTENT of every post over the wire just to compute read-times — bad LCP/CWV on a page that's about to receive the pillar-post traffic. Now: 24 cards/page fetched with .range() + exact count (only the current page's bodies leave the DB), crawlable ?page=N pagination (plain links with windowed numbers + Newer/Older, aria-current, ellipses), per-page self-canonicals via generateMetadata (folding deep pages into /blog would mark them duplicates and orphan older posts' links; page 1 keeps canonical /blog), and the full-collection ItemList schema is preserved via a slug+title-only query so search engines still see all posts described. ~80% HTML-weight cut on first paint. Build green — (this commit)
- 2026-07-25 — [improvement agent] Fixed an overconfident number label (mission #2). The listing-card "Below Market / Above Market / At Market" badge fired at ±3% deviation from `estimatedValue` — but traced the source: estimatedValue is a NEIGHBOURHOOD-AVERAGE heuristic (hood avg price ± ~$75-85K per bedroom × type multiplier, lib/listings/process-listings.js:184-200). It cannot see sqft, condition, lot, or street, so its error bars dwarf ±3% — the badge was stamping a confident market verdict on noise, in both directions ("Below Market" overselling to leads, "Above Market" killing legitimate listings). Now: fires only at ≥10% deviation, says what it measures ("Below area avg"/"Above area avg"), renders NOTHING within ±10% (being near a crude heuristic is not information — dropped the "At Market" claim entirely), and the supporting line is relabelled "Est. value" → "Area estimate" with the +% shade fixed to AA red-600. The estimate formula itself is untouched. Hand-verified thresholds: -12% → Below area avg, -5% → no badge, +11% → Above area avg. Build green. NOTE: estimatedValue's per-bed adjustments (±$75-85K) and type multipliers are themselves hand-tuned constants — flagged for a future data-driven calibration against actual per-type hood averages from the live feed (computeHoodStats now has real per-type counts to do it) — (this commit)
- 2026-07-25 — [seo agent] Wired the 5 pillar posts into the internal-link cluster. They published at 08:23 UTC with ZERO links from the static site — and links from established pages are how authority reaches new URLs. Added them to the GUIDES array (the one array that drives the /guides index cards, its ItemList schema, AND the RelatedGuides cross-link cards on all five landing pages), deliberately interleaved static/pillar because RelatedGuides takes the first `limit` non-current entries — every landing page now surfaces a mix (e.g. the LRT landing page links the Cooksville corridor post, the cash-flow landing page links the neighbourhoods ranking). One array edit → /guides grew from 5 to 10 entries with schema updated automatically, and every guide landing page now cross-links pillar content. Blurbs written fresh, no title duplication. Build green — (this commit)
- 2026-07-25 — [design agent] Conversion hierarchy on the blog post sidebar — the page organic traffic actually lands on. The sticky sidebar offered three CTAs and ALL were high-commitment (quiz, phone call, booking); the only low-friction ask (email field) sat at the article's bottom, which cold SEO readers rarely reach. Added SidebarEmailCapture (new client component): one email field → /api/newsletter/subscribe with source 'blog-sidebar' (so blog conversions are attributable in admin), real error state (never fakes success on a rejected submit), accessible labels/roles, accent palette — placed FIRST in the sidebar, above quiz/call/offer, matching the ask to a cold reader's temperature. Also fixed the sidebar's AA miss: "EXCLUSIVE OFFER" label emerald-500 → emerald-600. Mobile unchanged (sidebar is lg-only; mobile keeps the sticky alerts CTA). Build green — (this commit)
- 2026-07-25 — [improvement agent] BOTH retention channels were running on an 8% sample (mission #3, broken-numbers class). The daily alert pool fetched `/api/listings` with no pagination (default: page 1, 200 rows) and the weekly newsletter explicitly fetched `limit=200&page=1` — against ~2,547 active listings. Consequences: a saved search only matched if a qualifying listing sat in the 200 most-recently-modified (alerts silently under-delivered, and any price/photo update churned the pool), and the newsletter's "top 10 cash-flow deals" were the top of an 8% sample — the market's actual best deals mostly never reached subscribers. The GTA alert pool had the same bug. Fix: lib/listings/fetch-all-listings.js (mirrors the homepage/sitemap pattern — page 1 for the count, remaining pages in parallel, cap 15 pages; page-1 failure THROWS so a send fails loudly instead of emailing from an empty pool; later-page failures are logged and skipped since a partial pool beats no send). Wired into alerts/send (both Mississauga + GTA pools) and newsletter fetchScoredListings. CAUGHT IN REVIEW: my first edit pass printed the newsletter's import context without writing it — the build still passed (unbound identifiers only fail at runtime), which would have crashed Monday's send; verified both imports present before shipping. Unit-verified the helper: 3-page mock → full 400 rows with a failed page skipped+logged, page-1 failure throws, no duplicate rows. Merged to production ahead of today's 13:00 UTC alert send so it runs on the full pool — (this commit)
- 2026-07-25 — [housekeeping] Branch rebuilt from main after #32's squash: cherry-picked the three net-new commits (pillar cluster links, blog sidebar capture, fetch-all-listings in alerts+newsletter). DROPPED my earlier /sell layout retitle + H1 edit on purpose — main independently received a richer /sell page (page-level metadata already targeting "what is my home worth mississauga" via keywords, ValuationForm hero, RealEstateAgent service schema) which supersedes it; page metadata overrides layout metadata anyway, so keeping mine would have been dead code plus a conflicting H1 against a hero that no longer exists — (this commit)
- 2026-07-25 — [seo agent] Deepened the pillar cluster from the neighbourhood guides. The 24 hood pages are the site's established, ranked pages, but none linked the new pillar posts. Added a "Go deeper on the numbers" block on /neighbourhoods/[slug]: Malton links its 2026 guide + the 7-bedroom-math post (the exact hood the rooming-house objection comes from), Cooksville and Hurontario link the LRT-corridor post, and EVERY hood page links the city-wide ranking ("How {name} compares: Best Cash Flow Neighbourhoods 2026") — 24 contextual internal links to the ranking post plus targeted deep-dives, all from pages that already rank. Map keys verified against HOOD_DATA names exactly. Build green — (this commit)
- 2026-07-25 — [improvement agent] TIME-CRITICAL follow-up to the full-pool fix: /api/alerts/send had NO maxDuration, so it ran under Vercel's default function window (~15s). The old page-1-only pool squeaked under it; the fixed pool (13 upstream page fetches + per-subscriber matching + SEQUENTIAL Resend sends) could hit the wall and die mid-send — worst case silently emailing only the first chunk of subscribers, which is exactly the failure mode the send-diagnostics work was built to prevent. Added maxDuration=120 (newsletter uses 60 for one batch; alerts do N sequential sends, so headroom) + explicit force-dynamic. Shipped to production ahead of today's 13:00 UTC send — the first-ever full-pool alert run needed the window to actually complete — (this commit)
- 2026-07-25 — [design agent] Verification pass, no defects found (recording honestly rather than inventing churn): rendered /neighbourhoods/malton on a live dev server and confirmed the new "Go deeper" block ships correctly — all three pillar links present, the per-hood anchor text renders properly (React SSR comment nodes made a naive grep look like a miss; verified the actual markup), no fixed-width overflow risks in the page. Checked /guides for the planned 2-col fix and found it ALREADY two-up on sm+ — assumption was wrong, no change needed. The 13:00 UTC full-pool alert send (first ever, with the new maxDuration=120 window shipped 34 min ahead of it) gets verified from runtime logs right after it fires — (this commit)
- 2026-07-25 — [seo agent] Twitter/X cards were dropping the branded image on 7 key pages: Next.js REPLACES (not merges) the root layout's `twitter` object when a page defines its own, so every page-level `twitter:` block without `images` shipped a card with no image and leaned on X's inconsistent og:image fallback. Added explicit twitter images to blog/[slug] (per-post cover via blogCoverUrl), gta per-city metadata, market-pulse, compare, mortgage-calculator, news, and recent-sales (branded /opengraph-image). LESSON RECORDED: the bulk edit used a naive `[^}]*` regex which broke the build twice over — it chopped gta's single-line twitter block and cut recent-sales mid-`${YEAR}` template literal (the `}` inside the literal ended the match). Both repaired by hand; per-file Edit beats regex for structured code. Build green — (this commit)
- 2026-07-25 — [design agent] Homepage mobile sticky CTA — closed the last page of the sticky-CTA rollout (deferred until the hero one-primary-action audit resolved). "Get Free Deal Alerts →" → /alerts via the proven StickyMobileCTA (works for every auth state, unlike the hero's /signup button); verified rendering in homepage SSR. Item 1c-A sticky list now fully closed — (this commit)
- 2026-07-25 — [improvement agent] Daily alerts no longer resend the same listing up to 4 days running (the DOM≤3 window flaw flagged Jul 20): new alert_sent_listings migration + batched 7-day lookback guard in alerts/send, recorded only after Resend accepts, graceful no-op until Hamza runs the migration (one-time, noted in Needs Hamza). 5 unit cases pass; build green — (this commit)
- 2026-07-25 — [seo agent] Fixed thin/near-duplicate content on the 28 indexable /gta?city= pages. They carry unique titles/descriptions/breadcrumbs and sit in the sitemap, but the listings are client-fetched, so everything a crawler saw below the h1 was BYTE-IDENTICAL across all 28 — one h1 + a one-line subtitle of unique text each, which is why they don't rank. Added a server-rendered "Investing in {City}" section: the city's region (new `region` field on all 28 CITY_COPY entries), its real property tax rate READ from the same TAX_RATES constant the cash-flow engine uses (so the copy can never drift from the cards' numbers), and the per-city rent tiering note. Also fixed a link dead-end: the only way to reach a sibling city was the RegionSwitcher `<select>`, which crawlers can't follow — each city page now links its regional siblings plus /listings, /market-pulse and /mortgage-calculator as crawlable anchors. HONESTY GUARD: new `hasExplicitTaxRate()` export means a city with no researched rate (King) shows NO rate rather than printing the generic 0.95% fallback as if it were King's own — verified Toronto 0.63%, Brampton 1.13%, King none. Copy says the rate applies "whenever a listing doesn't report its own tax figure", matching what process-listings actually does. Verified at a true 375px mobile viewport over CDP: scrollWidth == clientWidth == 375 (no overflow), section right edge 359, body slate-500 on white = 4.8:1 and accent links 5.2:1 (both WCAG AA), single h1 per page. Build green (89 pages). FOUND A WRONG NUMBER (logged in section 2, not fixed here — SEO runs don't touch calc logic): Toronto's municipal LTT is missing from calculateCashOnCash/getClosingCosts, overstating cash-on-cash on every Toronto-area listing — (this commit)
- 2026-07-25 — [improvement agent] Fixed the top wrong-number item: Toronto's MUNICIPAL land transfer tax was absent from every cash-on-cash figure on the site. calculateOntarioLTT was the only LTT function and never saw the city, so closing costs on Toronto-area listings were understated by roughly half (~$16.5K on a $1M purchase), inflating their cash-on-cash and flattering them against Mississauga deals. Added calculateTorontoMLTT + chargesTorontoMLTT (incl. the 5 amalgamated districts) + calculateLandTransferTax(price, city), threaded city through calculateCashOnCash/getClosingCosts at all 3 call sites, and turned the listing-detail "Toronto pays extra MLTT" footnote into an actual line item. Hand-verified against published figures ($1M = $16,475 + $16,475 = $32,950; luxury tiers rebuilt bracket-by-bracket); no-city calls are byte-identical to before, so Mississauga is unaffected. Build green — (this commit)
- 2026-07-25 — [design agent] Measured (not eyeballed) WCAG AA on the homepage's primary email-capture band at a true 375px mobile viewport over CDP: the risk-reversal line "Free weekly email. No spam, unsubscribe anytime." rendered at text-navy/40, 11px = 2.28:1 against the #E6F1FB band — the single least readable string on the page, and it's the copy that answers the one objection ("will you spam me?") standing between a visitor and the subscribe button. Now text-navy/70 at 12px = 5.1:1, re-measured green with zero AA failures remaining in that band. Only occurrence of that token site-wide. NOTE for future design runs: the dusk-hero text (text-white/40 "Popular:" label etc.) can't be measured this way — the hero background is a gradient, so getComputedStyle backgroundColor is transparent and any ratio computed from it is meaningless; it needs a pixel-sampling check instead — (this commit)
- 2026-07-25 — [seo agent] Ran a full metadata/schema audit across every built route (canonical + og:image + JSON-LD + h1 count): hygiene is clean — every indexable page has a self-canonical, an og:image and exactly one h1, and the only pages without canonicals are the correctly-noindexed ones (admin, login, signup, saved, profile, recent, 404). The real gap it surfaced was CONTENT on /listings, the site's money page (title/H1 exact-match "investment properties for sale mississauga", the top commercial query): the listings arrive client-side, so a crawler saw four short paragraphs and no FAQ markup at all, while five lesser landing pages already carry FAQPage schema. Added a 5-question buying-side FAQ + FAQPage JSON-LD, deliberately DISJOINT from the /faq set (which covers the product, deal score and Hamza) so the two pages never publish competing FAQ markup for the same rich result. Questions target real transactional long-tail: is this every listing, down payment on an Ontario rental (20% — no default insurance on non-owner-occupied), closing costs, legal basement suites, best cash-flow areas. Every figure is one the engine itself uses (the $16,475 Ontario LTT on $1M was hand-verified this session; $2,500 legal / inspection come from getClosingCosts; the "legal counted in full, potential discounted" claim matches deal-score.js:161's 0.85 factor) so the copy cannot drift from the calculator, and the Toronto municipal LTT sentence is accurate as of this morning's engine fix. Verified on a clean prod build: 1 FAQPage block, 5 questions, every schema answer present in the visible HTML (no invisible-markup violation), single h1. Verified at a true 375px mobile viewport over CDP: no overflow (scrollWidth == clientWidth == 375), cards stack single-column, answer text 7.6:1 contrast — (this commit)
- 2026-07-25 — [improvement agent] Email-first /alerts form (lead-capture friction, section 1c-A's top open item). /alerts is where the primary "Sign Up Free" CTA and the new homepage sticky bar both land, and it presented six fields when only email was required — the four filter selects already defaulted to "Any". Email + button are now all that's visible; name and the filters live in a native <details> one tap away, still a SINGLE POST (no step-2 state machine, so no lead can be stranded mid-flow) with every filter and capture path preserved. Measured at true 375px: 298px collapsed vs 721px expanded, no overflow, 60px tap target, and the button now sits above the fold on a phone. Build green — (this commit)
- 2026-07-25 — [design agent] Echoed the "first month's mortgage on us" risk-reversal onto /book-call, above the calendar. It previously existed in exactly one homepage band, so anyone arriving via a listing's "Book a Viewing" — the highest-intent path on the site — hit the time-slot picker without ever seeing the strongest reason to book. Same honest copy, nothing new claimed. Caught two defects in my own first draft before shipping: success-green on a light tint measures ~2.2:1 (fails AA — the trap already fixed twice on this site) AND `text-success-dark` isn't a token in tailwind.config.js so it would have silently rendered as inherited colour; shipped accent-dark on accent tint, measured 6.7:1 chip / 14.2:1 body, no overflow at 375px — (this commit)
- 2026-07-25 — [seo agent] Audited technical SEO hygiene site-wide and found it genuinely clean — every internal link across all 89 built pages resolves to a real route (zero broken links), every <img> has a non-generic alt, and no page skips a heading level. Also measured Core Web Vitals at 375px on a production build and found no real problem: the homepage's alarming 1,548ms FCP turned out to be cold-start (warm: ~320ms), and the "extra render-blocking CSS on the homepage" I thought I saw was an artifact of my own script counting preload <link>s as stylesheets — only two CSS files exist. Recording both as clean rather than inventing churn. The one REAL defect the sweep surfaced: Leaflet's stylesheet was pulled from unpkg.com at runtime despite leaflet being an installed dependency — now bundled same-origin as its own code-split 12K chunk, with zero unpkg references left in the build (details in section 1c-E). Build green — (this commit)
- 2026-07-25 — [improvement agent] Removed the hardest block on the site's #1 conversion path: /signup (where the homepage hero CTA lands) REQUIRED a phone number, and /api/lead independently rejected any source='registration' post without one — so a phone number was mandatory to browse scored listings, the single most-abandoned field in any signup form. Phone is now optional on BOTH sides (relaxing only the client would have swapped a visible block for a silent 400), still fully validated whenever supplied so a typo'd or obviously fake number can never reach Hamza as a dead contact. Exercised all five API paths against a production build: no phone → 200 (was 400), valid phone → 200, "123" → 400 "valid phone number", 555-555-5555 → 400 "real phone number", missing email → 400. NOTE FOR HAMZA: this trades guaranteed phone numbers on registrations for more registrations — the field is still asked for prominently with an explicit reason ("add it and we'll text you when a new deal matches"); say the word and I'll make it mandatory again. Build green — (this commit)
- 2026-07-25 — [design agent] Site-wide footer AA fix (details in section 4). Every page's mandatory TRREB/PropTx/CREA/PIPEDA disclosures were 10px at ~2.4:1 and the Privacy Policy / Terms links at 2.23:1 — unreadable legal text under a site selling trustworthiness — plus every on-navy accent link at 2.75:1, including the tap-to-call phone number. Raised to white/60-70 at 11px and #8AB6FF for on-navy links (the site's existing on-navy blue, 6.9:1), and darkened the capture forms' required asterisk. Re-measured zero AA failures across /signup, /alerts, /, /listings, /blog. Process note for future runs: my first "still failing" re-measurement was a STALE READ — a long-lived headless Chromium had the old CSS cached and a zombie `next start` held port 3000; always kill both and cache-bust before trusting a re-measure — (this commit)
- 2026-07-25 — [seo agent] /market-pulse now answers the informational market queries it ranks for. Verified first that the remaining hygiene gaps really are closed (every indexable page is in the sitemap; the LocalBusiness/RealEstateAgent schema already carries full NAP, geo, areaServed, hours and sameAs — nothing to add), so this run went after content instead: a 5-question FAQ + FAQPage schema covering months of inventory, buyer's-vs-seller's market, average vs median, sale-to-list, and data provenance. CRITICAL CONSTRAINT respected — the answers are DEFINITIONAL and contain zero figures: this page's numbers change with every TRREB Market Watch, and schema in particular can be shown by Google long after a refresh, which is exactly how the hardcoded-stat problem bit before. Thresholds quoted (SNLR <40/40-60/>60, inventory <4/4-6/>6 months) are the standard TRREB/CREA balance bands, and each answer points at the live tile rather than restating it. Question set is disjoint from /faq and /listings so no two pages compete for the same rich result. Rendered in the SERVER layout beside the schema it mirrors (the page itself is a client component; keeping the copy in two files is how visible text and markup drift apart) — verified all 5 answers appear in the page HTML, no hardcoded price figures in the schema, single h1, and no overflow with cards stacking single-column at 375px. ALSO fixed a pre-existing AA failure the sweep caught on this page: the neighbourhood year-over-year figures, the negotiation-gap number and the sold-comp deltas rendered in success-green at 2.54:1 — unreadable DATA, which is the exact class of defect the mission calls out — now emerald-700/red-600, re-measured zero failures — (this commit)
- 2026-07-25 — [improvement agent] Closed the credibility gap in both email channels: the daily alert and weekly newsletter showed cash-flow and cap-rate figures with NO stated rent assumption, even though every listing card on the site states one (the investor critique that drove that change never reached the emails). Both now render "Assumes $X/mo rent" with the suite/multi-unit breakdown, reading the identical fields the site card reads so the two can never diverge, and rendering nothing when rent is unknown. Unit-verified 7 shapes + a live newsletter preview render. Build green — (this commit)
- 2026-07-25 — [design agent] Systemic fix for unreadable DATA figures — the exact defect class the mission names ("investors trust numbers"). The brand success token #10B981 measures ~2.5:1 on white and red-500 ~3.8:1, and both were being used for the numbers themselves, not decoration: the CASH FLOW column in /listings table view (the single most important figure on the site), the "Incl. +$X/mo basement income" line on every card with a suite (at 10px), the estimated-value delta, the homepage deal-card cash-flow chip, the /recent cash-flow column, the mortgage calculator's monthly cash-flow result, and the Suite/negative tags. All moved to emerald-700 (~5.3:1) / red-600 (~4.8:1); the basement-income line also went 10px → 11px. Decorative SVG check icons and tinted chip backgrounds left alone. Re-measured at a true 375px mobile viewport: zero AA failures on /, /listings and /mortgage-calculator. This closes the same token that has now been caught four separate times (homepage proof text, blog sidebar label, /market-pulse YoY figures, and my own book-call draft) — worth treating `text-success` on a light background as a code smell in review — (this commit)
- 2026-07-25 — [seo agent] /recent-sales — found by ranking every indexable page by server-rendered word count, the same lens that caught the GTA city pages and /listings. At 586 words (most of it shared header/footer) it was the thinnest page with real query demand: it targets "mississauga sold prices {year}" but the sales table is client-fetched, so a crawler saw little beyond the h1. Added a 5-question sold-data explainer + FAQPage schema answering what a searcher for sold data actually wants: whether sold prices are public in Ontario (they are NOT published on public portals — they come through a REALTOR® or a board-agreement VOW, which is exactly what this site is, and is the page's genuine differentiator; wording matches the VOW bona-fide-interest disclosure already in the footer), why sold beats list price, how to read the list-to-sold gap, how fresh the data is (honest about reporting lag), and how to build an offer from comps. NO figures in the copy or schema — the numbers here change with every sale and schema can be surfaced long after a refresh, the same discipline applied on /market-pulse. Question set disjoint from /faq, /listings and /market-pulse so no two pages compete for one rich result. Server-rendered content 586 → 1,022 words; verified 1 FAQPage block, all 5 answers present in the HTML, no hardcoded figures, single h1, zero AA failures and no overflow with cards stacking single-column at 375px — (this commit)
- 2026-07-25 — [improvement agent] Finished the disclosure chain the rent-assumption work started, and fixed a latent wrong-number bug on the page whose entire job is disclosure. (1) Both emails now state the FINANCING basis behind every CAP/cash-flow figure — "assumes 20% down at 4.89% over 25 years, plus property tax, insurance, maintenance and vacancy" + a link to /score-methodology — because a subscriber reading "+$312/mo" previously had no way to know whether that assumed 20% down or 35%. (2) /score-methodology HARDCODED every assumption as a literal string ('20%', '4.89%', '25 years', '$225/month', '8%', '5%', '0%'), so any engine change would have left the methodology page quietly describing a model the site no longer uses — the exact "wrong number is the worst bug" failure, in the worst possible place. All eight rows now interpolate DEFAULT_ASSUMPTIONS, and the emails build their line from the same constant, so engine and disclosure cannot drift. (3) That page's closing-cost row still read "Ontario land transfer tax + $3,000" — stale since this morning's Toronto MLTT fix; it now notes that Toronto and its amalgamated districts also pay the municipal LTT, which is included for those listings. Verified in a live newsletter preview render and on the rendered methodology page. Build green — (this commit)
- 2026-07-25 — [design agent] Daily alert email brought onto the dusk identity (last item in the 4b rollout queue; the newsletter's magazine identity left alone as Hamza's own direction). Masthead gets the brand dusk gradient with bgcolor/solid fallbacks so gradient-blind clients like Outlook land on exactly the old look, plus a gold kicker. Fixed four measured AA failures in the process: the ".ca" at 2.75:1 on navy, the PCF cash-flow figure at ~2.4:1, and the score badge's white-on-green/amber at ~2.4:1 — all now 4.4–6.9:1, closing the same data-legibility class in email that was fixed on the site earlier today. Verified by rendering the real template and screenshotting it. ALSO CHECKED, no change needed: tap targets at 375px across 9 pages (homepage, listings, alerts, market-pulse, recent-sales, neighbourhoods, sell, quiz, faq) — zero controls under the WCAG 2.5.8 24px minimum, recorded honestly rather than manufacturing a fix — (this commit)
- 2026-07-25 — [seo agent] /pre-construction now has explanatory content + FAQPage schema. It targets real query volume ("pre construction condos mississauga") but was a VIP signup form with no explanation and no FAQ markup. Added 5 questions covering what actually stops an investor signing: staged deposit structure, interim occupancy and its fees (a real carrying cost most brochures omit), the 10-day statutory cooling-off period under Ontario's Condominium Act, whether investors qualify for the HST rebate, and an HONEST pre-con-vs-resale comparison that states the downsides (no rent during construction, occupancy fees, slipping completion dates) — a page that pretends there are none reads like a builder's brochure and earns no trust. NO dollar figures or dates in copy or schema: the enhanced HST rebate is bounded by signing date and tiered by price, so /pre-construction/hst-rebate owns those numbers and this links to it. Server content 763 → 1,255 words; also darkened the VIP form's required asterisks (red-400 → red-600). CAUGHT MY OWN BUG BEFORE COMMIT: I first put the FAQ in pre-construction/layout.js, which ALSO wraps /projects and /hst-rebate — so the identical markup published on all three pages and left hst-rebate with TWO competing FAQPage blocks. Moved it into the index page; verified each precon page now has exactly one (or zero) FAQPage block and the question appears on one URL only.
- 2026-07-25 — [seo agent] NEW FINDING for a future run (pre-existing, not introduced here): a cross-page duplicate-schema audit shows THREE FAQ questions — "What are the best neighbourhoods to invest in Mississauga?", "Which Mississauga neighbourhood has the best rental cash flow?" and "Is Mississauga a good place to invest in real estate in 2026?" — published identically on ALL 25 neighbourhood pages (the index plus 24 hood pages). That is 25 URLs competing for one rich result with byte-identical answers, the exact pattern the per-page question sets have been kept disjoint to avoid. The fix is per-hood questions on the detail pages (the hood name and its live stats make genuinely distinct answers) with the city-wide three kept only on the /neighbourhoods index. Worth one focused run — (this commit)
- 2026-07-25 — [improvement agent] Finished the phone-optional work across every capture path: the signup-gate modal's step 2 still blocked on a phone number, contradicting /signup and /api/lead which both dropped that requirement earlier today — and it did so on the highest-intent surface on the site (a specific property's analysis), after the visitor had already given a real email and name. Phone optional there now, still validated when supplied; email and name remain required. Build green — (this commit)
- 2026-07-25 — [design agent] Legibility pass on the two remaining unmeasured surfaces, both found by sweeping rather than guessing. (1) /book-call — the page EVERY listing's "Book a Viewing" lands on — rendered its unavailable calendar dates at text-slate-300 = 1.48:1 and its booked time slots at slate-300 on slate-100, i.e. effectively invisible; the grid read as broken rather than as "these days are unavailable". Being disabled controls these are formally EXEMPT from WCAG 1.4.3 (stating that honestly rather than calling it a violation), but invisible is not the same as unavailable, so both moved to slate-400 — still obviously inactive beside navy active dates, now actually perceivable. Its required-field asterisk also went red-400 → red-600. (2) /news live-feed ticker: the market delta figures (e.g. "-2% YoY") were red-500 at 3.76:1 and success-green at ~2.5:1 — a REAL AA failure on data, the same class fixed site-wide earlier today; now red-600/emerald-700. Verified in the built client chunks (slate-300 and red-400 both gone from the book-call bundle) because a stale `next start` on port 3000 kept serving old CSS to the measuring browser — the same trap logged earlier; build artifacts are the reliable check when that happens.
- 2026-07-25 — [design agent] ALSO MEASURED, no change made: the open "reduce homepage time-to-capture" item assumed the weekly-email capture sits far down the page. It does not — on a 375x812 phone the capture band starts at 1,487px and its input at 1,715px, i.e. 2.1 screens down and the THIRD section on the page, with the sticky CTA covering everything below that. The premise doesn't hold, so the item should be closed rather than acted on; recording the measurement instead of manufacturing a change — (this commit)

## Hamza's advisor list (2026-07-25) — triage + status

Worked top-down. Items marked DONE shipped in this commit; the rest are either already in place, blocked on Hamza, or need a decision I should not make alone.

- [x] **1. Brokerage string + build check** — the sanitizer already removes the old brokerage name at render AND permanently via the auto-blog cron (shipped Jul 25, verified 0 live occurrences). What was MISSING is what the advisor actually asked for: a gate. New `scripts/compliance-check.mjs` runs as the first step of `npm run build` and FAILS the build on the old brokerage name (any spacing/casing, and on "Signature Realty" alone) or a U+FFFD mojibake character, anywhere in app/, components/, lib/, public/ or content files. Negative-tested: planting the string in a component exits 1, removing it exits 0. Internal docs that describe the incident (this backlog, CLAUDE.md) and the sanitizer itself are allowlisted, since they must contain the string to do their job and are never published.
- [ ] **2. IDX vs VOW field table for the broker of record** — NEEDS HAMZA to send. I can generate the table (public vs gated field inventory) in a follow-up run; the actual confirmation with TRREB/the brokerage is his call, not something to automate.
- [ ] **3. Written broker approval on the "First Month's Mortgage On Us" copy** — NEEDS HAMZA. Cannot be done from here. NOTE: I echoed this offer onto /book-call earlier today; if the broker asks for changed wording, it now appears in two places (homepage band + booking page).
- [x] **4. Ungate CAP + deal score** — the real defect the advisor spotted: the hero printed a cap rate in full while the card below showed a lock on the SAME property, which reads as a trick. CAP is now never gated on either the homepage cards or the /listings cards, and the rent assumption renders for gated cards too (a cap rate with a hidden rent assumption is exactly the unverifiable number the rent work set out to kill). Still gated: monthly cash flow and cash-on-cash — the financed, assumption-heavy figures — plus the listing page's full breakdown/BRRR. Lock glyphs replaced with "Free", which states the offer instead of the restriction.
- [x] **5. Rent shown on every card** — verified, not just claimed: RentAssumption renders on /listings + /gta cards, the homepage deal cards carry the short form, and as of earlier today both the daily alert and weekly newsletter state it too.
- [~] **6. One primary CTA per page** — largely already done (homepage hero audit, sticky CTAs, per-page primary actions). Worth one more focused design pass to confirm blog/cold traffic points at the Monday email and listing/sell pages at Book-a-Call.
- [x] **7. "�" encoding bug** — already fixed at render + permanently in the DB (Jul 25); the new build check now prevents reintroduction.
- [x] **8. Unify the listing count** — REAL and confirmed: the homepage claimed "2,000+ Properties Analyzed" while /about claimed "1,800+" for the SAME metric, with /sell and the auth panel each carrying their own. One `PLATFORM_STATS` constant in lib/constants.js now feeds all four, using the conservative figure and documented as a CUMULATIVE claim that must never be used for live inventory (that comes from the feed). Caught a bug in my own fix before shipping: the /sell edit landed inside a single-quoted string, which would have printed a literal `${PLATFORM_STATS...}` on a live page.
- [ ] **9. Server-render /listings and listing detail** — AGREED this is the biggest SEO lever, and it is correctly scoped as a 30-day item, not a "right now" one. It is a genuine architecture change (both are client-fetched to dodge AMPRE timeouts — see the comment in gta/page.js), it cannot be verified locally without AMPRE_TOKEN, and a botched attempt takes down the money pages. Planned approach for a dedicated run: ISR with generateStaticParams over the top-N listings + revalidate, keeping the client fetch as the fallback path, shipped behind a canary of a few listings before the full 2,500. Partial mitigation already shipped: server-rendered content and FAQ schema on /listings, /gta city pages, /market-pulse, /recent-sales and /pre-construction, so those no longer serve an empty shell.
- [x] **10. JSON-LD** — already complete and verified this session: RealEstateListing on listing detail, FAQPage on neighbourhoods + /sell (+ /faq, /listings, /market-pulse, /recent-sales, /pre-construction, the 5 landing pages), Article on blog posts, RealEstateAgent/LocalBusiness sitewide with full NAP/geo/hours/sameAs.
- [x] **11. Sitemap coverage** — audited this session: every indexable page is in the sitemap (listings, GTA city pages, hood guides, blog, static); the only omissions are correctly-noindexed pages. Submitting/refreshing in Search Console is Hamza's action.
- [ ] **12. Rent model recalibration — NEEDS HAMZA, and deliberately NOT auto-changed.** Verified the mechanics of the claim: HOOD_RENTS Malton 4-bed is $3,650, +$250 for detached = **$3,900/mo**, so the advisor has read the model correctly. What I cannot verify from this environment is the market side ($2,800–$3,500), and HOOD_RENTS drives estimatedRent → cash flow → cap rate → CoC → deal score for EVERY listing, so cutting it ~15% on one unverified assertion risks introducing the exact wrong number the mission calls the worst bug — in the opposite direction. Instead shipped `scripts/verify-rent-model.mjs`, which compares every hood/bedroom row against real recently-leased MLS comps via the site's own /api/rental-comps (12-month window) and prints the gap, flagging anything beyond ±10% as MODEL HIGH/low. It needs the AMPRE token, so run it against production: `node scripts/verify-rent-model.mjs`. Hamza: run that (or tell me the right Malton numbers) and I will recalibrate the table in one pass with the evidence attached.
- [ ] **13. Consolidate duplicate rental-analysis / cap-rate posts with 301s** — 60-90 day item. Needs a live content inventory (Supabase posts) to identify the duplicates; queued.
- [ ] **14. Build the missing high-intent pages** (rent by bedroom 2026, legal second-unit registration, Mississauga vs Brampton vs Hamilton) — queued for the SEO agent; these are exactly the striking-distance page pattern already proven. NOTE: "rent by bedroom 2026" should not be built until item 12 is resolved, or it will publish the same rent numbers currently in question.
- [ ] **15. Volition / Durham REI appearances** — Hamza's own action.
- 2026-07-26 — [improvement agent — Hamza: "run the rent model check against production"] Ran it. The check itself surfaced a bug that made per-neighbourhood calibration impossible: /api/rental-comps ACCEPTED lat/lng but never used them — fetchLeaseComps filters on `City eq 'Mississauga'` and BedroomsTotal ±1 only — so every "comp" came from anywhere in the city while the response was labelled "similar leases NEARBY". Investors read that as neighbourhood-level evidence on listing pages; it was city-wide. Fixed: new `fsa` param applies `startswith(PostalCode, 'L4T')`-style scoping, PostalCode is selected and returned per comp, and the labels now say "in this area" only when actually scoped, otherwise "across Mississauga". verify-rent-model.mjs now scopes each row by the hood's FSA (via FSA_TO_HOOD) instead of sending lat/lng the API ignored. PRODUCTION EVIDENCE gathered with the deployed (pre-fix) endpoint, on the Malton claim specifically: city-wide DETACHED 3-5 bed leased median = $3,375 over 20 comps in 12 months, and that sample includes exactly one Malton listing — 3428 Monica Drive, L4T, 3-bed detached, leased at $2,800 (DOM 50). Our model says Malton 3-bed detached $3,300 and 4-bed detached $3,900. So the model sits ~16% ABOVE the city-wide detached median for the city's CHEAPEST neighbourhood (Malton avg price $618K vs city ~$1.01M), and ~18% above the one real Malton comp. That is three independent signals — the advisor's $2,800-3,500 read, the city-wide median, and the L4T comp — all pointing the same way: the Malton rents are too high. STILL NOT AUTO-CHANGED: HOOD_RENTS drives cash flow, cap rate, CoC and deal score on every listing, and a full recalibration needs the per-hood run, which requires this FSA fix to be deployed first. NEXT: once this ships, `node scripts/verify-rent-model.mjs` returns a real per-hood/per-bed table and I recalibrate in one evidenced pass — (this commit)
- 2026-07-26 — [seo agent] Fixed the duplicate-schema defect logged last run, and it was worse than first measured. `neighbourhoods/layout.js` rendered the city-wide FAQ, a directory ItemList AND a generic breadcrumb — but a layout also wraps [slug], so all three published on the 24 hood detail pages too. Every detail page therefore carried TWO FAQPage blocks and TWO BreadcrumbLists, and three questions ("What are the best neighbourhoods to invest in Mississauga?", "Which Mississauga neighbourhood has the best rental cash flow?", "Is Mississauga a good place to invest in real estate in 2026?") were published byte-identically on 25 URLs competing for one rich result. The ItemList and breadcrumb were ALSO straight duplicates of ones page.js already renders on the index. Fix: layout is now metadata-only (with a comment explaining exactly why nothing else belongs there — this is the same trap I fell into on /pre-construction yesterday and caught pre-commit), and the city-wide FAQ moved to the index page where it belongs. Each hood page keeps its OWN per-hood FAQ, which was already genuinely distinct (name, trend, yield, DOM, price, rents). VERIFIED on a clean build across all 25 pages: exactly 1 FAQPage + 1 BreadcrumbList each, ItemList only on the index, and ZERO FAQ questions appearing on more than one page site-wide.
- 2026-07-26 — [seo agent] Same run, data-legibility sweep on the hood pages (the site's established ranked pages) since the measurement was already open: the worked cash-flow example on every hood guide rendered its figures in red-500 (3.76:1) and green-600 (3.3:1), the index's YoY figures in success-green (2.42:1), the inventory label and sold-comp deltas in red-500, the "Live" pill in success-on-success-tint, and the mortgage calculator's result rows in green-600/red-400 (2.77:1). All moved to emerald-700/red-600. Verified in the BUILD ARTIFACTS rather than a live measurement — a stale `next start` was again serving old CSS to the measuring browser, the recurring trap; the prerendered hood HTML now has 0 red-500 and the index chunk 0 text-success. The one remaining green-600 is the header's tap-to-call ICON, which at 3.3:1 passes the 3:1 bar for graphical objects — left alone deliberately — (this commit)

- 2026-07-26 — [improvement agent — Hamza/advisor: "shut up and do the SSR fix"] **/listings and all ~2,500 listing detail pages are now SERVER-RENDERED.** Root cause was never the 'use client' boundary — client components DO server-render their markup; the pages were empty because their DATA arrived in a useEffect after mount, so Googlebot got a shell. Fix: (1) /listings is an async server component with ISR (revalidate 600) that fetches page 1 of inventory, runs it through the SAME processListings pipeline the cards use, and hands it to ListingsContainer as `initialListings` — which already skipped its own fetch when those are present, so filters, sorting, map and gating are behaviourally unchanged. (2) The detail page's client component moved to listing-detail-client.js and now takes `initialListing`; the new page.js is a server component (ISR 900) that fetches via /api/listing-single and processes it server-side so the rendered numbers match the hydrated ones exactly. Both degrade to the OLD behaviour on a feed failure (null/[] → the client fetch runs), so a bad MLS response is a slow page, not a broken one. VERIFIED WITH JS NEVER EXECUTED (curl against a production build, APIs temporarily stubbed with fixtures since this environment has no AMPRE token, fixtures reverted after): /listings HTML contains the address, 5 listing links and neighbourhood names; /listings/{id} HTML contains the address, $615,000 and the neighbourhood. TWO BUGS CAUGHT IN MY OWN CODE DURING THAT VERIFICATION — both would have shipped silently: the host check matched only "localhost" so a "127.0.0.1" host built an https:// URL against a plain-http server, TLS-failed into the catch and rendered empty; and the detail page keyed its origin off NODE_ENV, which is "production" under `next start` locally too. Also lost ~40 minutes to a zombie `next start` holding port 3000 and serving a stale build — every "still broken" reading until I moved to port 3007 was that, not the code. NOTE FOR THE NEXT RUN: /gta is still client-fetched and needs the same treatment (its comment cites AMPRE timeouts across 30+ cities, so it needs per-city ISR rather than a straight copy of this) — (this commit)
- 2026-07-26 — [improvement agent — Hamza: "now do /gta"] /gta and all 28 /gta?city= pages are now SERVER-RENDERED, completing the SSR work. This page was the one with a real reason to be client-only — its comment cited SSR timeouts on the whole-GTA query across 30+ cities — so rather than ignore that, the server fetch is bounded by AbortSignal.timeout and ANY failure (slow feed, upstream error, abort) returns [] and hands rendering straight back to ListingsContainer's client fetch, i.e. exactly today's behaviour. The single-city pages get an 8s budget because they query one city and carry the indexable SEO value; the hub gets 4s because it is the heavy query and is not where the ranking value sits. VERIFIED on a clean port against a production build with JS never executing: /gta?city=Toronto renders the Toronto listing and correctly EXCLUDES the Brampton one (server-side city filtering works), the hub renders both, and — the test that matters for the original timeout concern — a deliberately 12-SECOND feed against the 4s budget still returned a complete page in 0.46s, falling back to the client path instead of hanging. Fixtures used for all of this (no AMPRE token here) and fully reverted; git confirms 0 changes to the API routes. SSR item from the advisor list is now closed for /listings, /listings/[id] and /gta — (this commit)
- 2026-07-26 — [design agent] Fixed the "018.6"/"02$" step numbers the advisor spotted — but NOT the bug it looked like, and worth recording accurately: the step numbers are hardcoded '01'/'02'/'03' and render correctly on screen. The garbling came from the illustrations: BrowseScene and AnalysisScene carried decorative <text>8.6</text> and <text>$</text> inside their (already aria-hidden) SVGs, sitting immediately after the 5%-opacity step watermark, so anything reading the page AS TEXT — scrapers, and Google's text extraction — saw "018.6" and "02$". Two changes: the near-invisible watermark became a real accent numbered badge (white on accent, 5.2:1, better hierarchy for a "How It Works" anyway), and the decorative numerals became pure geometry (a checkmark in the score badge, a currency glyph drawn as strokes) so the illustrations look the same at their 28px render size while the text stream stays clean. Verified on a production build: "018.6", "18.6" and "02$" are all absent from the served HTML, 0 <text> nodes remain in the scenes, build green. HONEST LIMIT: verified the text extraction and the markup, not a scrolled screenshot of the section — headless --screenshot won't scroll to an anchor — (this commit)
- 2026-07-25 — [seo] Site-wide metadata audit (26 routes) came back healthy — every page 200 with unique title/description/canonical/OG-image, all in sitemap, no duplicate titles or descriptions, exactly one h1 each. Found ONE real defect: Google requires FAQPage schema to be visible on the declaring page, and /pre-construction/hst-rebate (5 Q&As) plus /neighbourhoods (3 Q&As) declared schema with NONE of it on screen — no rich-result eligibility + manual-action exposure. Both now render a visible details/summary accordion from the SAME array the schema is built from, so they cannot drift. Verified: each affected page type emits exactly 1 FAQPage block with 100% of questions visible (/clarkson also confirmed down to 1 block after the agent's layout-leak fix) — (this commit)
