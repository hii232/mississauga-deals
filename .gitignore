# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# local env files
.env*.local
.env

# python virtualenv - CLAUDE.md's TRREB refresh process creates one here to
# run scripts/trreb-extract.py (`python3 -m venv .venv && ./.venv/bin/pip
# install pypdf`), so every run that touches market data leaves one behind.
.venv/

# vercel
.vercel

# reference files (old CRA code)
/_reference/
